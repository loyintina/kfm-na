# 解析页设计（两轴插件契约）

> 状态：**宪法 v2（2026-09-21：§四 三区排布改版 + ④ 服务/连接卡
> 本地相 B 案拍板；初版 2026-09-20 用户拍板两轴原则）**。解析页
> （终端右侧滑出的解析器页）的唯一设计来源。动解析页结构/加卡/
> 加终端前必读。
>
> 一句话：解析页 = **当前对象 × 已注册插件卡** 的装配结果——对象轴
> （Endpoint）与能力轴（插件卡）正交，互不感知对方存在。

## 〇、由来（为什么是两轴）

用户 2026-09-20 拍板的两条适配律：

1. **新终端律**：未来接入其他终端（不止服务器/本地），现有卡不许改；
2. **新功能律**：未来增加新功能卡（不止现有三张），现有终端不许改。

两相 if/else 在第三个终端进来时就是灾难，故两轴从第一天就是注册表，
不是分支。规格原型照抄 nz 移交的 `plugin-contract.md`（契约签名/
脑皮分离/槽位唯一主人/清单外状态即 bug），Rust 本地化。

## 一、通用设计标准（宪法定位回顾）

手机屏幕三主框：**中央是对象主体，左侧是路由（切换中央对象），右侧
是解析器（解析中央对象）**。解析页因此不自己维护「显示哪一相」的
开关——**中央终端当前连着谁，就解析谁**（跟随 `session_router` 与
设置页 terminal.json 裁决的当前对象）。光球（上侧）与配置页（右侧
齿轮入口）是全局件，不在本契约范围。

## 二、对象轴：Endpoint（数据源提供方）

每个终端对象实现同一组**能力面**；能力给不出 = 显式 None，插件卡
据此进降级相，不许对象假装能给。

| 能力 | 服务器相现状 | 本地相 | 未来 N 终端 |
|------|------------|--------|------------|
| `exec(命令) → 输出` 短命执行通道 | ws exec 跑 tmux 命令（tmux_exec） | local PTY exec（exec_probe 已证可行） | 各自传输 |
| `sys_info() → Option<SysInfo>` 体征 | HTTP 轮询 /api/na/sys（svc_health） | `na_sys::collect("/data")` 直读 | 各自采集 |
| `health() → 会话清单` | HTTP /api/na/health | 本地 PTY/进程表 | 各自协议 |
| `link_snap() → 链路状态` | tunnel::snap()（隧道即链路） | 恒在线（回环） | 各自链路 |
| `display_word() → 对象词` | settings 里的服务器名 | 「本地」 | 自报 |

决策记录（2026-09-20）：**Exec / Query 两条腿，不统一单入口**——
tmux 卡吃短命执行通道、环境/服务卡吃轮询快照，数据面本不同构，
贴现状抽象，不为对称而对称。

对象轴的注册机制复用 **cordis-na `Plugin`/`ServiceKey`**（
`conn_provider_ws`/`conn_provider_local` 已是毛坯）：Endpoint 作为
服务注册，插件卡按 ServiceKey 寻址当前对象。

## 三、能力轴：插件卡（声明契约 + 自渲染）

每张卡 = nz 契约签名的网格涂装版：

- **id + 数据契约声明**：「我需要 SysInfo / 会话表 / 链路快照」——
  只认数据类型，不认来源（HTTP 还是 /proc 与卡无关）；
- **脑/皮分离**（现状已是）：`ui/*_card.rs` 文案合成纯函数 = 脑，
  termview 涂装段 = 皮，layout/hit = 几何；
- **自报高度** `card_h()`：排布器不问卡的内部账；
- **动作意图**：重排/新窗/重连 = 卡发意图，经当前对象的 exec 通道
  执行——卡不直连传输层；
- **降级显形**：对象给不出契约数据时，卡进自声明的降级相
  （「—」/「本对象不支持」占位），降级形态写进契约，不许各卡
  现场发明；清单外状态 = bug（nz §7 思想）。

## 四、三区排布器（槽位层；v2 2026-09-21 用户拍板改版）

v1（2026-09-20）= 竖链 + 单页滚动，已收编为 `parser_chain.rs`。
v2 用户拍板（「tmux 竖排放右下角常驻……保证右滑后直接手指能点到
切换」）改版为**三区**。立法理由：排版服从操作频率——tmux 切换是
解析页唯一高频动作，其余卡都是「看一眼」的只读信息；右下 = 单手
握持拇指热区，高频动作钉热区常驻，「右滑→点窗口」恒定两步。

- **区域划分**：页面分两竖列——**右列固定网格宽**（列数 = 实现
  参数，约束 = 刚好放下会话框「名 + ✕」加左右留白，不随屏宽变）；
  **左区 = 剩余全部宽度**（环境卡/连接卡字段值是长串，必须吃宽区）。
  右列再分两段：**右下常驻区**（tmux 卡竖排 1×N，钉视口底）+
  **右上滚动区**（连接·服务合并卡——通联状态与 tmux 是「操作 +
  状态」的天然配对）。左区 = 一个滚动区（环境卡 + 未来新卡）。
- **常驻语义**：tmux 卡永不被滚出屏。卡自身维持既有上限（6 窗）+
  内部滚动；常驻区高度 = 卡自报高，封顶视口（超高内滚）。
- **滚动分流**：垂直拖拽按落点 x 命中分流——落左区滚左账，落右上
  区滚右上账，落常驻区喂卡内滚动；两本滚动账各自独立钳制（v1
  page_scroll 钳制语义逐份继承）。三区同吃 bottom_inset（BAR-120
  线视口化契约不变：区底 = 输入栏带以上）。
- **卡的归属**：卡→区 = 注册表静态声明（槽位属性）。卡对排布零
  感知原则不变——不 import 邻卡常量、不问自己排第几、不知道自己
  在哪个区；自报高度 `card_h()` 契约不变。
- **排布器输出**：三区几何（左区窗 / 右上区窗 / 常驻槽）+ 两本
  滚动账唯一源；卡侧 `layout_in` 排布器配给制签名不变，v1 四笔账
  （slot_top/chain_bottom/reserved_below/slot_rect）按区重组。

## 五、试金石与破例律

**现有三卡作为首批插件迁入**（行为零变化，既有钉全保）是设计对错
的试金石：迁进去不需要为任何一张卡破例 = 轴切对了；要破例 = 轴
切错了，当场返工契约，不许带病前进。

## 六、落地阶段

1. ✅ 摸底（2026-09-20）：耦合点清单见本文件 §二表与 bugs 外存档
2. ✅ 本宪法（先修宪）
3. ✅ Endpoint 注册表 + 当前对象核（`src/endpoint.rs` 纯逻辑；
   A 档 4 考题 4 变异全咬：两席有序/能力面全开/设置映射/翻相
   epoch——壳消费从第 4 步起）
4. ✅ server 相 exec 腿迁入（2026-09-20）：tmux 卡 exec 通道唯一源
   = `endpoint::plan_exec` 裁决（Ws/LocalPty/NoServer），壳
   `endpoint_exec_ok`+`endpoint_exec` 两臂，kill/refresh/reflow/new
   四调用点 + 自动刷新闸全部收编（报错文案/静默语义逐字节不变，
   attach 两臂走连接重孵不在 exec 腿）。零行为变化；`tmux_exec::exec`
   全仓唯一直调点收进 endpoint_exec
5. ✅ 卡链排布器收编（2026-09-20，`src/ui/parser_chain.rs`）：
   注册链 CHAIN 三席（Tmux→Link→Sys，间距归排布器私有），
   slot_top/chain_bottom/reserved_below/slot_rect 四笔账唯一源 +
   heights() 高度收集唯一源。收编三笔旧账：layout_vp 链底手抄、
   inset_extra_live+INSET_EXTRA 六处预留手抄、link/sys 卡
   「我接在谁下面」的 layout 链式签名（改 layout_in 排布器配给制）。
   消费点：parser_page/termview×3/android_app×9 全迁移；两卡几何
   考题改钉新 API（期望值不动），parser_chain_spec 6 考题 3 变异
   全咬 + 迁移等价钉（预留带 = 旧手抄账逐值相等）
6. 🚧 local 相三数据源（collect("/data") / local exec / 本地会话表）：
   ① ✅ local exec 腿接线（2026-09-20）——`local_pty::local_exec`
   与 `tmux_exec::exec` 同一契约面（短命本地 PTY `sh -c` 收全输出
   进 mpsc，10s 超时杀子收尸，环境 = 交互本地会同一份 L3 bash），
   壳 `endpoint_exec` LocalPty 臂接通 + `endpoint_exec_ok` 扩席；
   tmux 卡四调用点零改动同吃两相。真机探针先行（2026-09-20）：
   L3 overlay 装 tmux 3.7c，new/ls/kill 生命周期全绿。考题
   local_pty_spec +3（echo/stderr 合并且非零仍 Ok/超时兜底），
   变异 3 枚全咬（超时比较反向/EIO 臂反向/Ok-Err 对调）。
   ② ✅ attach 对称（2026-09-20）——本地相点行 = 本地 PTY 重孵带
   `tmux new-session -A` 命令，点已附着 = 裸 shell 重孵脱离
   （BAR-116 同规）。工序根：ConnConfig.command 本地侧语义对齐
   ws 侧「sh -c 命令行」（ShellPlan 添 args，旧义「shell 路径覆盖」
   无调用方且与 conn.rs 字段文档相悖）；壳 parser_attach 对象轴
   分流 server/local 两臂，respawn_remote_with 按名分工厂泛化
   respawn_named_with；附着账分两本（remote/local_attached），
   cur_attached() 唯一认领点，翻相随行刷页牌；本地死亡/手动
   重孵 = 裸 shell → 附着账勾销（远程不清：default_config 带
   attach 命令，不对称来自配置差）。考题 +1（命令行语义 spawn
   →输出→Exited），变异 argv 漏 args 咬住；
   ③ ✅ 环境卡本地相 collect("/data") 直读（2026-09-20）——
   compose 添对象相参数（本地相：词 = 注册表静态词「本地」，
   后端相不掺和，Kfmv4 后端也出本机体征）；字段映射抽
   fields_of 两相共用唯一源；svc_health 轮询器对象轴分流
   （本地相 = na_sys 直读零网络，服务器相 = 既有 HTTP 轮询），
   翻相清账（互不清带）+ Kfmv4 相节拍复位保鲜。考题 +1
   （本地相词/字段/占位三钉），变异分支反向咬住；
   ④ 服务/连接卡本地相——**2026-09-21 拍板 = B 案降级相**（宪法
      §三降级显形：连接/服务卡本质是远程通联可视化，本地相无通联
      对象 → 卡头「连接 · 本地直通」，字段全 —，右列只留
      后端=本地 PTY / 会话=n 两个真数据）+ 本地相隐藏 [重连] 钮。
      A 案（回环恒在线 + 本地会话表）挂账：PTY 槽状态 tmux 卡与
      终端本体已覆盖大半，有真需求再补。待做
7. ✅ 三区排布改版（2026-09-21 用户拍板，宪稿 §四 v2）：tmux 卡竖排
   常驻右下（Dock 钉视口底，RIGHT_COL_W = 16 格固定）+ 右上滚动区
   （连接·服务卡）+ 左滚动区（环境卡吃宽区）。parser_chain 三区
   契约：ChainSlot{id,region} 注册归属 + regions() 几何唯一源 +
   slot_rect 钳制唯一 + 双滚动账 Scrolls{left,right}；parser_page
   tmux 卡竖排一行一框（上限 6 框内部滚，BAR-119 保底两行）；
   link_card 两竖列改两段纵排全宽；壳手势 ParserDrag{Session,Left,
   Right} 三账本 + parser_geom 一包同源五消费点。考题全量重写：
   parser_chain 6 钉 4 变异全咬（钉顶/漏 eff/忘 max(0)/右列随屏）、
   parser_page 24 钉 2 变异全咬（忘全宽/保底退化）、link_card 3 钉、
   sys_card 12 钉（夹具改三区）
8. 解析页跟随翻相 + 池内容平移动画（复用既有视口平移机制）
9. redroid 判卷 → 用户肉眼终验

## 七、明确的非目标

- 不做插件热加载/动态下发（注册表编译期静态，cordis-na PluginEntry
  启动配置表已够）；
- 不做卡的用户自定义排序/显隐（排布器有序列表是代码常量，未来有
  需求再议）；
- 文件树页（左滑）不在本契约——它是路由位不是解析位，解冻时另议。

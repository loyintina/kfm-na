# KFM-NA 立项（Kaf Fee Meo Native）

> 2026-08-13 立。状态：尖刺 1 进行中。验收 1 包体 430KB ✅；雷 1 转软渲染排除 ✅
> （判词见第十节）；验收 3 连接部分 ✅——ws 连接层（2e0bf01：session 13 题 A 档 +
> live 钉 host 实测 + 手机端 opened/output/exited 三格实拍）。终端渲染切片已落地
> （termview 15 题 A/B 档 + 常驻会话驱动 live 钉 + APK 构建过），待手机实拍判卷。
> 待办：验收 2 冷启动计时、验收 3 剩余（tmux attach 进 kimi code 滚屏）、
> 验收 4 键盘/IME。
>
> 深夜进展：验收 4 英文输入 ✅ / 键盘避让 ✅（JNI insets，BAR-006）/
> 盲文 tofu 修（BAR-007）。**中文输入死结已破**（同日）：病根 = NativeActivity
> 无 Java InputConnection + winit native-activity 零 Ime 事件代码；解法 =
> Java 皮三件套（android/java/，SDL 同款）commitText → JNI → ime_queue →
> 终端，打包脱离 cargo-apk 改手工管线（scripts/package-apk.sh）。
> 首刀实拍踩 BAR-008（替换内容 View → 绑错 surface 全黑），改 1px IME
> 焦点占位叠加，待二次实拍判卷。

## 一、定位

kfmv4 的 native 手机客户端。本质诉求：**一套内置 AI 的同居操作系统**——
把 Obsidian（文件/笔记）和命令行（服务器）用手机 UI 模式统一起来，
以远程连接服务器为基础，实现手机功能的扩展。

核心三件套（用户在用的全部，其余皆是赘余）：
1. **光球对话**——内置 AI 的居所
2. **tmux 里的 kimi code**——远程操作服务器
3. **文件树**——仿 Obsidian 手机端交互（kfmv4 tree-model 语义直接移植）

终局愿景：NA 成长到与 kfmv4 同等高度时，接管现在的 kfm 和数据。

## 二、架构三层

```
┌─────────────────────────────────┐
│  L2 覆盖层 UI（手势壳）           │  左缘手势→文件树 · 光球→对话面板 · 工具卡
│  （边缘手势由壳接管，与终端内容不冲突）│
├─────────────────────────────────┤
│  L1 网格眼睛 + 按键注入的手        │  AI 的感知与操作（见第四节）
├─────────────────────────────────┤
│  L0 终端底座（全屏）              │  alacritty_terminal + wgpu，
│                                 │  跑 tmux 里的 kimi code
└─────────────────────────────────┘
```

设计判断（2026-08-13 用户拍板）：
- **终端即中央页面**。kfmv4 的中央 HUD 是「浏览器里没有底座硬造底座」，
  手机上底座天然是终端。HUD 九宫格/卡片堆不继承
- **边缘手势是壳的，内容手势是终端的**——手机端十几年的成熟分界（Termux 同款）
- **浏览器转发路线否决**：终端是高频字节流，走 DOM 差分只会更慢

## 三、服务端复用（一行不动）

| 能力 | 协议（kfmv4 现有） |
|------|------------------|
| 终端 | terminal-pty ws（PTY 字节流双向） |
| AI 对话 | /ai/chat 系接口 |
| 文件树 | tree 接口（root/expand/操作） |

web 客户端保留给桌面，两端并存，互不杀。

## 四、眼睛与手 v2（native 红利）

kfmv4 的眼睛费劲的根源：xterm.js 把终端渲染成 canvas 位图，AI 看不见终端里的字。
native 版终端网格（`alacritty_terminal` 的 Grid）就在内存里——第几行第几列
什么字符直接可读：

- **眼睛 v2 = 网格快照 + 覆盖层矩形**（布局自算，比 DOM 采样精确一个量级）
- **手 v2 = 往 PTY 注入按键序列 + 触发壳手势**——没有「按错位置」这个 bug 类别
- AI 读 kimi code 屏幕 → 回话 → 注入操作，全闭环

设计思想来源：kfmv4 `docs/active/眼睛与手.md`（只读引用，不复制）。

## 五、工具卡

AI 调用工具 = 一等 UI 对象：edit 弹文件卡播编辑动画、grep 弹结果卡，
人也能召唤同样的卡用 AI 的工具。详见 `docs/active/工具卡.md`
（四个待拍板项定了才准实现）。

## 六、技术栈与零件

| 层 | 选型 | 理由 |
|----|------|------|
| 语言/打包 | Rust → aarch64-linux-android，cargo-apk | 服务器交叉编译，APK 几 MB |
| 窗口/输入 | winit（android-activity） | 软键盘/IME/手势，现成的 |
| 渲染 | softbuffer（软渲染，2026-08-13 改判） | wgpu 双后端在本机随机原生暴毙（判词见十）；终端负载本是 CPU 场景，零驱动依赖 |
| 终端模拟 | alacritty_terminal（crate） | 转义序列/网格/滚屏，最硬的骨头现成的 |
| 网络 | tokio + tokio-tungstenite | ws 客户端 |
| 树控件 | egui_ltreeview 或自绘虚拟列表 | MIT；文件树不是性能瓶颈 |

**明确不借**：Termux 本体及其 terminal-emulator 库（GPLv3 传染，且 Java 栈不搭）。

## 七、尖刺 1 验收标准（五条全过才算成，不达到不扩功能）

1. APK < 10MB
2. 冷启动到终端画面 < 1 秒（手机实拍）
3. 连上服务器 terminal-pty ws，`tmux attach` 进 kimi code，滚屏不输 Termux
   ✅（2026-08-14 实拍：SSH 隧道回环直连；触摸滚动 = A 档手势状态机
   src/scroll.rs（点按/拖动分阈、像素→行余数挂账）+ alacritty display_offset）
4. 软键盘打字正常，中文输入法候选词正常
   ✅（2026-08-14 实拍：中文落字过；雷见 ledger BAR-008~015）
5. 协议解析与终端网格逻辑有服务器端无头测试（cargo test，不依赖手机）

已知的两颗雷（尖刺存在就是为了踩它们）：~~wgpu 的 Android Vulkan 兼容性~~
（2026-08-13 已踩爆：双后端随机原生暴毙，弃 GPU 转 softbuffer，判词见十）、
winit 中文 IME 的粗糙度（待踩）。踩完再决定后续切片做不做。

## 八、切片路线

1. **尖刺**：全屏终端 + ws + 键盘/IME（验收标准见上）。2026-08-14 增补
   **快捷键行**（Termux 同款两排七列：Esc/Tab/Ctrl/Alt/Shift/↑/PgUp，
   End/Enter/PgDn/←/↓/→，↑↓ 同列、方向十字锚右）。**Rust 自绘**（keybar.rs
   布局/命中/粘滞状态机 + termview.render_keybar——BAR-017：Java View 被
   原生 busy 重绘盖掉，覆盖层 UI 的统一模式就是 Rust 自绘）；修饰键一次性
   粘滞，组合映射在 keymap.rs（A 档考题盯着 Ctrl+字母这条 kfmv4 没联动的
   病根）；方向键/End 按对端光标模式（?1h）分岔 CSI/SS3；行高是 Rust 常量，
   终端网格永远让位
2. 左缘手势 → 文件树覆盖层（读 tree 接口，点文件 = 终端里打开）
3. 光球 → 对话面板覆盖层（打 /ai/chat）
4. 眼睛/手 v2 + 工具卡（网格眼睛、按键注入、工具即卡）

## 九、纪律

出生即带三门（chain 全绿 / fix 带钉 / 文档耦合），见 AGENTS.md。

## 十、飞鸽传书（实拍回传通道，2026-08-13）

手机走蜂窝/WiFi NAT，服务器 adb 反连不回去，logcat 拿不到——C 档实拍
判卷缺证据链。通道设计：APK 既然能从服务器下载，就能向服务器回传。

- APK 侧 `src/report.rs`：panic 钩子 + boot 里程碑（android_main 进入 /
  resumed / wgpu 适配器 / 启动完成）HTTP POST 到
  `http://127.0.0.1:8021/kfmv4/api/na-report`（2026-08-14 拓扑：SSH 隧道
  对接两端 8021，无 nginx 无公网；best-effort
  吞错——上报通道自己绝不能炸成二次事故）
- kfmv4 侧 `/na-report` 端点落盘 `/root/kfm-na/field-reports.log`
  （不挂 verifyLocalOrigin，手机本就是外部来源）
- 缘起：首版紫屏 APK 实拍闪退（2026-08-13），尸检打包无误（入口符号/
  清单/权限齐全），定为运行时 panic，建此通道抓现场
- 战果（同日）：回传里程碑把死亡行锁到 wgpu configure 段；结合开源调研
  定为 cargo-apk 默认 configChanges 不全 → OriginOS 启动注入配置变化 →
  Activity relaunch 杀使用中的 surface（android-activity#196，MIUI 同款）。
  修复 = `[package.metadata.android.application.activity] config_changes`
  补全 na-mainloop 全集（manifest 实测 0x40001ff0）
- **终审判词（同日晚）**：configChanges 修完仍死。升级仪器（每秒心跳 +
  死亡出口同步直报 + 裸窗对照组）后真相浮出——裸 winit 窗稳定（15s+），
  wgpu 一加进来就在 adapter/surface/configure 随机原生暴毙，Vulkan/GLES
  双后端同病、零 Rust panic，且历次「闪退」混着等待期手动退出被 exit(0)
  灭迹的假死。定性：本机 GPU 驱动栈与 wgpu 抽象层犯冲，非代码逻辑病。
  **用户拍板转 softbuffer 软渲染（a44f936），紫屏实拍一次通过，包体
  1.88MB→430KB**。GPU 路线留档后查（git 历史）
- 后路备忘：若 NativeActivity 还有第二个坑，生态共识是迁 GameActivity
  （Bevy 已默认，cargo-apk 被官方标 deprecated）——届时走 xbuild/gradle
开发方法论：**考题先行分三档**（A 纯逻辑考题先行+变异抽检 / B 胶水答案先行
考题钉住 / C 感官实拍判卷），细则见 AGENTS.md 方法论章——本文件的
尖刺五条验收标准即 C 档考题的首个实例。
第二档基建（信箱/语义巡逻/计数同步）等复杂度长出来再引——引什么看
kfmv4 对应机制的实证收益，不一股脑搬。

## 十一、插件基座（base）（2026-08-15 落地）

规格书 `kfmv4/experiments/dsh-na/na/plugin-architecture-spec.md` v1.1 的最小核心实现：

- **模块**:`crates/cordis-na/src/`(2026-08-17 阶段 1 搬家：原 `src/base/`
  升格为独立 crate `cordis-na`=通用运行时，kfm-na 经 `pub use cordis_na as
  base` re-export 保持消费侧路径不变；同刀切除 G1 `Term` 占位——内核不再
  有任何 harness 服务字段）——`ctx.rs`（公告栏：内核事件总线类型化字段 +
  插件服务 registry,错误两分 / 单一来源 / realm 隔离）、`effect.rs`（可逆
  效果栈，`Disposer = Box<dyn FnOnce() + Send>`,LIFO + take-once）、
  `fiber.rs`（生命周期五态机 + 依赖引擎：epoch 签名 / 卸载三相 / 失败钉死 /
  依赖环注册期报错）、`event.rs`（事件三派发：Emit / Serial 顺序短路 /
  Waterfall;Parallel 缓建）
- **契约测试**:`crates/cordis-na/tests/base_spec.rs`——A 档 17 道考题先行
  （红→绿，1/3/5/6 做过变异抽检），互操作组合矩阵层按评审裁决另立，不在
  该文件。搬家验收（评审裁决 4 口径）:全量可实跑基线 126 通过/2 ignored
  搬家前后全绿 + 终端插件第一个外部消费者无缝（term_emu 5 + termview 33
  题绿，实拍见落地通报）
- **阶段 2(2026-08-18 落地，设计页 `na/cordis-na-liveness-gate.md`，评审
  三问全批）**:G2 活性闸（INACTIVE_ACCESS)——fiber 卸载/失败后其 Ctx
  一切操作 = panic,Owner 分形判活（Fiber 带激活代数，reload 旧句柄永死；
  锁内只读判活、放锁后 panic，持锁 panic 毒化 Mutex 系考题实证）+ Events
  六法同闸 + disposer 不受闸；G5 归层（预算检查默认关，harness 显式开启
  50ms);G3/G4 缓建桩入考题。考题 17→28 道（136 通过/4 ignored 全绿）
- 设计定稿口径见跨线信箱三封信（kfmv4 `docs/ledger/agent-inbox/`：
  基座设计送审 / 基座设计回信·评审裁决六条 / 基座落地通报·整合确认）：
  v1 全同步，async 契约预留 v1+；瞬时返回契约（apply/unload
  超预算记 SlowApply 报警，不记失败）
- 后续切片（规格书 §6 阶段 2/3）：conn/session/protocol 抽连接 provider
  插件，终端/输入/覆盖层逐个注册化

## 十二、连接 provider 插件（2026-08-16 落地）

规格书阶段 2「边界手术」第一刀：常驻 ws 终端连接从 `android_app.rs` 硬编码
调用降级为插件服务。设计页 `kfmv4/experiments/dsh-na/na/connection-provider.md`
（v0.1，评审五条裁决全过）。

- **服务键**：`dyn conn::TermFactory`（注册表式，独占绑定 v1）——
  `default_config()`（插件配置表解析出的默认连接参数）+ `spawn(&ConnConfig)
  -> TermHandle`。`TermHandle { outbound, events }` 是裸 mpsc 通道对，
  事件桥收进工厂内部（原 android_app 手工建桥工序消除）
- **瞬时返回契约**：apply 只注册工厂、不真连接；真连接在 spawn 时由工厂
  内部开线程（`conn.rs` 线程模型不变）
- **状态存活**：插件只持可蒸发的工厂闭包；已建立的连接归调用方持有，
  卸载/重载插件不断已连会话（考题 7/8 钉死）
- **transport 注入缝**：`conn::Spawner`（生产 = `ws_spawner()`，考题 =
  假 transport 线程回显，零网络依赖）
- **改动面**：`conn.rs` 加 `ConnConfig`/`TermHandle`/`TermFactory`/`Spawner`/
  `WsTermFactory`（`spawn_terminal_session` 原签名保留，内部委托 String 版）；
  新增 `src/plugins/conn_provider_ws.rs`；`android_app.rs` 启动改走
  `Base` + 插件取工厂；`session.rs`/`protocol.rs`/现有考题一题未动
- **契约考题**：`tests/conn_provider_spec.rs` 6 道（注册成功 / 默认配置锚 /
  事件桥收敛 / 卸载回滚+句柄存活 / reload 换工厂旧句柄不动 / 注册冲突隔离）
- 冒烟路径 `echo_roundtrip`/`spawn_smoke` 保持原样不进插件（评审裁决 5）

## 十三、终端模拟器插件（2026-08-16 落地）

边界手术第二刀：termview（alacritty 芯）从 android_app 直接构造降级为插件
服务。设计页 `kfmv4/experiments/dsh-na/na/terminal-emulator.md`（评审五条
裁决全过）。与连接 provider 同构——**工厂是服务、实例归调用方**：

- **服务键**：`dyn termview::TermEmuFactory`（注册表式，独占绑定 v1）——
  `build() -> Result<(Box<dyn TermEmu>, 主字体名, CJK字体名), String>`；
  build 失败（字体全灭）走 Err 返回值，不算插件失败
- **形态必然性**（评审裁决 1）：registry 存 `Arc` 共享不可变，终端是每帧
  `&mut` 渲染的独占可变对象——`TermEmuFactory: Send+Sync`（共享）vs
  `TermEmu: Send`（独占），类型约束编码状态存活分层
- **方法面零增删**：trait = android_app 现调 11 方法原样抽取，方法体一行
  未动；自由函数（grid_dims/paintable/颜色表）不进 trait；trait 定义处
  钉演化纪律注释「新增方法须有调用方先例」
- **注入缝**：`TermAlacritty::with_candidates`（考题喂 DejaVu 夹具，
  生产 = FONT_CANDIDATES）
- **改动面**：termview.rs 加 trait+impl（纯抽取）、新增
  src/plugins/term_alacritty.rs、android_app.rs（term 字段换
  Box<dyn TermEmu>，Base 装配提前、两个插件同一基座）、新考题
  tests/term_emu_spec.rs 5 道；termview 33 题 + scroll 7 题不动
- v1 零配置（字体/字号/颜色常量即行为零变化锚；未来加字段须在设计页
  修订记录写「默认值=当前常量」，防双事实源漂移——评审裁决 4 提醒）

## 十四、输入/IME 域插件（2026-08-16 落地）

边界手术第三刀，第一批收官。设计页
`kfmv4/experiments/dsh-na/na/input-ime.md`（v0.1，方案 A 批准）。
**形态判别**：本域无独占可变对象，两服务均 Sync 内部可变 → 共享实例直挂
（规格书 v1.2 §4.2 形态判别准则的第三个实例）。

- **服务键**：`input.modifiers`（`Arc<keybar::ModifierState>` 具体类型直挂）
  + `ime.insets`（`Arc<dyn ImeInsets>`，构造注入：生产 JniInsets 持
  AndroidApp 句柄，考题喂假实现）
- **方案 A 核心动作**：`keybar.rs` 进程静态 `MODS` 删除，状态归属服务实例；
  `render_keybar` 改吃 `mods` 参数（TermEmu trait 调用方驱动变更，评审裁决
  1 批准）；两道旧考题迁移（断言一字未改：keybar_spec 粘滞题 /
  termview_spec 快捷键行渲染冒烟题）
- **JNI 桥端点**：ime_bridge 的 commitText 在 JNI 回调线程拿不到 ctx——
  `keybar::install_bridge_mods` 装入服务实例句柄（与 ime_queue::global
  同性质：静态只是句柄，单一来源仍是服务）
- **不进插件**：ime_queue（桥端点，B 档胶水同 report.rs）、keymap/keybar
  布局命中（纯函数）
- **改动面**：keybar.rs（静态删、ModifierState + 桥端点）、insets.rs
  （ImeInsets trait 跨平台 + JniInsets 薄壳 cfg android，模块出 cfg）、
  ime_bridge.rs（桥端点取修饰键）、termview.rs（render_keybar +mods）、
  android_app.rs（三处服务化）、新增 plugins/input_ime.rs +
  tests/input_ime_spec.rs 5 道
- 零总线事件（连续第三刀）：IME 事件驱动本机是死路（BAR-006），总线第一个
  真实用户留给第二批插件

## 十五、自观测补全（2026-08-25 立项，方向纲领见 kfmv4 `library/notes/自我进化理论地图-自观测到控制论-2026-08-24.md`）

> 定位：na 已有**调试闸门三件套**（`shot-req`/`text-req`/`keys-in`，`调试闸门.md`）＝
> 自观测的"看症状"（A0 已完）——从外部看画面/读文本/控输入。
> 本补全把它往内推：从"看症状"到"看机制"（运行时状态+决策），接控制论"能观/能控/反馈"。
> 顺序：A（状态导出+事件日志）→ B（插桩+回放）→ C（实验+变异+判据外部化）→ D（决策轨迹语义化，长线）。

| 小步 | 内容 | 对应自观层 | 验收 |
|---|---|---|---|
| A1 | 运行时状态导出：渲染/事件循环/会话状态 → 结构化可查询 | 能观地基 | A 档：状态可查询钉 |
| A2 | 标准化事件日志：`log` crate → 带版本的结构化事件流（替代 logcat ad-hoc，语义化/回放底座） | 底座 | A 档：事件流带版本 |
| B1 | 按需自插桩：渲染/事件处理点现场插探针，重跑读精确值（替换"分段计时探针"为**按需**） | 插桩 | A 档：插桩读精确值 |
| B2 | 确定性回放：现有 `pump_take_replay`/replay 缓存扩成**可慢放复现**（渲染帧/事件回放） | 回放 | B 档：轨迹回放复现 bug |
| C1 | 假设-干预-再观察：闸门 `keys-in` 扩成"注入→观察→归因"循环（因果定位） | 实验 | A+B：因果定位 |
| C2 | 变异抽检：现有测试（termview_spec 等）加变异（改坏判据看抓不抓） | 判据可信 | A：变异抓判据 |
| C3 | 判据外部化：判据在 agent 够不着处（复用 kfmv4 check 闸，其判据独立于 na） | 判据外置 | 评审/check 闸确认 |
| D1 | 决策轨迹语义化 + 一致性检查（最小——推理步功能分类→标准路径→偏离检测；长线研究） | 前沿 | A：功能分类+偏离最小 |

注：A0（调试闸门三件套）已落地（2026-08-24），本补全从 A1 起；C3 判据外部化在 kfmv4 已基本就位，na 侧主要为确认对接。

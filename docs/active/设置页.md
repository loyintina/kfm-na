# 设置页（配置卡）设计 — docs/active/设置页.md

> living 文档。设置页（齿轮召唤的右侧滑入页）的内容与交互设计唯一来源。
> 视觉/导航规范不在此重复——主题宪法 theme.md §五「双池的目录语义」
> （三层目录宪法，2026-09-13 拍板）是上位条款。

## 一、页面骨架（三层目录在配置卡的落地）

- **标签栏（大类）**：系统管理 |（未来追加 API/角色/工具/session/脚本…）
- **下池（子目录选择）**：当前大类的子目录列表，聚焦行即选中
- **上池（二级选项）**：聚焦子目录的选项集 + 顶部联动下拉框
- **三级**：点击需要展开的二级选项 → 全屏页承载三级框

## 二、第一大类「系统管理」——服务器配置（2026-09-13 用户定方向）

### 2.1 背景与现状（诚实清单）

- 远程会话 = ws 连 `127.0.0.1:8021`（conn.rs ConnConfig 默认），
  隧道由 **Termux autossh** 持有（-L 8021 / -R 8022 / -R 8024 等）
- **2026-09-13 NA 自持通道已实证**（state.md 当日条）：
  沙箱 OpenSSH 客户端 + `$PREFIX/bin/na-tunnel.sh` 看守，
  **9021 正连**（NA→kfmv4）、**9022 反连+调试**（服务器直达 NA sshd）
  双向全通。Termux 依赖进入可退役状态
- **2026-09-19 隧道收归 na 原生**（用户拍板，取代 russh 提案）：
  `src/tunnel.rs` 内置 ssh 正连看门狗——启动即 spawn L3 prefix 的
  `ssh -N -L 9021:127.0.0.1:8021`（servers.json 的 ssh/tunnel 字段
  终于有消费者），单线程 1s 滴答探活 + 退避重拉（0/2/5/10/30s 封顶），
  状态出 `TunnelSnap`（连接/服务插件卡与断线状态卡的唯一数据源）。
  **让位/接管语义**：本地口被占且能连通（Termux 隧道还在）→
  ExternalUp 不抢，30s 复查；外部一断下一拍自持接管。
  **-R 9022 反连 2026-09-21 起 na 自持**（用户拍板「9022 我们
  自己的推送路径」）：v1 曾划给 Termux 维护——Termux 休眠冻结
  = 推送全瘫一整天实录；并进自持隧道后看门狗双腿（BAR-117）
  一并维护，na 活着反连就在。`-R 127.0.0.1:{remote_port}:
  127.0.0.1:8024` 显式绑回环：不新增公网暴露面（2026-09-01
  红线）；远端冲突时 ExitOnForwardFailure 整路死 = 看门狗
  知情重拉，不静默裸奔。
  na-tunnel.sh 脚本退役为手动兜底。
  **2026-09-20 可视化面落地（连接/服务卡 v1，用户立项）**：解析页
  第二张二级卡（纵排在 tmux 卡下），`src/ui/conn_card.rs`——卡头
  「连接 · 状态词」（自持在线/外部借用/连接中/退避 ×N/未启动，
  `tunnel::state_word` 唯一文案源）+ 四字段行（目标/本地口/重拉/
  错误）+ 分隔线 + [重连] 钮（`tunnel::request_reconnect`：杀娃、
  退避清零、下一拍立即重拉）。数据 = tunnel.rs 全局快照直读
  （supervisor 写、卡读，免穿 App plumbing）；TunnelSnap 加 epoch
  维进解析槽烘焙 sig——隧道状态翻转自动重烘，漏维 = 鬼影。几何：
  tmux 卡按 `conn_card::INSET_EXTRA`（= 卡间距 + 卡实高，同源钉死）
  预留底部带，涂装/命中/滚动五处调用点同加——眼手同尺。
- 会话切换：Ctrl-]（`\x1d`）在 android_app.rs UI 层拦截，双槽互换
  （本地 PTY ⇄ 远程 ws），写死

### 2.2 字段（用户 2026-09-13 列示）

每服务器条目：名称、服务器 IP、端口、用户、**密钥所在地址**、
**密码（空 = 密钥登录）**、**绑定切换快捷键**（未来多设备时每服务器
独立绑定，直达切换）。

全局项：**默认终端**（本地 / 某服务器）、**切换快捷键修改**。

### 2.3 目录映射（三层宪法套用；2026-09-13 二版修订，用户拍板）

- 下池（子目录）= 系统管理大类的子目录列表——**目前仅一行
  「系统管理」**（服务器配置；用户拍板：服务器列表**不进下池**）
- 上池（二级选项）= 服务器配置表单（kfmv4 API 卡模式）：
  - 第 1 行「默认服务器」**下拉框**——选项 = 本地终端 + 服务器池；
    换选 = 写 terminal.json `defaultSession` 落盘 + **活跃会话立即
    联动翻到对应槽**（2026-09-21 用户拍板：双槽保活 + replay 补屏，
    切回还是原样；纯核 = `DefaultSession::session_name`），
    下方服务器字段联动刷新
  - 第 2 行「服务器切换」= 全局切换快捷键（v1 只读展示 Ctrl+]，
    编辑走三级页 = v1b，宪法 §六 组合键录入控件）
  - 之下 = 下拉选中服务器的字段三级框（名称/IP/端口/用户/密钥
    地址/密码/wsUrl，v1 只读；选本地终端时无字段，上池占位）
- 三级全屏页 = 需要展开的字段编辑（候选：快捷键录入——修饰键下拉
  + 键名手填，宪法 §六 组合键录入控件条款）

### 2.4 切换行为决策（2026-09-13 用户拍板）

- **会话切换轮换环废弃**：AI 面板（光球召唤）**不参与**任何会话
  切换轮换；多服务器也不做轮转
- 改为：**全局切换键**（默认 Ctrl-]，本地 ⇄ 默认服务器）+
  **每服务器独立绑定快捷键**（设置里新增服务器时绑定，直达）
- 待拍板：多设备并存时全局切换键在「本地⇄默认服」还是
  「本地⇄最后活跃服」之间切——实现前用户定
- 切换键录入 = 手填，不做捕获（宪法 §六 组合键录入控件）
- **切换即刷新（BAR-124，2026-09-21 用户拍板）**：切换时对端强制
  全屏重画——resize 抖动（先 rows-1、120ms 后归位，两次净变化 →
  SIGWINCH）。同尺寸 Resize 补发是白发（净变化为零内核不发信号）；
  静默待机 replay 为空时无此抖动 = 整屏对端残影。已知边界：raw
  shell 无重画协议只能重印提示行；每会话网格快照是根治欠账（挂账）
- **切换换模式账 + 清屏（BAR-125，2026-09-21 用户拍板）**：切出存
  终端模式快照（鼠标上报/alt-screen/应用光标/bracketed paste），
  切入复位受管模式 + 清屏（「切换后直接自动清屏」）+ 恢复切入方
  快照。不换账 = tmux 鼠标模式泄漏到本地 shell，滑手势变 SGR 滚轮
  乱码。每会话网格快照（内容级）仍是挂账根治，本件只治模式层

### 2.5 数据模型（providers.json 模式复刻）

`servers.json`（app 私有目录，JSON 数组，宽容缺省）：

```json
[{
  "id": "main", "name": "阿里云主服",
  "ssh": { "host": "8.145.46.182", "port": 22, "user": "root",
           "keyPath": "…/files/usr/etc/ssh/id_ed25519", "password": "" },
  "tunnel": { "localPort": 9021, "remotePort": 9022 },
  "wsUrl": "ws://127.0.0.1:9021/ws", "command": null,
  "hotkey": { "mod": "ctrl", "key": "]" }
}]
```

`terminal.json`（全局）：`defaultSession`、`switchHotkey{mod,key}`。

- 密码空 = 密钥登录；文件在 app 沙盒私有目录（v1 可接受，
  未来 Android Keystore 加密另立项）
- **通联端口 = 工程参数，JSON 可设、UI 不摆**（2026-09-13 用户
  问、agent 建议、同日报价确认方向）：两端口本质不同——
  `localPort`（正连出口）绑在**手机本地**，同设备同时只活一条
  隧道时换服务器照用不撞，多服务器同连才需错位；`remotePort`
  （反连入口）绑在**服务器本地**，标识的是「这台设备」，撞口
  只发生在「多设备连同一服务器」或「同设备对同服务器双连」。
  分配规则：**每设备占 10 口段**——本机 9020 段（9021/9022），
  第二台设备 9030 段，以此类推；撞段改 JSON 一个数即解围。
  UI 不出端口项（防隧道活着时被改出事故）

### 2.6 分期

- **v1（本次）**：配置文件 + 配置页 UI（三层目录全链）+ 热重载
  （conn.rs 已有「配置变更=自我重载换新工厂」的缝）；ssh 字段
  存储+展示，隧道仍由 na-tunnel.sh/Termux 持有
- **v2**：app 原生隧道管家（替代裸 nohup 看守，vivo 杀 app 后
  可自愈），ssh 字段全部生效，Termux 依赖正式退役

### 2.7 实现台账

- **v1a-1 解析核（2026-09-13 闭环）**：`src/settings.rs`（providers.json
  同规：宽容缺省、纯逻辑零 IO）；hotkey→字节**直调 keymap::map_text**
  同源不另造。考题 tests/settings_spec.rs 13 题 + 变异三咬全中
  （port 默认改 2222 / ctrl 臂删 / defaultSession 恒 Local）。
- **v1a-2 壳接线（2026-09-13 闭环）**：冷启动读
  `settings/servers.json`+`settings/terminal.json`（缺=现状默认不响、
  坏=上报回退，配置文件不许炸终端）；ConnConfig 来自默认会话指向的
  服务器（wsUrl 空按 tunnel.localPort 拼回环），无条目=8021 现状锚；
  切换键拦截从写死 `\x1d` 改吃 switchHotkey 字节（空=不拦截），
  切换横幅展示串跟配置走；defaultSession=Server(id 解析成功) →
  远程装活跃槽、本地待机。**待实机验证**（配置文件待推送 + 热更）。
- **v1a-3 三层目录 UI（2026-09-13 代码闭环，实机判卷待办）**：
  `src/ui/cfg_page.rs` 数据核（下池行表=[全局+服务器+新增]、上池
  字段行+顶部联动下拉框、focus/dropdown 状态机、几何自由函数
  row_rect/trigger_rect/field_rect/dropdown_panel_rect；考题
  tests/cfg_page_spec.rs 12 题+变异两咬：不收 panel/向上弹全中）；
  termview 涂装 `paint_cfg_pool_content_impl`（池行 2 格、聚焦 6%
  白底、触发器+三角、下拉 panel 96% 底 245 混色，blend 走
  frame.blend_px）；触摸路由（Started 池区仲裁/下池点按聚焦+
  rebuild/触发器 toggle/panel 项点选同步收合）；**配置槽 sig
  增维**——cfg_epoch 代际进 ConfigSig（聚焦/下拉/行表重建都
  bump，漏维=旧行表新聚焦鬼影）；13 维超元组 trait 上限（PartialEq
  只到 12 元）踩手机 1.97 E0277，ConfigSig 元组改具名结构（字段即
  注释）。**踩坑**：transition 帧 off≠0 时行左缘出屏整行不画
  （softbuffer 兜底取舍，GLES 烘焙恒 off=0 不受影响）；下拉命中
  max_h 不钳、涂装屏高钳——行数超屏会眼手两张皮（v1a 已知让步，
  行数少无实际影响）。**判卷结论**：实机拍出下池行表/上池字段/
  联动全通，但用户判「字体太小/上下池分工错/无三级框」——
  推翻，v1a-4 重对齐。
- **v1a-4 池卡模式重对齐（2026-09-13 代码闭环，实机判卷待办）**：
  用户投 kfmv4 池卡实证 4 图（会话管理/tool/脚本/API）拍板重设计
  ——修宪 theme.md §五 二版（**下拉服务上池内容自身选项集、不与
  下池联动**推翻初版双向联动；池行 3 格圆角框、选中 accent 渐变
  描边弃 6% 白底；上池 = 标签列+值框的字段框行）+ §2.3 二版
  （下池仅一行「系统管理」；上池 = 默认服务器下拉行 + 服务器切换
  行 + 选中服务器字段框行）。兑现：cfg_page.rs 重写（UpperRow/
  options/option_sel 模型，考题 13 题+变异两咬：不收 panel/向上弹
  全中）；termview 涂装重写（round_d SDF 圆角框行、标题 faux-bold
  双画、accent 页环同尺采样描边、下拉 panel 选中描边）；壳
  rebuild_cfg_rows 重写 + **apply_default_server_pick——下拉换选
  = terminal.json defaultSession 写盘**（settings::terminal_to_json
  与 parse 同口径，写坏上报不炸）+ 下池命中改 lower_row_at_y。
  **实机判卷全通**：下拉开合/点选/换选写盘（terminal.json 落盘
  `defaultSession:main` 经闸门读回核对）/选中服务器字段框联动/
  双池高度生长；标签列 6 格截断 5 字标签 → 8 格（180593f）。
  **一箭双雕验证**：换选 main 后冷启 → active=remote 走
  `ws://127.0.0.1:9021/ws`（NA 自持隧道）ws 握手完成、
  remote_dead=false。
- **v1a-5 四版大观感重标定（2026-09-13，用户拍板）**：用户持 kfmv4
  实证 4 图量化对比判 v1a-4「框无左粗线/字小行矮/间隔挤」——修宪
  theme.md §五 四版（**左粗三边细通用化**：所有三级框恒有 10px
  accent 渐变竖胶囊左条 + 三边细描边；字段框行 4 格 ×2、下池行
  4.5 格 ×1.5、行隙 1.5 格、内边距 2 格、标签列 12 格、字号
  36/30、标签栏字 36、下拉三角 ×1.5）+ §一 半格网（0.5 格精度统一
  放宽，位置+尺寸都取半格整数倍——用户拍板）。
  兑现：cfg_page 常量重标 + **上池像素滚动**（§五「超出部分上池
  内滚动」v1a 欠账被迫兑现——行高 ×2 后 10 字段行恒溢出；
  scroll_upper_by clamp [0, 内容高−池高] 到位不空涨代际，几何
  自由函数全带 scroll 维）；termview 涂装 row_frame 左粗条
  （竖胶囊吃框剪影内像素）+ 池内缘裁剪带（框/文字/三角同一带
  断墨）+ draw_text_left_ex/draw_items_left_inset 全参版（老
  调用方行为不变）；壳层起手/抬手触发器命中带 scroll 维 +
  panel_touch 移动分支上池纵向滚动（panel_drag 横向锁让路，
  一滑一义仲裁不变）。考题 16 题（+滚动 clamp/装得下恒 0/触发器
  下拉随滚 3 题）。
- **v1a-6 六修：最小容量律+组件+值框收紧+字档反转（2026-09-13，
  用户拍板）**：修宪 theme.md——§三 新立**最小容量律**（所有级别
  装修框净高 ≥3 格；内容距框缘上下 ≥0.5 格、左右 ≥1 格；功能光标
  不是容器不适用）+ §六 新立**组件**概念（无边框文字容器，可编辑
  或仅展示，字段框标签列即组件）+ 上池值框收紧（上下线各往中心缩
  半格 = 3 格高居中于 4 格行，**左竖条取消**——左粗条收窄为可选
  列表行专属：下池行/下拉选项行/未来文件树行）+ **字档反转**
  （标签列 0.85 亮 = 行的标题，框内值 0.5 灰；用户判「框前灰框内
  亮逻辑反了」）。兑现：cfg_page 新增 FIELD_BOX_H=3 格、
  value_box_rect 居中、panel 从值框下缘起弹；termview row_frame
  增 bar 开关（false = 四边 8% 白细线无左条），三调用点下池行
  true/值框 false/下拉项 true，标签/值字档对调。考题 18 题
  （+值框 3 格居中钉），变异一咬（改坏居中偏移即红）。
- **v1a-7 七修：字档全反转+上池行隙减半格+三级框角部渐细
  （2026-09-13，用户拍板）**：三问齐发——①六修只反转了颜色没
  反转字号（「哪有标题小内容大的」）→ 标签列 36px title 档 /
  值 30px meta 档（字大且亮 / 字小且灰）；②上池行距 1.5 格减半格
  → FIELD_ROW_GAP = 1 格新立（下池 1.5 格不变，ROW_GAP 归下池
  专用）；③**三级框角部渐细**——用户判「下池三级框的角不是其他
  框那样渐细的，是一根细条」：病灶 = row_frame 是
  paint_cfg_pool_content_impl 的局部手抄（§六 样式唯一来源的
  漏网之鱼），左粗条 = 独立竖胶囊贴在均匀细环里，角部两张皮。
  修法 = 提升为文件级共享组件 paint_row_frame + 页环非对称内芯
  同配方（内芯左让 10/其余让 3、半径 R−3，角心偏移使厚度在角部
  弧线上渐细，颜色同步渐混入 8% 白；复用文件级 rr_sdf/
  ring_gradient_rgb/lerp_rgb）。修宪 theme.md §五 三条 + §六
  共享涂装清点 + 标定表两行。考题 18 题（行隙断言改
  FIELD_ROW_GAP），变异一咬（上池行隙改回 ROW_GAP 即红）。
- **v1a-8 八修：标签栏换案填色标签页块（2026-09-13，用户拍板）**：
  用户判开口框方案「还是丑」，投实证图（信箱 IMG_20260913_172208，
  终端标签条样式）换案——**标签块**（paint_tab_chip 文件级共享组件
  新立）：无边框色块、上两角圆角 R=1 格、下缘直边；选中 = accent
  渐变满填 α255 + 文字深色反差，未选中 = 6% 白薄填 + 0.5 白。
  **底线组件**：标签行下缘紧挨 1px 渐变细线、池区同宽、色向内卡
  反转 c2→c1，空态也画。选中块 x 沿用 fx_spring 弹簧滑块。**退役**：
  开口框/线长随机机制——cursor.rs 几何 + paint_open_cursor/
  paint_cursor_arc 涂装封存（#[allow(dead_code)] 标记），待文件树
  光标复用；TabBar 剥 rng/geom、快照减两维、ConfigSig 同步减维。
  考题：spec_cfg标签栏_涂装钉 全量重写（块形态/角外零墨/直边/
  6% 白/底线精确采样/不越缘/空态/换 accent 跟变 12 断言），变异
  一咬（下缘圆角化即红）；cursor_spec TabBar 集成钉退役留档。
- **v1b-1 九修：组件池页+跳框（2026-09-13，用户拍板「可以，就
  这样来吧」）**：配置卡第二标签「组件池」= 组件库实机花名册——
  **唯一信息源 = src/ui/comp_registry.rs 常量表**（CompEntry：
  名/大类/状态/实现坐标 symbol+file/规范/考题/说明；考题棘轮 =
  symbol 字符串必须真实出现在 file 里，表与代码漂移即红）。
  目录映射：大类（装修框/组件/功能光标/控件/动效引擎 5 类）=
  下池行（meta=计数），类内组件 = 上池行（value=现役/封存/计划），
  **点上池行 = 开跳框**。跳框（§六 新立条款，kfmv4 三截图实证）：
  压暗层 α150 吃下层全部触摸 + 居中卡（paint_rect_ring 池框同尺、
  内卡反转 c2→c1）+ 标题 36px faux-bold 居中 + 1px 渐变分隔线 +
  字段区**题注 30px 灰在上/内容 36px 亮在下**（与上池字段行
  相反是有意的：跳框里题注是脚注）+ 全宽「关闭」钮（row_frame
  bar=false）。几何/折行单源 = src/ui/modal.rs（wrap_text 贪心
  格宽尺/card_rect 高随内容封顶屏高−8 格/close_btn_rect/hit）。
  壳：tab 维入 cfg_page（set_tab 切页清零聚焦/滚动/下拉/跳框）、
  模态手势槽（Started 全归跳框，抬手命中判定）、组件池 tab 的
  上池区建池区手势槽+触发器误判闸（tab1 首行不是下拉行）。
  考题：comp_registry_spec 7（symbol 实存棘轮）+ modal_spec 10 +
  cfg_page_spec +6（tab 切换清零/跳框开合幂等/上池命中带 scroll
  维/快照带新维），变异两咬（折行判据 >= 化即红/set_tab 漏收
  跳框即红）。
- **v1b-2 十修：跳框预览画板+池区手势让回（2026-09-13，用户拍板
  「重点是这个内容，而不是档案」）**：跳框分隔线下、字段区上插
  **预览画板**（6 格高展台，三级框工艺 bar=false）——按
  comp_registry 条目新增的 preview 枚举维（16 变体）做实涂装
  微缩**实时渲染**：画板是内容不是档案，与字段区的关系 = 展品
  与题签。光球/齿轮/开口框等复用本件涂装（gear 抽 paint_at
  共享核，宪法 §六 禁手抄；paint_open_cursor/paint_cursor_arc
  封存复活）；动效引擎类画函数曲线（弹簧响应/缓动两族/手势
  轨迹/视口平推示意）。几何单源：modal.rs 加 MODAL_PREVIEW_H/
  preview_rect/fields_top，card_rect 卡高公式含画板带（分隔线
  带改 0.5格+1px+0.5格）。考题 modal_spec +2（画板在分隔线与
  字段区之间/卡高含画板带）+ gear_spec +1（paint≡paint_at 同一
  掩码钉，覆盖矩阵棘轮咬住 gear.rs 不回退），变异四咬（y 公式
  抽 1px 即红/画板 5 格即红/卡高抽画板带即红/paint 私算心位
  偏 1px 即红）。
  **bug 根修（用户报「下池某些区域右滑回不到终端页」）**：池区
  起手建 cfg_pool_touch 槽后，拖过 slop 只记账不交出，横向锁定/
  快滑裁决永远轮不到 → 拖过 slop 时**让回面板页全家**：
  cfg_pool_touch.take() → 补建 panel_touch（dragged=true）+
  panel_drag::new(起点) 并 feed 当前点；Ended 通路白拿（锁定走
  finish_panel_drag，未锁定走 decide_swipe）。B 档壳接线无考题，
  实机回归判卷。
- **v1c 十一修：三级框渐变色归位+标签随机双色体系（2026-09-13，
  用户拍板，修宪 theme.md §四/§五/§七）**：①三级框整框进 135°
  双色渐变体系（与页环同向 c1→c2，§三 逐层反转：页环正→池框反
  →三级还原）——未选中细边不再是 8% 白，改渐变薄态 α140（新标
  定，ROW_FRAME_THIN_A）；**角部渐细只渐形状不渐色**（推翻七修
  混白——实机判「圆角发白」），左角弧线 alpha 沿弧 255→140 过渡。
  三级框两形态钉死：全包框（下池行/下拉项）/只有左竖线（上池值
  框——取代六修四边 8% 白细线）；跳框关闭钮/预览展台**不属三级
  框**，新立 paint_thin_frame 均匀细框（四边 3px 渐变）承接。
- **v1d 十二修：渐变暗底+去框+均匀渐变（2026-09-14，用户拍板，
  修宪 theme.md §三/§四/§五/§七）**：①**所有渐变框内芯吃渐变
  暗背景**（dark(c)=lerp(c，黑，FRAME_BG_DIM=200) ≈ 22% 亮度，
  同一把 135° 尺不透明直写，嵌套反转内芯同步；paint_rect_ring
  加 grad_fill 分路——终端卡壳/AI 页主题基座走平色不吃）；
  ②三级框未选中暗形态**去所有框**只留渐变暗底（十一修「只有
  左竖线」形态退役，值框无边框化）；选中行 = 全包框（左粗 10+
  三细 3 整环渐变 α255，角部渐细只渐形状不渐色保留）；
  ③标签选中块两截硬切改**竖向均匀渐变** c1→c2（t=dy·255/(h−1)
  逐行精确——实机判「硬切换」）；④上池值框左竖线取消。考题：
  termview_spec 六枚涂装钉整批重写（三页 chrome/标签栏/三级框/
  双池），变异四咬全中（暗底平填回潮/未选中画框回潮/两截回潮/
  FRAME_BG_DIM 改值——均即红）。
- **v1e 十三修：暗标签渐变+标签列背衬+下拉三件套（2026-09-14，
  用户拍板，修宪 theme.md §四/§五/§六/§七）**：①未选中标签块
  三段条带硬切改**同一把 t 尺均匀渐变薄态 α48 满块**（实机判
  「暗块依然硬切」——只降 alpha 不降连续性）；②**标签列背衬**
  新立：圆角 36 无边框块（与值框同高对齐）= 渐变暗底 + 8% 白
  提亮（α20——实机判「边框去掉后背景不太明显」）；③下拉三重订：
  触发器 = 三级框全包框（选择控件归选择语言）+ 右缘 ▼；展开
  面板 = 整面圆角无边框深底（近黑 α252，旧 α245 退役）；选项行
  方形无个体背景，选中行 = 均匀细框。考题：标签栏钉未选中段
  重写+三级框钉加背衬断言+**下拉面板涂装钉新立**（before/after
  双帧对拍：深底精确值/角外原样/细框边芯），变异四咬全中（暗
  标签条带回潮/背衬不提亮/深底 α245 回潮/触发器去框——均即红）。
  ②标签体系入随机色：每标签独立走一遍 §2.2 约束区间随机
  （TabBar 持色列 colors，presence bind_tab_bar 后召唤配置卡 =
  全色列重随+accent 取选中项）；点选 = retint_cfg **瞬时**换色
  不重随（颜色过渡不做，留待卡片内拖动切换统一设计）。标签块
  选中 = 上 2/3 c1+下 1/3 c2 两截满填、交界 ±3px 短渐变；未选中
  = 上/下 1/3 条带薄态 α48+中 1/3 留 6% 白底；底线两模式之
  「配合标签」= 整根选中标签 c2 纯色（与选中块下 1/3 同色一体）。
  考题：tab_bar_spec +1（色列钉⑪）、ai_presence_spec +1（标签
  色列钉：召唤重随/retint 瞬时/未绑定兜底）、termview_spec
  标签栏涂装钉整文重写+三级框涂装钉新立（两形态/角部不混白/
  薄态精确值），变异五咬全中（底线渐变回潮/角部混白回潮/四边
  细框回潮/块色吃 accent 参数/retint 顺手重随——均即红）。
- **v1f 十四修：字段框动态宽度+动效预览动画+上池底距
  （2026-09-14，用户拍板，修宪 theme.md §五/§六/§七）**：
  ①字段框行两框宽随文字实量（标签锚左/值锚右/间隔 ≥3 格/
  值框最小 4 格/超长换行 ≤2 行，固定 12 格标签列退役）——
  触摸命中侧先量后锁（term→cfg_page 锁序）；②动效引擎四件
  跳框预览动画化（白球 r10 α220 = 手指，loop 2400ms，now_ms
  涂装入参单源，帧泵 ≤30fps + ConfigSig anim_bucket 维）；
  ③上池末行底距 1 格（upper_content_h +FIELD_BOTTOM_PAD）。
  考题 cfg_page_spec 27（+6）/termview_spec +5，变异六咬全中
  （固定宽回潮/间隔偷窄/底距删除/动画静态化/左对齐回潮/折行
  退化）。夹具教训：host 字体无 CJK，落墨钉用 ASCII。
- **隧道欠账（2026-09-13 实证）**：app 沙箱 na-tunnel.sh 看守会被
  **闸门会话回收**（setsid/nohup 均活不过闸门 disconnect；pgrep -f
  自匹配陷阱——看守在闸门里的「活着」全是自己匹配自己）→ 过渡 =
  **Termux 代管版** `~/na-tunnel-tmx.sh`（同转发映射，9021 正连/
  9022 反连，Termux 存活期独立于 app 重启）；代价 = 9021 绑回
  Termux 依赖，v2 app 原生隧道管家是唯一真解。另：app 侧 ws 断线
  重试有限次后放弃（日志停在最后一次重孵），隧道恢复后需重启
  app 才重连——重试退避/无限重试策略待 v2 一并定。

## 三、第二大类「终端设置」（2026-09-24 用户拍板「滚动的像素级」）

### 3.1 定位与目录映射

下池第二行「终端设置 / 滚动行为」（focus 1；focus 0 = 系统管理
服务器配置不变）。上池首行 = 「像素级滚动」下拉（复用单下拉机制）：

- **关（行级保底）——默认**：触摸滚动照旧 TouchScroll::moved 行级
  通道（余数挂账整行提交），行为与历史版本逐格等价。这是降级
  保底：新机制出现意外随时切回，不许删。
- **开（像素级实验）**：触摸滚动走 moved_px 不取整通道，零头进
  分数视口（TermView::scroll_frac_px，带符号 px）。

### 3.2 分数视口契约（实现单源 src/termview.rs）

- alacritty 网格只存整数行；滚动零头（scroll_frac_px）在渲染/合成
  期补——与键盘 kb_frac 同套路。正零头 = 往历史（内容下移），顶缘
  多收 line=-1；负零头 = 往最新（内容上移），底缘多收
  line=screen_lines。
- 钳制：offset=0 不许负零头（贴底之下没有更新的行）；
  offset≥history 不许正零头（历史顶之上没有更老的行）。拨关
  清亚行零头（画面回整行，不残留亚行态）。
- 收集路径双缘多收行（softbuffer render_into / GPU
  collect_gpu_cells 一把尺）；GLES 合成期实例平移
  dy - kb_frac + scroll_frac，scissor 带 (顶带内缘, 内容底沿)。
- tmux 全屏（mouse_report_active）协议无像素概念：像素车道下走
  **外置视口浏览态**（§3.5）；行级保底车道照旧 SGR 滚轮事件。
- 一次触摸内不换道：通道分流发生在两次触摸之间（设置页点开关）。
- **拖滚增益 DRAG_GAIN=2**（2026-09-26 用户拍板「2 倍滚动比：移
  10px 滚 20px」，单源 src/scroll.rs）：只乘手指位移、slop 门不动；
  行级/像素/滚轮三通道共用，甩尾初速吃增益后位移同倍跟随。

### 3.5 外置视口浏览态（2026-09-25 用户拍板「跟终端同款的像素级
滚动」——tmux 滚轮一次闪 5 行的根治；同日 v2 后台模型三条拍板）

**力学**：tmux attach 下屏上以外的内容在服务器 tmux 手里，本端网格
只有可见行——「平移视口」成立的前提是手里有全部内容。滚轮 tick 路
的本体病灶 = tmux 默认绑定 `WheelUpPane → send-keys -X -N 5
scroll-up`（服务器 list-keys 实证），1 tick 跳 5 行，跟手换算再细
也救不了对端的 5 倍率。

**契约**（实现单源 src/termview.rs browse 族 + android_app 接线）：

- **触发**：像素车道 + mouse_report_active + 有附着会话
  （cur_attached Some）的拖动起手 → **温热快照零感知切入**
  （v2 拍板①「不该有切入等待」——闲时拖动抬手/退浏览时
  browse_fire_capture 后台抓好的快照起手即用；无温热的首抓才
  走 capture-pane 往返，位移挂账切入补滚）。抓取命令
  `tmux_ctl::cmd_capture`（capture-pane -p -e -S -，目标 `'=name:'`
  精确会话+活动窗格，尾带 KFM_CAP_OK 成功标记——BAR-153：tmux
  报错文本与快照同走 stdout，capture_strip_marker 验收无标记不
  许进任何内容路径）。
  非 tmux 附体（裸 TUI 无快照源）= 滚轮翻译旧路不动。
  **切入方向闸**（BAR-158，2026-09-26 实录定罪）：挂账只许朝历史
  方向（d>0）净积压（scroll.rs browse_pending_gate 钳非负）——
  追底态朝 live 方向的位移是无效方向，放进状态机靠贴底钳回来，
  钳回瞬间的切入/退场换源 = 用户看得见的一闪；三条切入路（拖动
  挂账/甩尾挂账/首抓落地）同闸，甩尾朝 live 当场燃尽不挂账。
- **快照**：enter_browse 喂独立 Term（与 live 同网格尺寸，切入零跳
  动；超屏行沉 browse scrollback = 可滚里程；尾补 ?25l 藏快照光
  标——光标是 live 的发言权）。抓取往返期手指位移挂账
  （browse_pending_px），落地瞬间一并补滚——起手零丢失。
- **浏览期动态播放**（v2 拍板②「tmux 窗口就该是动态播放的」；
  v3 2026-09-25 晚用户拍板「视口在中央也该跟贴底一样活」——
  immutable-history 合并模型）：**历史不可变**——新输出只把屏顶行
  挤进历史尾，故刷新只小抓当前屏（rows 行级文本，capture_parse
  头行 `history_size history_limit` 双数验收），抓取线程合并出
  全量（merge_capture：旧文前缀 hist_old+k 行照抄 = 新历史，新屏
  接续；合并产物与全量抓逐字节同真），判负（清史/resize 过境/
  k>rows 超屏滚动/**hist 撞 history_limit 顶 = 史顶丢行**）即
  作废基账回全量。成本降两个量级 → 节奏提速：250ms 节流 +
  在途不叠（exec 往返自限 ≈2-4Hz）+ live 有新活动才抓（feed_seq
  比对，静默期零 exec 空转），落地 swap_browse_term 视口锚定
  内容守恒（BAR-154：offset 补偿底部追加行数），分数零头保留。
  **快照 Term 在抓取后台线程解析构建**（BAR-154：266KB ANSI 解析
  占 UI 线程 = 拖动卡一下病灶——exec 收文本 → 同线程剥验收尾标
  → build_browse_term → 转发预建 Term，排水只换壳零解析）。
  渲染/滚动/命中/选择/导出全切 active_term（=快照），与终端像素
  滚动同一套 scroll_px/分数视口机制；永不发滚轮 tick。
  live term 照常被会话泵喂——退出即回最新，零追赶。
  长按选择/复制在快照上照常用。
- **退出**（任一即退，退出 = 回 live 最新）：①**触底自动回**
  （v2 拍板③——滚回最新 = 回到现在：offset=0 且零头清零即退，
  不靠击键，手机交互）②击键（route_input 头，保底退场路）
  ③会话切换 Ctrl-] ④attach 切换 ⑤resize（resize_cells 防御
  清快照）⑥对端鼠标上报消失（about_to_wait 轮询——tmux 退出/
  脱离/重孵清场自愈）。退出清分数零头（视口态换源归零）；
  击键/触底退场即补温热快照（下次起手零等待）；
  **同次触摸内触底/击键退场后抑制重返浏览态**（BAR-154：触底
  回 live 手指未抬，在途抓取落地又 enter 把人扔回历史 = 「页面
  被静止」病灶——browse_suppress 置位，抬手 Ended/切会话/
  attach 才解锁）；
  切会话/attach 连带作废温热账（旧会话画面不许过境）。
- **降级保底铁律不变**：设置页切「关（行级保底）」= 整条快照路
  不触发，滚轮翻译照旧。
- **惯性甩尾**（2026-09-25 用户拍板「工业级滑动」，scroll.rs Fling
  物理机，参数直译 kfmv4 文件树 canvas-scroll 实测手感）：拖动期
  moved_px_at 逐事件采样末速（d/dt×16.667×1.7 增益，停滞即杀速——
  按住停顿再松手不甩）；抬手交接帧泵（about_to_wait 时间切片等比
  折帧，位移走与拖动同一把尺 scroll_px——浏览态滚快照/live 滚
  终端史/首抓在途进挂账），速度每帧 ×0.96 衰减，启动阈 0.5/燃尽
  阈 0.3 px/帧；新触摸/捏合落地即取消，触底（浏览态 = 与拖动同一
  判据回 live + 同次触摸抑制）/触史顶即燃尽，车道翻牌（设置切关/
  切全屏 TUI/切会话/attach）即终止。滚轮车道（全屏 TUI）不甩——
  滚轮语义归对端。钉 scroll_spec 五钉（采样公式/点按低速不甩/
  停滞杀速/衰减燃尽位移账/时间切片折帧），变异实咬（摘增益→红、
  衰减改 1.0→红→复原绿）。

#### 3.5.1 v4 推流画布（2026-09-25 tmux -C 控制模式，用户拍板
「直接换真方案」——v3 轮询快照降级为保底路径）

**为什么是 v4**：v3 的 250ms 轮询合并再快也是「逼近」——刷新即
2-4Hz 一顿一刷。模型的正身 = **隐藏画布在后台随字节流即时生长，
视口只是画布上的一块平移窗**（用户原案：打开 na 就把 tmux 窗口在
后台静默渲染好，滚动 = 平移视口，不看的位置动画也不许停）。

**力学**（2026-09-25 服务器 tmux 3.4 逐条实证）：`tmux -C
attach-session -f ignore-size -t '=名:'` 起控制模式常驻通道
（第二条 ws 连接，复用 conn::ws_spawner，服务端 pty_sess 零改动）
——tmux 把窗格新输出以 `%output %<pane> <三位八进制转义载荷>`
行即时推来；命令回应包在 `%begin/%end` 块内，**%output 严格不插
进块内**（带内播种天然对齐：块前字节已在快照、块后字节续喂，零
丢失零重复）；`-f ignore-size` 必需（控制客户端 pty 默认 80x24，
不带旗 attach 会抢用户窗口尺寸）。

**契约**（实现单源 src/tmux_ctl.rs 控制协议族 + src/ctrl_feed.rs
播种/续喂相位机 + src/termview.rs Canvas + android_app ctrl_ 薄壳）：

- **带内播种**：通道起即在控制通道发 `cmd_ctrl_seed` 双块——
  头行块（display-message KFMHDR 前缀：史量/上限/光标列/光标行/
  pane id 五件，parse_seed_header 认领）+ capture 块（-p -e -S -
  全史，块内正文即纯净 capture，无壳无尾标）。capture 收齐 →
  **Canvas 在后台线程构建**（BAR-154 同款：UI 线程零解析），
  构建期 %output 字节进 ctrl_pending，安装时先补喂再接流。
- **续喂**：稳态 %output 字节（pane id 过滤——非活动窗格的字节
  不许混进）直喂画布。浏览中 = feed_browse 进视图画布；不在浏览
  = 喂 App 后台画布（browse_canvas）。**转义序列可跨 %output 块
  截半**——Processor 常驻画布（即用即弃会把半边序列当垃圾），
  这是 Canvas 类型与快照 Term 的唯一差别。**锚守恒靠 alacritty
  内置**：display_offset>0 时新行入史自动抬 offset（阅读位钉死，
  termview_spec 实证钉——不需 swap 臂的补偿账）。
- **进出**：拖动起手 browse_canvas 落位 = 零等待零抓取（连温热
  快照的 exec 往返都不需要）；触底/击键/甩尾退场 = 画布交还 App
  后台续喂（take_browse_canvas），下次起手零等待。**切会话/attach
  不拆通道**——画布交还旧会话条目后台续喂（预热池），切回零等待；
  resize = 全池画布作废重建但通道不拆；通道死亡 = 该会话条目判负
  拆通道留壳进 5s 退避。
- **预热池（全会话后台渲染，2026-09-26 用户模型定案）**：打开 na
  即把服务器真表全部 tmux 会话在后台静默接控制通道+播种建画布，
  切窗/滚动零现场连接。池扩缩**唯一凭据 = 服务器真表 List**（不是
  本地记忆）；容量帽 `WARM_POOL_CAP = 8` + 当前会话兜底（真表外也
  保住当前）；池条目 = WarmSess 壳（ctrl: Option<TermHandle> /
  feed 相位机 / canvas: Option<Canvas> / pending / build rx /
  播种账），ctrl=None 即退避中但壳与对账账保留；对账档：当前会话
  5s、停放会话 30s；`ctrl_teardown_all` 仅在关闭 tmux 功能（want=
  false）时全拆。
- **自愈**：5s 对账重播（重走播种双块——换尺/换 pane/UTF-8 跨
  帧损坏全自愈）；%layout-change/%window-pane-changed 等 Notify
  清零对账账逼下拍重播；通道 %exit/Exited/Failed = 判负回落
  （拆通道 + 5s 退避），**v3 轮询链自动接管**（ctrl_active 闸开 =
  browse_fire_capture/250ms 浏览期刷新/温热快照全链复活）。
- **v3 保底语义不变**：推流活着时轮询抓拍全链熄火
  （browse_fire_capture 头闸 + 浏览期刷新闸双封）；推流死了 v3
  原样接管——设置页「关（行级保底）」照旧整路不触发。

**判卷**：tmux_ctl_spec 控制协议钉（cmd_ctrl_attach 含 -f
ignore-size 常驻无 exit / ctrl_unescape 八进制实证+畸形不丢字节+
UTF-8 跨行拼接还原中文 / parse_ctrl_line 六类分类实证+畸形不
panic / cmd_ctrl_seed 双块串 / parse_seed_header 实证 fixture+
缺件畸形全 None）；**ctrl_feed_spec 相位机八钉（BAR-155：pty 实录
fixture 全序（真流形态 \r\n）/attach 空块跳过/头块关不产 Build/
\r 尾必剥（三号病灶：剥 \r 留壳层 = pane 段 "3\r" 数字解析失败
100% 判负）/行跨包截半装配/播种窗口字节三路/判负三路/Notify 逼
对账+%exit——变异三咬全中）**；termview_spec
推流画布五钉（**续喂生长锚守恒
= alacritty 自动抬 offset 的根基假设实证** / 交还往返零损耗 /
转义跨块续喂=常驻 proc 的存在理由 / 对账重播锚守恒 /
**BAR-156 播种游标归位：capture 恒发全屏含尾空行、文本尾≠真实
游标，Build 携头行游标 CUP 归位——动态行续喂帧原位更新零复制**）。
C 档实机判卷点：kimicode
输出中滚到中部，内容**连续即时**流动（不再一顿一刷）且阅读位
钉死不动；触底回 live 后再进零等待；**切到其他 tmux 窗口后第一次
滚动零等待**（预热池：报表应见 N 会话 = N 条「ctrl 开（播种发出·
预热池）」）；拔网/杀 tmux 会话后 v3 轮询
无感接管。

**判卷**：termview_spec 浏览态五钉（快照切换与像素滚动/resize 清
快照/光标藏起来/原地刷新锚守恒=BAR-154 补偿语义（追加 2 行 offset
+2、可见内容同批）/feedseq 活动判据），变异咬人全中（enter 不喂
快照、exit 不清零头、swap 不补偿追加行——均即红）；
tmux_ctl_spec cmd_capture 钉（'=name:' 精确会话+活动窗格/-e 保色/
-S - 全史/史量双数头行）+ BAR-153 标记验收钉 + v3 增量合并钉
（屏顶行挤进历史尾/判负四路回落，变异双咬：摘 k>rows 闸→红、
切片上界 +1→红→复原绿）。C 档实机判卷点：拖动起手零等待
切入（温热），浏览期内容随 live 动态刷新且阅读位钉死不跳，触底
回 live 后同次拖动不再被扔回历史，全程无解析卡顿。

### 3.3 数据模型

terminal.json 增 `pixelScroll: bool`（宽容缺省 = false）。
点选写盘 + term 即时 set_pixel_scroll 分流（apply_pixel_scroll_pick，
写盘失败 = 上报不炸，同默认服务器行纪律）。

### 3.4 实现台账

- **v1（2026-09-24）**：scroll.rs moved_px（同 slop 门不取整，越阈
  第一笔 slop 段不计入）/ termview scroll_px+分数视口+双路径多收 /
  GpuCell.py i64 化 / GLES present_frame clip (top,bottom) 对 /
  触摸分流 + 启动读回 + 设置页 UI。考题 scroll_spec +2 /
  termview_spec +3 / settings_spec +1，变异三咬全中（多收行删除 /
  贴底钳制删除 / moved_px 取整回潮——均即红）。

## 四、第三大类「会话池」（2026-09-26 BAR-163 工单⑥，一期只读）

### 4.1 定位与目录映射

na-agent 的会话与信箱在手机上有了只读查看口。下池 = 路由表
（/root/.kfm/session/ 各 agent 线文件夹 + 信箱特殊路由收尾）；上池 =
选中路由的条目表（线 → NNNN-*.jsonl 会话文件列表带字节数；信箱 →
*.md 信件列表）；点条目 = 开查看器跳框看内容（会话 → wire jsonl
尾部 60 事件渲染成可读对话；信 → markdown 直读）。**一期只读**——
不发信、不起线、不删会话。

### 4.2 数据面（不开新口）

全走 na-server `/agent` 前缀反代（127.0.0.1:9041 agentd，照 `/kfmv4`
别名先例）：手机经既有 9021 隧道本地口 `GET 127.0.0.1:{port}/agent/
api/agent/...` 直达 agentd。新增四面：lines/<线>/sessions（线内会话
列表）、lines/<线>/sessions/<名>/tail?n=N（点名会话尾部事件）、
mailbox/letters（信件列表）、mailbox/letters/<名>（信正文）。闸：
会话名只认 NNNN-*.jsonl、信名只认 ASCII *.md（README.md 不算信）、
非法 → 400、不存在 → 404。路径段非 ASCII（0001-会话.jsonl）原样
UTF-8 透传（两端自家码）。

app 侧取数 = src/sess_pool.rs（svc_health 同款：http1 手写客户端打
隧道本地口 + OnceLock 快照 + DIRTY 脏帧）；wire 渲染 = src/wire_render.rs
纯函数（tool_call/tool_result 配对折叠一行、usage 聚合一条账行、
坏行计数容错）。

### 4.3 查看器跳框

与组件池 comp modal **同层级**（modal 盖配置页最上层一档，不引入新
层级；BAR-119/120/121 inset/层级契约不动）——同一套压暗层/居中卡/
标题/分隔线/关闭钮，几何是 modal.rs 的无预览画板族（viewer_card_rect/
viewer_fields/viewer_fields_top，眼手同尺）。点条目先落「加载中…」
框，取回件标题对上才喂真文（在途旧件不盖新框）；关闭钮/框外 = 收。
切标签页查看器随 modal 律清零。

### 4.4 实现台账

- **v1（2026-09-26）**：na-server /agent 反代（httpd.rs agent_upstream
  只放 /api/ 面 + main.rs agent_proxy tokio 中继，读超时 3700s 吃
  /send 慢口）；agentd 四端点 + 闸；wire_render / sess_pool（A 档
  纯核）+ android_app 接线（tab2 分流/rebuild_cfg_rows 路由+条目/
  查看器开合与喂文/sess_pool::configure+take_dirty）。考题：
  agent_proxy_spec 4 / sess_pool_api_spec 5 / wire_render_spec 6 /
  sess_pool_spec 7 全绿；变异四咬全中（摘 /api/ 闸 → 钉①红；
  摘 is_session_file 闸 → 钉②红；摘 usage 聚合 → 钉③红×3；
  摘空态占位 → 钉④红，均复原绿）。

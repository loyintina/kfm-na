# UI 基础层契约（ui-base）

> 这是什么：2026-08-30 用户拍板立项的 na UI 架构契约——硬切换基座 +
> 基础层内置采样缝 + 动画全插件。四节契约 + 纪律条款表 + 决策记录。
> 性质：契约文档（纪律载体，随实现增补；本文档自举三行块排版纪律）。
> 关联：`立项.md`（三层架构）/ `multi-end-layering.md`（分层宪法——
> ui-base 全部落在壳层，核心层零触碰）/ kfmv4 `term-contract.md`（先例）。
> 状态：已立项未实现。第一消费者=AI 对话面板（先 AI 后文件树），
> 控件与缝从它身上提取，提取一个登记一个。
> 视觉基调（2026-09-04 用户拍板）：像素+霓虹+精灵球的游戏感是底色，
> 体验层不复刻 kfmv4（协议层兼容照旧），游戏感住长什么样、不住动不动。

## 一、总架构

- 基础层**只写目标值**：状态变化瞬时生效，渲染下一帧即达（硬切换）。
- 基础层内置**采样缝**：可动画属性渲染时过一道槽位，无插件占槽则直通
  目标值——`rendered = anim_slot(prop).sample(now).unwrap_or(target)`。
- 动画全是插件（ui-fx）：占槽驱动插值，拔掉后功能等价，只是变糙。
- 主题包 = 调色板 + 字体 + 曲线绑定，填的正是同一批槽。

## 二、可动画属性注册表

（初始为空——只有登记进此表的属性才允许开槽位。第一消费者动工后填入。）

| 属性 | 首消费者 | 开槽日期 | 备注 |
|---|---|---|---|
| AI 面板 Y 偏移 | AI 对话面板 | 2026-09-04 | 定时缓动（ui-fx，fx_ease.rs）：落下 ease-out / 收起 ease-in（2026-09-04 用户拍板，CSS transition 语言，取代当日的弹簧墩感方案；定档 350ms/250ms，09-11 二审提速 250ms/180ms）；无占槽=硬切。**2026-09-11 曲线定稿 power2.out**（1-(1-t)²，nz/kfmv4 GSAP 同款减速——前身 09-06 物理重力 t² 被录屏逐帧实锤判「冻结→跳变」：起步亚像素蠕动+alpha 随 t² 晚显影）；**2026-09-10 加配滑动淡入 → 09-11 二审取消**（用户判「不好看」）：alpha 恒 1.0，panel_fade_alpha 退役备查（考题钉住）；面板槽 tint.α + AI 文字 u_alpha 恒实，终端网格/键行/上层 chrome 恒 1.0（「终端格子永不动画」红线）；softbuffer 兜底路径保持硬切（基座语义：功能等价只是变糙） |
| 配置页 X 偏移 | 配置页（API 接入管理） | 2026-09-10 | 面板栈模型第二公民（ai-presence §五B/D12）：左滑召唤从右缘滑入（目标值 0 靠泊 / +屏宽 屏外右），右滑推回；定时缓动复用 fx_ease 方向分档（入场滑向 0 = d<0 走 rise_release 臂 180ms、离场滑向屏外右 = d>0 走 power2_out 臂 250ms——水平滑动反向复用；2026-09-11 起两臂同为减速曲线，gravity_fall 臂退役；09-11 二审提速 350/250→250/180），显影 09-11 取消（恒 1.0）；无占槽=硬切；**跟手拖拽锁定期壳层旁路缝采样**直读 panel_drag.current_offset（直接操纵不是动画，ai-presence §五B 跟手拖拽条） |
| 文件树页 X 偏移 | 文件树页（占位壳先行） | 2026-09-11 | 面板栈第三公民（ai-presence §五B）：右滑召唤从左缘滑入（目标值 0 靠泊 / **-屏宽** 屏外左——符号与配置家镜像，曲线同一件 fx_ease 不感知左右），左滑推回；跟手拖拽旁路、无占槽=硬切全同配置家。落地动机：不留手势语义洞——右滑在任意栈态都有唯一归宿（ai-presence §五B 滑向契约表） |
| 解析页 X 偏移 | 解析页（占位壳先行，青蓝 0x00081026 底） | 2026-09-12 | 面板栈第四公民（ai-presence §五B 四公民落地·三缘语义 D7）：**右缘家**——左滑召唤从右缘滑入（目标值 0 靠泊 / **+屏宽** 屏外右，符号语义与配置页完全相同），右滑推回；曲线/跟手拖拽旁路/无占槽=硬切全同配置家（同一件 fx_ease 不感知左右）。**配置页让出左滑召唤槽**（设置页独立第四页，D7：齿轮唯一召唤口、右滑唯一关闭口） |
| 键盘 inset（chrome 跟随） | 输入栏+快捷键行 | 2026-09-04 | 弹簧平滑（ui-fx）：100ms 轮询轨迹是阶梯，纯镜像太硬；**终端 resize 不过缝**（pty 抖动红线，永远吃真实值） |
| 上池池高 | 配置页双池 | 2026-09-15 | **分域律**（BAR-095）：Upper 域（点下池）= 250ms ease-in-out 缓动与光标/平移同钟同曲线（「光标到位池高也到位」，零过冲）；Page 域（切标签）与非平移变更 = 目标值直通（新页池高起步帧就位，贴死零二次动画）。BAR-094 曾一刀硬切=误读「无过冲」为「无动画」，已改判；滑行期逐帧重烘挂债（BAR-096） |
| 下池光标滑行 | 配置页下池 | 2026-09-15 | 250ms ease-in-out cubic（BAR-094）：与视口平移同钟同曲线严格同步零过冲（select 同刻挂账，相对进度 ≡ pan t 有钉）；切标签/行表 clamp 硬切不动画；滑行期逐帧重烘配置槽 = 挂债（同标签游标，BAR-096） |
| 标签栏游标 | 配置页标签栏 | 2026-09-15 | 250ms ease-in-out cubic（BAR-095 曲线单一源）：select 重定基续滑，原欠阻尼弹簧过冲回摆用户逐帧点名废除；游标 x 逐帧进 ConfigSig = 逐帧重烘挂债（BAR-096） |

## 三、槽位 API（缝）

- 槽位占用作为 cordis-na 插件面的一类 capability，manifest 声明占用哪些槽。
- 组合策略 v1：每属性单槽，后占者赢，不做混合栈（等真实需求逼出来再议）。
- 铁律：**缝不能后补**——没有缝，任何动画都要改每个控件。动画可后插，
  缝不行，所以缝随第一消费者首次动工一次落位。

## 四、帧时钟策略

- 按需启停：无活跃动画 = 零额外帧、零唤醒申请（对照夜判据：na 自身
  0.45% 单核不许被动画系统抬升）。**收敛补终帧（2026-09-14 用户
  实机抓「下拉收起留一行黑色余影」定案）**：活性探针翻false 的
  那一圈必须再脏一帧——否则屏幕定格在收敛前最后一帧（进度≈0.02
  的余影/光标≈差 2px），泵停后无人擦除。泵闸条件 = `prev || curr`，
  不是 `curr`。
- 帧预算 = 显示真实刷新周期（BAR-077，2026-09-10 拍板）：壳每次
  resumed 经 JNI 读 `Display.getMode().getRefreshRate()` 喂核心层，
  120Hz 屏 8ms / 60Hz 屏 16ms 自适应；未喂前默认 16ms 保守基线，
  钳 4~33ms 防病态。旧契「上限 60fps」废止——120Hz 屏硬钳 60fps
  = 屏幕每刷两次画面才动一次 = 肉眼拖影。最后一个动画结束即停表。
- **产帧许可 = vsync 锁相（BAR-081，2026-09-11 残影定案）**：动画期
  （Choreographer 挂表窗口）帧时钟不吃定时器预算，吃 vsync due 账——
  一跳产一帧、相位跟屏（定时器泵与 vsync 双钟不锁相 = swap 间隔
  1/9/25ms 的残影真凶）；连跳合并产一帧；链死 >32ms 看门狗退预算
  节流防卡死、链复活即重锁；未挂表（Choreographer 缺席/动画收尾后）
  = 预算路照旧。零新唤醒源：due 消费走既有 4ms 轮询圈。

## 五、基础层-插件分界表

| 项 | 归属 | 判据 |
|---|---|---|
| 目标值语义（内容不被键盘盖、行带位置） | 基础层 | 语义，拔不得 |
| fling 惯性滚动 | 基础层 | 决定「看哪一行」=语义 |
| 键盘避让的目标值计算 | 基础层 | 同上 |
| 键盘避让的呈现动画 | ui-fx 可占 | ~~只许镜像键盘自身轨迹（逐帧 insets），禁自造补间~~ **2026-09-04 用户拍板改判**：100ms 轮询（BAR-006 事件驱动死路）拿到的轨迹是阶梯，纯镜像实看太硬——改为轮询采样点间弹簧平滑；目标值仍恒等于真实 inset（缝只许插值），终端 resize 不过缝 |
| 面板/卡片/浮层进出场动画 | ui-fx | 拔掉=功能等价只是变糙 |
| 快捷键行 | 层级低于 AI 面板 | 过渡帧照画：被落下的面板盖住是自然结果；硬切拿走 = 两端 pop 闪烁（BAR-063） |
| 主题（调色板/字体/曲线） | 主题插件 | 填同一批槽 |
| 终端格子内容 | 永不动画 | 可读性+性能红线 |

## 六、纪律条款表（验收钉，实现时挂考题进 chain/regress）

| 纪律 | 考题判据 |
|---|---|
| 缝在基础层 | 无插件占槽：下一帧渲染值==目标值（硬切） |
| 拔动画插件功能等价 | na-regress 禁用 ui-fx 全卷绿 |
| 动画不碰终端格子 | 动画运行中 grid 快照逐字节不变 |
| 零空转帧 | 空闲期帧计数=0，零唤醒申请 |
| 避让只镜像 | ~~自造时长补间=红；逐帧跟 insets=绿~~ 2026-09-04 改判（D5）：轮询阶梯弹簧平滑=绿；缝输出偏离「向真实 inset 收敛的插值」=红 |
| 动画零光栅零上传 | panel-anim 仪表：动画期 ras/upload 计时不涨（2026-09-07 §七） |
| sig 全列 | 槽 sig 必须列全 paint 读过的每个输入（LayerSigs 注释逐槽清单）；漏输入=鬼影=红 |

## 七、渲染成本模型（图层烘焙，2026-09-07 随 AI 面板动画立项落地）

> 用户拍板「干脆先把引擎做好，未来做 UI 减少重复劳动」——本节是
> ui-base 缺的另一半：§一~四管「值怎么变」，本节管「变的成本谁付」。

- **合成期优先律（2026-09-15 用户拍板「明明有这个路径就必须走」——
  本节唯一硬法）**：一切动画默认合成期（placement/alpha），**禁止把
  动画进度写进烘焙 sig**——写进去 = 逐帧重光栅+逐帧上传，等于绕开
  整个成本模型。新动画入册时审：进度值只许流向合成期参数，流向
  sig 即违宪；确属画布内容变（真重烘焙）才进 sig，且须在本节债表
  挂账说明。违宪判例：视口平移（十七修）曾把 pan 偏移写进 sig 逐帧
  双代重烘焙（真机掉帧实感），十九修已升合成期双纹理。
- **槽位**：每块静态可烘焙面 = 一个槽（ChromeSlot：画布+纹理+可见性）。
  八槽（2026-09-15 十九修平移升合成期+BAR-092 三咬）：键行 / AI 面板底 / 配置页 /
  文件树页 / 上层 chrome（输入栏+光球+放大镜）/ **终端卡**（碳灰环+
  近黑内芯，终端包壳与三面板同尺同配方——终端是基座不是卡：无色相
  碳灰环；网格视口随壳内缩：横 49 = 外缘16+环粗3+净垫(12+一整格字宽
  18，09-11 晚用户实拍「太紧」加宽) / 纵 31 = 16+3+12，几何单源
  termview::MARGIN_X / MARGIN_Y / **平移旧代**（PanOld：平移起步帧
  从配置槽画布整幅拷贝=旧代冻结封存，每代一次）/ **平移新代**
  （PanMove：起步帧烘新代稳态——BAR-092 三咬实锤 Upper hold 期配置
  槽无上池行，新行必须独立成层；连环平移 PanOld 从上一笔 PanMove
  接力捕获）。
  菜单/工具卡/对话框等未来 UI 入册即白拿「动画零光栅」。
- **烘焙**：内容 sig（LayerSigs）变化 → 重光栅槽画布 + 一次上传
  （slot_bake）。sig 必须列全 paint 读过的每个输入，漏输入=鬼影。
- **动画**：placement/alpha 变换在合成期进实例 rect（layer_prog），
  **永不触碰槽画布**——AI 面板 panel_off 即第一实例（烘焙画布恒为
  靠泊位，panel_off 不进 sig）。
- **每帧内容**（终端网格、AI 文字实例）走 GPU 实例管线，与槽位正交。
- **已知的期 2 债**（记入 state.md 欠账）：①键盘避让期 ime 逐帧变 →
  面板环/键行带 sig 含 ime 仍逐帧重烘焙（面板环改「inset=0 烘焙 +
  placement 平移」需解决顶缘不等价问题）；②槽画布 v1=全屏尺寸，
  条带槽（键行/输入栏）改条带画布省内存省上传；③放大镜内容跟网格活，
  拖选期强制重烘焙（正确性换性能，可接受）。
- **BAR-096 债（2026-09-15 立案，池区动画帧饥饿）**：光标滑行/标签
  游标/池高缓动的 sig 维逐帧触发配置槽**全页**重烘+~14MB 上传
  （draw_avg 实测 47ms/帧）→ 250ms 动画只画 5-7 帧 = 用户逐帧
  「跳变/过冲感」的真凶（曲线本身单调无过冲，panc 遥测+钉双证）。
  根治 = 光标框/标签游标/池框**合成期化拆槽**（小画布独立槽或
  placement 平移），动画帧 CPU→0。在拆槽落地前，池区动画的平滑度
  受 ~21fps 上限约束——判卷时区分「曲线错」（红）与「帧饥饿跳」
  （债，BAR-096）。
- 判卷仪表：panel-anim（动画期逐帧）/ gles-stage（300 帧滚动）照旧。

## 八、决策记录（三行块自举——拍板理由入库）

【D1】总架构
决定：硬切基座+基础层内置采样缝+动画全插件+主题填槽
理由：动画可后插，缝不可后补（改缝=改每个控件）；硬切基座=确定性系统，考题只断言终态
否决案：一体式动画（拆不出去/主题无槽可填）；缝也插件化（插件没有可挂的钉子）
标签：成本不对称

【D2】交互语义归属
决定：fling 与避让目标值在基础层；避让呈现动画可占槽但只许镜像键盘自身轨迹
理由：fling 决定「看哪一行」是语义；跟踪参考轨迹不添延迟，自造补间添延迟且失同步时输入区被盖
否决案：键盘避让一律禁动画——把「跟踪」与「自造补间」混为一谈（2026-08-30 用户否决后改立本条）
标签：能观能控

【D3】控件来源
决定：控件库锚定第一消费者提取式生长；收编进库需 ≥1 真实消费者+1 考题
理由：无消费者的通用控件库是想象设计；需求不会自己长，但会长在真实消费者身上
否决案：先造通用控件库再找消费者（2026-08-30 用户质疑「需求不会自己长」后合议定案）
标签：打脸结晶

【D4】排版纪律
决定：含决定的条目尾部带 决定/理由/否决案 三行块；理由挂四标签封闭集（打脸结晶/外部判据/成本不对称/能观能控）
理由：拍板理由入库=二阶控制的第一个传感器（需求冶炼笔记 §三）；否决案栏让未来 agent 不翻案
否决案：自由文本无格式（需求冶炼笔记开放问题一的悬置态——理由不可机械检索）
标签：外部判据

【D5】键盘避让动画改判（2026-09-04 用户拍板，修订 D2）
决定：chrome（输入栏/快捷键行）跟键盘开合走弹簧平滑——开第二道缝「键盘 inset」；终端 resize 不过缝，永远吃真实值
理由：事件驱动在本机是死路（BAR-006），100ms 轮询拿到的轨迹是阶梯，纯镜像实看太硬（用户原话「没有对应动画，显得很硬」）；D2 的「禁自造补间」防的是失同步盖输入区，弹簧恒向真实 inset 收敛不失同步
否决案：纯镜像轮询阶梯（=现状，用户判硬）；终端网格也跟动画 resize（pty 抖动红线）
标签：打脸结晶

【D6】渲染成本模型=图层烘焙引擎（2026-09-07 用户拍板「干脆先把引擎做好」）
决定：槽位烘焙（sig 置脏）+ placement/alpha 合成期变换；动画帧永不触碰槽画布；首批三槽随 AI 面板动画落地，未来 UI 入册即白拿
理由：面板动画实测 68fps+28~37ms 毛刺的病根=动画期全画布重光栅+全屏上传；逐 UI 打补丁是重复劳动，引擎化一次投入（浏览器/Flutter/游戏精灵同款分层合成，外部判据）；不引外部引擎（wgpu 判死先例），槽位设施 ~200 行落在壳层
否决案：逐 UI 单独优化（每块新 UI 重复交一遍学费）；直接上成熟 UI 引擎（体积/控制权代价，且缝+槽架构已自洽）
标签：成本不对称

【D7】三缘语义标准+设置页独立第四页（2026-09-12 用户拍板，四公民落地）
决定：三缘各有一家——左缘=路由（文件树，切换中央对象）/ 右缘=解析器家族（解析页，解析中央对象）/ 顶缘=全局 AI（光球面板）；设置页=独立第四页：齿轮按钮唯一召唤口 + 右滑唯一关闭口，不占任何滑槽（SummonConfig 手势角色删除）；未来浏览器是中央对象（占终端页位置），入口在路由侧（左缘家），不占面板槽
理由：不留手势语义洞——任意栈态每个滑向都有唯一归宿（一滑一义）；设置页是全局管理面不属于任一缘的语义家，齿轮按钮显式入口比滑槽更可发现；左右缘对称后「切换对象 vs 解析对象」两个动作分家不混
否决案：设置页继续占左滑召唤槽（解析页无槽可进，左右缘语义不对称）；设置页做中央对象（它不是被解析/被路由的内容，是全局管理面）
标签：能观能控

【D8】平移动画升合成期（2026-09-15 用户拍板「明明有这个路径，我们自己都没走，那这个路径就没意义了」）
决定：视口平移（Page/Upper 两域）从烘焙期双代同画升合成期——平移起步帧 PanOld 槽整幅拷贝旧配置画布（=旧代冻结，零重涂装）；动画期合成四连：「配置槽@0 全幅 + 裁剪带隙底填色 + PanOld@−t·travel + 配置槽@(W+G)−t·travel（带内第二遍=新代进场）」，后三笔 scissor 到裁剪带；配置槽烘焙期照常画新代稳态（pan 不进涂装），ConfigSig 删 pan_q——平移进度永不离 sig；裁剪带/留隙 G/曲线语义照 theme §七不变；softbuffer 兜底路径保留涂装域双代同画（降级保真，不是违宪豁免）
理由：真机实测 Page 平移掉帧感而 Upper 顺——同曲线同时长，差在每帧成本：pan_q 逐帧触发配置槽全页双代重光栅+~12MB 重上传，恰好是 D6 立模型时要灭的病；手势面板流畅 = placement 合成期同款机制已存在，平移没接上是实现偷懒不是能力缺失；升合成期后每帧 CPU≈0，曲线自由（ease-in-out 不再需要硬快曲线遮掉帧），未来一切平移类 UI 白拿
否决案：继续烘焙期只换更快的曲线（=遮掉帧不治掉帧，用户否决「硬快曲线」问句时已识破）；旧代 CPU blit 位图（治标，上传仍在）；独立新代槽 PanMove（配置槽带内第二遍可代，省一张全屏纹理 12MB）；裁剪带局部重烘焙（sig 复杂化，仍是逐帧光栅）
标签：成本不对称

【D9】池区动画时序归正（2026-09-15 用户拍板「过冲是我从来没要求你做的，你为什么擅作主张加入」「同步改异步也不是我允许的」）
决定：池高弹簧废除 → 目标值硬切零动画；下池光标弹簧 → 250ms ease-in-out cubic 与视口平移同钟同曲线（select 同刻挂账，严格同步）；BAR-093 的 pan_freeze 冻结喂入随之废除
理由：过冲（ζ=0.76 欠阻尼 ≈2.5% 屏高「墩一下」）是键盘 inset 手感参数被复用到池区，从未被用户要求；pan_freeze 制造三症状——PanMove 新代烘在冻结旧池高上（新页面带旧行高）、贴死后弹簧续弹（二次高度动画+过冲）、光标先到高度后弹（同步改异步）；灭弹簧后喂入即到位 = sig 起步帧变一次一烘，平移期恒定，BAR-093 治帧饥饿的目标由灭弹簧天然达成且不再冻结任何喂入
否决案：池高渐变与平移同步走 t（高度是 sig 维，逐帧重烘违合成期优先律 §七，帧饥饿复发）；弹簧改临界阻尼保留池高动画（用户拍板「平移到位后不要再有任何动画」，硬切才是字面执行）；光标硬切随池高（用户逐帧判「瞬移+过冲」不应该有，下池选中框要平移过去）
标签：打脸结晶

——kfm-na(Kimi Code) · 2026-08-30 立项 · 实现随 AI 对话面板动工

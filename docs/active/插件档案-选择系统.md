# 插件档案 002 · 选择系统（输入栏文本选择 + 复制粘贴菜单）

> 2026-09-02 立项（用户拍板：先写设计文档再开工）。
> 定位：输入栏线的下一自然延伸——把「只能打字+光标」升级到「能选、能复制、能粘贴」的
> 标准移动端文本编辑体验。本文档是交互细节、状态机、边界规则、验收判据的唯一源。
>
> 活文档纪律：后续对选择系统的任何修改、事故、调参追加到「闭环后迭代」一节。

## 起点：要解决什么问题

当前输入栏只有光标和定位柄，没有选区。用户无法：
- 复制已输入的文字到别处；
- 删除/替换一段文字；
- 把别处复制的内容贴进来；
- 快速全选后清空或替换。

这是移动端文本编辑的「基础尊严」功能，必须在进入 AI 对话页/浮层之前补齐。

## 范围与边界

### 做（MVP）
1. 输入栏内**长按进入选择模式**。
2. **选区双锚点**（左/右），可拖动调整范围。
3. **选中文字高亮**（品牌色半透明底）。
4. 弹出**操作菜单**：复制 / 剪切 / 粘贴 / 全选。
5. 与系统剪贴板打通（Android `ClipboardManager`）。
6. 选择状态与现有输入栏状态（cursor/composing/handle/follow/scroll）互斥规则。

### 不做（明确延后）
- 终端网格选择（终端是另一套文本模型）。
- 双击选词 / 三击选段（可二期加，先保长按+手柄稳定）。
- 富文本 / 样式粘贴（只处理纯文本 `ClipData.Item`）。
- 悬浮工具栏 / 自定义菜单项（先系统标准四键）。
- 选择历史 / 多剪贴板。
- TalkBack / 无障碍（二期）。

## 状态机

选择系统新增状态核字段（全部挂在 `InputBarState::Inner`）：

```rust
selection_start: usize,   // 选区左锚点（char 下标，含）
selection_end: usize,     // 选区右锚点（char 下标，不含）
selecting: bool,          // 是否处于选择模式
sel_anchor: SelAnchor,    // 当前被拖动的锚点：None / Left / Right
```

与既有状态的关系：

| 既有状态 | 选择模式下的语义 |
|---|---|
| `cursor` | 当 `selecting=false` 时照旧；当 `selecting=true` 时 `cursor = selection_end`，退出选择模式后光标落在选区尾 |
| `handle` | 选择模式下隐藏蓝色定位柄，改为显示双锚点 |
| `composing` | 进入选择模式前先 `finish_composing`（组合文本落真字），避免虚拟拼音参与选区 |
| `follow` | 拖动锚点导致 cursor 移动时，按编辑操作规则回尾锚（follow=1） |
| `scroll_px` | 锚点拖出 field 上下边界时，自动滚动视口（同拖动滚动规则） |

### 状态迁移表

| 事件 | 当前状态 | 下一状态 | 规则与禁令 |
|---|---|---|---|
| 长按文本区（≥SELECT_LONG_PRESS_MS=400ms，位移<slop） | 普通编辑 | 选择模式 | `selection_start = selection_end = 点按 char 下标`，`selecting=true`；**禁令**：组合态必须先 finish；**禁令**：长按期间若位移≥slop则判为拖动滚动，不进入选择 |
| 拖动左锚点 | 选择模式 | 选择模式 | `selection_start = min(锚点位置, selection_end)`；锚点不能越过右锚点 |
| 拖动右锚点 | 选择模式 | 选择模式 | `selection_end = max(锚点位置, selection_start)`；锚点不能越过左锚点 |
| 点选区外文本区（短按） | 选择模式 | 普通编辑 | 退出选择，`cursor=点按位置`，`handle=1` |
| 点选区内文本 | 选择模式 | 选择模式 | 不退出，仅重定位菜单（若菜单已显示） |
| 点「复制」 | 选择模式 | 普通编辑 | 文本写入剪贴板；退出选择，`cursor=selection_end`，`handle=0` |
| 点「剪切」 | 选择模式 | 普通编辑 | 写入剪贴板 + 删除选区文字；退出选择，`cursor=selection_start`，`handle=0` |
| 点「粘贴」 | 任意 | 普通编辑 | 有选区则替换选区；无选区则在 cursor 处插入；退出选择 |
| 点「全选」 | 任意 | 选择模式 | `selection_start=0, selection_end=text.len()`，菜单保持 |
| 点发送钮 / Enter | 选择模式 | 普通编辑 | 先退出选择（不剪切），再执行 submit |
| Esc / 点终端区 | 选择模式 | 普通编辑 | 直接退出选择 |
| 打字（IME commit / 字符键） | 选择模式 | 普通编辑 | 先删除选区，再插入新字符 |
| 退格 | 选择模式 | 普通编辑 | 删除整个选区 |

## 视觉规格

### 选中高亮
- 底色：`theme.bar.select_bg`（默认品牌蓝 `#0066FF` α=0.35，先取 term 侧 `SELECT_BG` 迁移）。
- 范围：从 `selection_start` 对应字左边界到 `selection_end` 对应字左边界。
- 高度：与文字行高同高（`LINE_STEP_PX`）。
- 圆角：4px（与光标柄同族）。

### 双锚点手柄
- 形状：与蓝色定位柄同款「上尖三角+正方承载」，但尺寸缩小到 28×28（选择锚点比光标柄小）。
- 颜色：品牌蓝 `SELECT_BG`（与定位柄一致，保持视觉同族）。
- 位置：
  - 左锚点：尖朝上，底边贴选区首字底边，水平中心对齐首字左边界。
  - 右锚点：尖朝上，底边贴选区末字底边，水平中心对齐末字右边界。
- 热区：以锚点为中心 48×48，便于单指拖动。

### 操作菜单
- 触发：进入选择模式后 150ms 淡入（硬切也可以，看实现复杂度）。
- 位置：优先显示在选区上方；若选区上方空间不足（如贴键盘），则显示在选区下方。
- 形态：水平圆角条，含四个文字按钮：「全选 | 复制 | 剪切 | 粘贴」。
- 未启用项灰显：剪贴板为空时「粘贴」灰显；无选区时「复制/剪切」灰显。
- 颜色：背景 `theme.bar.menu_bg`（近黑半透明），文字 `theme.bar.menu_text`，禁用 `theme.bar.menu_disabled`。

## 与系统剪贴板交互

### Android 侧
- Java：`ClipboardManager` + `ClipData.newPlainText("kfm-na", text)`。
- JNI 两入口（加到 `android/java/dev/kfm/na/MainActivity.java`）：
  - `nativeClipboardSet(String text)`：复制/剪切时调用。
  - `nativeClipboardGet()`：粘贴时调用，返回当前纯文本或空串。
- Rust 侧：`android_app.rs` 接到 JNI 回调后，走 `InputBarState::paste(text)` 插入或替换。

### 纯文本契约
- 只读/写 `ClipData.Item` 的 `getText()`；遇到 HTML/URI/intent 时忽略，视为空。
- 粘贴最大长度限制：先不设，未来若发现系统剪贴板里有几 MB 文本导致卡，再加 4096 字符闸。

## 触摸手势仲裁

输入栏文本区同一套触摸流里已经存在：
- 短按 → 定位光标
- 长按 → 进入选择
- 拖动 ≥slop → 滚动视口
- 锚点拖动 → 调整选区

仲裁规则（按优先级）：
1. **Down 时命中锚点热区** → 进入锚点拖动模式，即使长按也不判滚动。
2. **Down 在文本区，Moved ≥ TAP_SLOP 且未达长按时间** → 滚动视口（保持现有行为）。
3. **Down 在文本区，停留 ≥ SELECT_LONG_PRESS_MS 且 Moved < TAP_SLOP** → 进入选择模式。
4. **Down 后快速抬手（< SELECT_LONG_PRESS_MS，Moved < TAP_SLOP）** → 短按定位光标并退出选择。

锚点拖动 vs 文本滚动：锚点拖到 field 上/下边缘 12px 内时，按滚动速度（每秒 2 行）自动滚屏，让选区能跨出视口。

## 状态核接口新增

```rust
impl InputBarState {
    /// 进入选择模式；若已有组合态先 finish
    pub fn enter_selection(&self, anchor: usize);

    /// 设置左锚点（被拖动的左柄）
    pub fn set_selection_start(&self, pos: usize);

    /// 设置右锚点（被拖动的右柄）
    pub fn set_selection_end(&self, pos: usize);

    /// 退出选择模式，cursor 落在 selection_end
    pub fn clear_selection(&self);

    /// 全选
    pub fn select_all(&self);

    /// 返回当前选区文本；无选区返回 None
    pub fn selected_text(&self) -> Option<String>;

    /// 删除选区文字；返回是否发生了删除
    pub fn delete_selection(&self) -> bool;

    /// 在 cursor/选区处插入文本；有选区先替换
    pub fn insert_or_replace(&self, s: &str);
}
```

渲染层 `render_inputbar` 新增：
- 若 `selecting` 为真，在对应字上画高亮矩形。
- 若 `selecting` 为真，画双锚点手柄（锚点出窗则不画，不冒充）。
- 菜单由 `android_app.rs` 在触摸事件后决定是否弹出；渲染层只负责选区+锚点，菜单走 Android View 浮层或自绘条带（先选简单方案：自绘条带，避免 Activity 层级污染）。

## bar-inject 新增指令

为了 AI/遥控轨道能测试选择系统，新增：
- `select <start> <end>`：进入选择模式并设置选区。
- `select-all`：全选。
- `copy` / `cut` / `paste`：执行对应动作。
- `unselect`：退出选择模式。

## 判卷与验收

### A 档纯逻辑考题（服务器可跑）
1. `selection_set_start_does_not_cross_end`：左锚点拖动不能越过右锚点。
2. `selection_delete_replaces_text`：删除选区后文字与光标位置正确。
3. `selection_insert_replaces_selection`：有选区时插入先删后插。
4. `display_text_with_selection`：组合态+选区同时存在非法，进入选择时 composing 被 finish。

### B 档渲染考题（需 TermView 字体）
5. `selection_highlight_pixels`：选区文字底有品牌蓝高亮像素。
6. `selection_anchors_blink_independent_of_caret`：锚点稳显，不随光标闪烁。
7. `selection_menu_appears`：进入选择后菜单像素出现。

### C 档装机实拍手检
8. 长按普通文本 → 出现高亮+双锚点+菜单。
9. 拖动左/右锚点 → 选区实时伸缩，不闪退。
10. 复制 → 切换应用能粘贴出相同文字。
11. 剪切 → 原文删除 + 剪贴板有内容。
12. 粘贴 → 在 cursor/选区处正确插入。
13. 全选 → 全部文字高亮。
14. 选择模式下打字 → 选区被替换。

## 风险与已决项

1. **菜单实现路径**：
   - **方案 A（已定）**：自绘菜单条带在 `render_inputbar` 内，与选区同帧，不新增 Android View。
   - 质量闸门：实拍验收必须达到「一看就是专业 App 的选择菜单」；若自绘版看着像半成品，回炉或切方案 B。

2. **长按时间**：**400ms**（Android 系统默认值）。

3. **锚点尺寸**：**28×28 视觉 + 48×48 热区**。

4. **菜单项顺序**：**「全选 | 复制 | 剪切 | 粘贴」**。

## 闭环后迭代（活文档，新账往后追加）

### 2026-09-02 · 状态机+渲染首版落地
- 状态核：`BarSnap/Inner` 新增 `selecting/selection_start/selection_end/sel_anchor`。
- 方法：`enter_selection/set_selection_start/set_selection_end/clear_selection/select_all/selected_text/delete_selection/insert_or_replace`。
- 既有方法改造：`insert_text/backspace/clear/enter/submit/set_cursor` 全部处理选区。
- bar-inject 新增指令：`select <start> <end>` / `select-all` / `unselect`。
- 渲染：`ui/prompt_bar.rs` 画选区高亮 + 双锚点 + 菜单气泡；主题 token 新增
  `select_bg/select_handle/menu_bg/menu_text/menu_disabled/menu_pressed`。
- 考题：input_bar_spec +8 道（选择状态机），termview_spec +1 道（高亮/锚点/菜单像素），
  theme_spec 配方钉更新。
- 缺口：①触摸长按/锚点拖动/菜单命中待实现；②系统剪贴板（复制/剪切/粘贴）待 JNI。

### 2026-09-02 · 触摸仲裁 + 系统剪贴板闭环
- 真指触摸：输入栏长按 400ms 进入选择模式；锚点热区 48×48 可拖动；菜单气泡四键命中。
- 剪贴板 JNI：`clipboard.rs` 新增 `get_clipboard_text`；`copy_and_toast` 复用原有写板能力。
- 菜单动作：`execute_bar_menu` 实现全选/复制/剪切/粘贴；复制/剪切写系统剪贴板 + Toast。
- bar-inject 新增指令：`copy` / `cut` / `paste`。
- 几何单源化：`TermView::bar_selection_geometry` 统一输出锚点尖位置 + 菜单位置，渲染与触摸命中同尺。
- 考题：input_bar_spec 新增 `bar_inject_parse_clipboard指令`。
- 装机判卷：待 `na-shot.sh` 实拍选择态像素 + 复制/粘贴闭环验证。

### 2026-09-02 · 剪贴板 Toast 线程崩溃修复
- 事故：`copy_and_toast` 在 gate 值守线程调 `Toast.makeText`，抛
  `java.lang.NullPointerException: Can't toast on a thread that has not called Looper.prepare()`。
- 根因：Toast 必须在带 Looper 的线程（主线程）调用；gate 线程只有 JavaVM，没有 Looper。
- 处置：移除 `copy_and_toast` 中的 Toast 调用，只保 `setPrimaryClip` 写板。
  后续如需要 Toast 提示，应通过 `Activity.runOnUiThread` 或 `Handler` 移到主线程再调。
- 考题：无新增（平台胶水，实拍判卷）。
- 装机判卷：复制/粘贴闭环 + termux-clipboard-get 读回验证。

### 2026-09-02 · 输入法上下文菜单直送状态核（曲线救国）
- 背景：vivo ROM 锁死系统剪贴板——`setPrimaryClip` 报告成功，但 `getPrimaryClip`
  读回始终 null；Termux `termux-clipboard-set/get` 同样失效。系统剪贴板在当前
  环境整体不可用。
- 方案：`KfmInputConnection` 覆写 `performContextMenuAction`，把输入法工具栏的
  `selectAll/cut/copy/paste` 桥到 `ime_queue`，排干时直调 `InputBarState`——
  复制/剪切/粘贴/全选不再依赖系统剪贴板，而是由 NA 内部状态核直接完成。
- 上报：`[ime-input]` 通道全量记录 `commitText/backspace/composing/context-menu`，
  host 可实时监控用户输入。
- 考题：`ime_queue_spec` 新增 `spec_队列_上下文菜单动作入队`。
- 装机判卷：输入法工具栏点复制/粘贴/全选，输入栏行为正确 + `[ime-input]` 日志可见。

### 2026-09-03 · 用户实拍五 bug 一轮修复（①~⑤号迭代）
- 背景：用户实机反馈五条——①锚点三角与矩形错位 ②锚点拖不动 ③剪切无效
  ④菜单四格空白无文字 ⑤菜单离选区太远。全部经「静态代码审读 + 动态注入
  实拍」双路径复现定罪后动手。
- ①②同源（锚点柄三把尺各走各的）：渲染三角中心 ax+14、矩形中心 ax+4
  （静态错位 10px）；触摸热区中心取柄左缘 tip 点（与视觉中心错位 14/18px，
  指按在看得见的柄上却落热区外——拖不动的根因）。修复：两图元水平中心
  统一锚 ax；`bar_selection_geometry` 锚点语义改为「柄视觉中心」
  (ax, tip+18)，热区与视觉同点。
- ③剪切：IME 工具栏 context-menu 路径（d1aa3d7 已补 JNI 转发）之外，自绘
  菜单的触摸分流也有结构洞——菜单浮出栏带后 DOWN 落进终端路径，菜单格
  点了没反应。修复：BarTouch 新增 `menu` 字段，DOWN 分流时菜单命中优先于
  栏带/终端登记，Ended 臂执行；Moved 只记 slop 不滚文本；长按计时跳过。
- ④菜单文字：`draw_text_centered`（termview，与 draw_items_left 同光栅
  路径，格内水平+垂直居中），四格绘「全选|复制|剪切|粘贴」menu_text 色。
- ⑤菜单位置：位置规则抽单源 `selection_menu_rect`（渲染与几何同尺）——
  弃「选区垂直中心-20」改贴选区首行上方 12px；气泡是浮层不受栏带上缘
  钳制（可浮出栏带盖终端区），只守屏顶 8px；上方放不下翻末行下方，
  再不行贴栏底。
- 副产物钉：旧题 `spec_selection_高亮与锚点稳显` 菜单扫描带写死屏幕坐标，
  菜单位置规则一改即假红——改由几何给扫描带（眼手同尺的活样板）。
- 考题：termview_spec +3 道（`spec_bar046_锚点视觉中心等于热区中心` 双锚点
  像素包围盒 ≡ 几何坐标 ±2px / `spec_bar046_菜单贴选区上方` 像素间距
  12±2px / `spec_bar046_菜单按钮绘文字` 菜单区亮像素 >200）。
- 装机判卷：✅ 已闭环（2026-09-03，bdda22a 装机版 vc1789300015）——自动
  复验脚本前台守望（frames 101→122 确认前台后开跑）：注入拖右锚点
  `sel 0..5 → 0..2`（②过）；点剪切格 `text 10 字 → "三四五六七八九十"、
  selecting=false`（③过）；终版实拍菜单文字/位置与锚点形（①④⑤过）。
  剩余唯一未验证路径：**IME 工具栏剪切**（`[ime-input] context-menu: cut`
  日志判卷，等用户手动测）。

### 2026-09-03 · 排障教训：注入触摸「全哑」三前提（BAR-046 排查实录）
上午注入触摸全不生效，排查半天根因不在代码：**na 挂起态弃窗（BAR-004）后，
依赖 window 的路径全静默空转**；scroll/bar-inject/dump 特意不依赖窗口所以
照常回执——「有回执 ≠ 触摸真生效」。判背景铁证组合：
`foreground=false` + `frames` 计数冻结（na-stats.sh）。另：dump 截图
`ime_bottom` 恒 0（gate.rs `dump_now` 里 `render_inputbar(..., 0, ...)`
写死）——键盘弹起时截图里栏位置与真机触摸几何不一致，注入坐标必须按真
键盘态算。**注入判卷三前提：foreground=true、frames 在涨、键盘态已知。**

### 2026-09-03 · BAR-047 长文全选高亮盖穿栏框
- 现场：用户粘贴知乎长文（含硬换行+长空格段）后全选，蓝色高亮块溢出
  输入栏圆角框——上溢压过框顶边框、近框圆角处横出框外。na-shot 实拍
  定罪（当天第二轮「用户操作→我截图找问题」闭环）。
- 病灶：文字绘制有 per-pixel 垂直裁剪，选区高亮矩形什么裁剪都没有——
  半滚出栏顶的行拿满行高矩形画出栏框。单行文本时天然在框内，长文+
  全选+尾锚才把缺口顶出来（BAR-046 修完选择系统后第一场长文实战）。
- 修复：高亮矩形裁进文本可视区（垂直钳 field_y0..field_y1、水平钳文本
  区右缘），空矩形不画；圆角填充改方角（原生选择矩形即方角）。
- 考题：`spec_bar047_选区高亮不溢出文本区`（未修时溢出像素 6650，修后 0；
  BAR-046 三钉+旧选择钉复跑无惊动）。
- 装机判卷：待注入长文+全选+实拍同景复验。

### 2026-09-03 · BAR-048 选择菜单追隐藏首行爬屏
- 现场：用户长文全选后上下滚动输入栏，菜单跟着「被隐藏的选区首行」往
  页面上方爬——内容越多爬得越高（原话：「我往上滚动输入栏里面的内容，
  结果菜单框它自己往页面上方走了」）。
- 病灶：菜单锚定选区首行 `line_y(row_s)`，全选时首行早滚出栏顶不可见，
  菜单追隐藏位置定位。BAR-046⑤号迭代把菜单改成「贴选区首行上方」时只
  考虑了选区可见的场景，没考虑首行滚出视口。
- 修复：`visible_sel_anchor_y` 钳出选区与视口的交集——菜单锚可见段；
  选区整段滚出视口则菜单不画、`bar_selection_geometry` 同步返回 None
  （看不见的点不得有触摸热区）。渲染与触摸几何同尺同源。
- 复测迭代（同日第二轮）：用户复测「不会移动了，但是会上下抖动」——
  锯齿效应：锚第一个可见行时，逐像素滚动让该行 y 连续移 63px、跨行
  边界锚点跳行又瞬移回 63px。根治：锚 y 钳进视口边界——选区起点在视口
  之上时菜单钉栏顶上方 12px 固定位，滚动全程纹丝不动。
- 考题：`spec_bar048_菜单锚可见选区`（钉栏顶固定位对拍+滚出视口 None 钉）
  + `spec_bar048_菜单滚动钉死不抖动`（同段滚动行程三个位置 menu_y 恒等——
  未修时实测 862 vs 832 锯齿 30px）。BAR-04x 十一钉复跑全绿。
- 装机判卷：待注入长文+全选+滚动+实拍同景复验。

### 2026-09-03 · BAR-049 文本内衬——文字/高亮不贴 field 上下沿
- 现场：用户对照其他输入框实拍指正——「输入的内容其实并不是紧贴着栏的
  上下沿的，而是有一段距离，这样看起来就算是全选也不是很怪」。一语中的：
  这是 BAR-047 高亮「溢出感」的根子之一（高亮钳到 field 边=贴死框线）。
- 取值：kfmv4 `.ai-input` padding 14px CSS × 3（1260 屏 3x DPI）≈ 40 物理。
- 契约：文本视口 = field 上下各收 TEXT_PAD_Y(40) 内衬，`input_bar::text_view_h`
  单源——文字裁剪/高亮钳制/光标与锚点可见性/菜单钉位/点按换算/滚动钳制
  （gate 注入 gh_field_h + 触摸 scroll_by_px）全吃这把尺；field 框界判定
  （触摸命中/锚点边缘自动滚）保持全框不吃内衬。
- 考题：`spec_bar049_文本内衬_高亮不贴框沿`（内衬带零高亮像素+视口内高亮
  健在）；spec_bar048 两钉钉位改文本视口上缘、spec_视口滚动 max_eff 222→
  302 同尺；78 题全绿。
- 装机判卷：待装机实拍（长文全选——高亮与框沿应见明显间距）。

### 2026-09-03 · 菜单气泡放大（用户拍板）
- 原 420×72（≈24dp 高、字号 30）实拍「感觉太小」→ **640×120**（40dp 高、
  每格 160 宽拇指舒适击区）、字号 40（与输入栏正文同号）。
- 尺寸提常量单源：`input_bar::MENU_W/MENU_H/MENU_TEXT_PX`——渲染/触摸几何/
  考题同吃；窄窗（含 600 宽考题夹具）内缩守边 8px 防越屏。
- 考题无新钉（尺寸调整非行为修复），BAR-046/048 钉同步换常量引用，78 题全绿。

### 2026-09-03 · BAR-050 拖拽柄「脖子」凹口
- 现场：用户放大实拍指认——三角与正方形对齐了，但正方形四角全圆，
  上边角与三角底边接触处有明显向内凹的缝隙（na-anchors-zoom.png 可复见）。
- 病灶：三角底边平直 28px，`fill_round_rect` r=8 把方形上边角也圆了——
  角部内收、三角底边两角微凸，一凹一凸接缝豁开。
- 修复：方形上边角改直角（下边角保持圆角）——圆角矩形后再以直角矩形
  平顶 8 行，平直 28px 对接（Android 原生柄剪影）。
- 考题：`spec_bar050_锚点柄接缝无凹口`（顶边内收带 8 行逐行满宽 ≥26px，
  未修实测 22px）；79 题全绿含 BAR-046 中心钉无惊动。
- 装机判卷：待装机实拍。
- 备注：光标定位柄（44px 大方块版）是同族同构画法，若实拍也见凹口，
  同一招平顶可复用（用户本轮未点它，不动）。

### 2026-09-03 · BAR-050 同族余案：光标定位柄接缝
- 用户细察后补认：定位柄不明显是因为它的三角底边（36）窄于方形边长
  （44），圆角内收后两腰凹槽不明显——「不过既然发现了就一起修掉吧」。
- 同一招平顶：圆角矩形后直角矩形覆盖顶 12 行（r=12 内收带），上边角
  直角、下边角保持圆角。
- 考题：`spec_bar050_定位柄接缝无凹口`（方块顶带 12 行逐行满宽 ≥42/44）；
  变异抽检过：拆修复实测 35<42 必红，装回必绿；80 题全绿。

### 2026-09-03 · BAR-051 柄形像素级对位（三角/方块同轴同宽）
- 现场：用户放大实拍再指认两条——①选择锚点三角比下方块偏右约 1px；
  ②定位柄 BAR-050 平顶后，三角底边(37)窄于方块边长(44)，原凹槽位置
  变成凸起，观感更差。
- 病灶：`fill_triangle_up` 旧语义以 cx 为对称轴画**闭区间**（底行
  2*(w/2)+1 奇数宽），与偶数宽 `fill_rect` 方块对缝恒偏半像素，多出的
  那一列在右侧——①的 1px 偏右即此；②是三角/方块宽度本就不匹配，
  圆角时代凹槽被圆角掩饰，平顶后平直边对不齐裸露出来。
- 修复：光栅改**左缘 x0 + 精确宽度**语义（半开区间，底行恰好
  [x0, x0+w)，与 fill_rect 同锚）；两个调用点同迁——锚点三角锚
  (ax-half) 起 28px 与方块同跨；定位柄三角 36→44 与方块同边长。
  光栅全仓仅这两个调用点，改语义无第三方案发。
- 考题：`spec_bar051_锚点三角与方块同轴`（三角最底可见行与方块行
  x 跨度中心恒等，未修差 1px）+ `spec_bar051_定位柄三角与方块同宽同轴`
  （底行宽 ≥36，未修实测 33；中心同轴）；变异抽检过：光栅改回闭区间
  两钉双双必红，装回必绿；BAR-050/046 旧钉无惊动，82 题全绿。
- 装机判卷：待装机实拍（放大看两柄接缝与对轴）。

### 2026-09-03 · BAR-052 肩部钝角圆角（一体图钉光栅）
- 现场：用户三指认——两柄三角/方块交接处生硬，成熟输入法柄（Android
  原生图钉柄）此处是**钝角的圆角**（肩部 fillet），不是平顶直角硬拼接。
- 病灶：BAR-050 平顶方案让承载块在接缝行比三角行宽出 2px(锚点)/3px
  (定位柄)——左缘逐行 |Δx| 硬台阶 + 斜边到立边单像素瞬时转向，正是
  「生硬」的像素级本体。
- 修复：**fill_pin_handle 一体光栅**（termview.rs）取代三角+圆角矩形+
  平顶三图元拼接——逐行解析左缘：斜边（锚点 45°/定位柄 m=21/17）→
  肩部精确切圆弧（与斜边和立边同时相切，圆心 (xl+r, y_v+r(√(1+m²)-1)/m)，
  过渡摊 4~6 行）→ 立边 → 底角标准圆角。右缘按承载块边缘等距镜像
  （BAR-051 同轴纪律继承）。fill_triangle_up 随之退役（全仓仅柄用）。
  外沿/轴/热区全不变：锚点 r_sh=7、定位柄 r_sh=10。
- 考题：`spec_bar052_锚点柄肩部钝角圆角` + `spec_bar052_定位柄肩部钝角
  圆角`（①斜边+肩弧带逐行左缘 |Δx| ≤1/≤2（定位柄斜率固有 1~2 步进，
  旧台阶 3px 分界）；②全宽立边在顶点行之后才抵达——未修时锚点 2px/
  定位柄 3px 台阶实测必红）。旧钉处置：BAR-050 定位柄宽度钉阈值 42→40
  （弧带行宽渐进，凹口硬保证移交平滑钉）、锚点钉 ≥26 原样守底；
  BAR-051 同轴钉/BAR-046 中心钉无惊动。变异抽检：r_sh=0 两钉双红
  （尖角立即回潮被抓）；84 题全绿。
- 判卷边界：底角标准圆角收边（末行固有 r-√(2r-1) 跳变）不在平滑契约
  内——那是 BAR-050 起用户认可的既有圆角矩形剪影。
- 装机判卷：待装机实拍（放大看两柄肩部，应与 Android 原生柄同质感）。

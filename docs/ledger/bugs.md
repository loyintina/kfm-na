# bugs.md — KFM-NA BAR 账本

> 每条 bug 修复登记一行。钉 = `#[test]` 名带 BAR 编号，位置列在右列。
> 纪律见 AGENTS.md 门 2（fix 提交不带钉 = 提交不了）。
>
> 分级：I = 事故级（用户实拍踩到/数据损坏）· L = 逻辑错（测试/审计发现）· V = 观感类

| 编号 | 模块 | 病灶与契约 | 级 | 状态 | 钉位置 |
|------|------|-----------|----|------|--------|
| BAR-001 | termview | 字形竖直居中 → 高矮字母底边错位（里倒歪斜）。契约：同基线字母底边对齐、高字母顶边更高、下伸字母探过基线 | V | 已修 | tests/termview_spec.rs `spec_bar001_基线对齐_同基线字母底边对齐` |
| BAR-002 | termview | 真机 NotoSansCJK-Regular.ttc 在 fontdue 0.9 下光栅全空（能载不能画）→ 只见光标不见字。契约：load_font 必须跳过空光栅字体（探针字符 'M' 无墨即弃） | I | 已修（跳过 → Roboto 兜底，11:30 实拍字已出） | tests/termview_spec.rs `spec_字体_空光栅判不合格` / `spec_字体_真字形判合格` |
| BAR-003 | termview | 真机落到 Roboto（比例字体）→ 定宽格摆比例字形，间距忽近忽远。契约：'i'/'M' 步进宽不等即弃；路径候选全灭落内嵌 DejaVuSansMono | I | 已修（等宽判定 + 内嵌兜底） | tests/termview_spec.rs `spec_字体_等宽判定` / `spec_字体_加载跳过比例字体` / `spec_字体_内嵌字节可直接用` / `spec_字体_候选全灭落内嵌等宽` |
| BAR-004 | android_app | 切后台再进页面消失：Android 退后台销毁 native 表面，壳不弃窗不弃 softbuffer 表面，回前台对着死柄画。契约：suspended 弃窗弃表面，resumed 重建；Term/会话还活着就不重开会话 | I | 已修（B 档生命周期胶水，C 档实拍判卷，无输入输出可出考题） | —（tests:na，平台胶水） |
| BAR-005 | termview | 网格从 (0,0) 画起，边缘字符被屏幕圆角/曲面切半。契约：帧缓冲四周 MARGIN 带内必须是纯背景，墨水全在带内之后 | V | 已修（MARGIN 12px 黑带，不画框） | tests/termview_spec.rs `spec_边距_首格不贴边` |
| BAR-006 | android_app/insets | 软键盘盖住底部内容不上滚。初版走 Ime::Enabled 事件 + 42% 估计——实拍判卷：该事件在本机（OriginOS）从未触发（全日志零条），估计式避让是死代码。正道：JNI 直调 WindowInsets.Type.ime() 拿真实高度，500ms 节流轮询驱动 resize | V | 已修（JNI insets.rs，C 档实拍判卷） | —（tests:na，平台胶水） |
| BAR-007 | termview | TUI 转动点（盲文 U+2800 块）画方框：字体选择按 Unicode 段（needs_cjk）路由，盲文 < U+2E80 被分给主字体 DejaVuSansMono——它没有盲文字形。契约：按字形覆盖挑（lookup_glyph_index），主缺备用有才换；双缺记 tofu 目击名单上报 | V | 已修（覆盖路由 + tofu census） | tests/termview_spec.rs `spec_cjk_按覆盖挑选` / `spec_渲染_tofu目击名单` |
| BAR-008 | android/java | 中文输入 Java 皮第一刀把内容 View 替换成自定义 SurfaceView → 原生层绑到不可见 surface：全黑屏、触摸/键盘全哑，只有切后台间隙能瞥见真终端一行。NativeContentView 与窗口 surface（takeSurface）的回调时序是原生渲染命脉，动不得。契约：原生渲染路径一行不动，IME 用 1px 焦点占位 View（KfmImeView）正交叠加——input queue 被 NativeActivity 整窗接管，焦点给谁只决定 IMM 用谁的 InputConnection | I | 已修（占位叠加，B 档平台胶水 C 档实拍判卷） | —（tests:na，平台胶水） |
| BAR-009 | android/java | BAR-008 修完点击不弹软键盘：占位 View 持了焦点，但 View.onCheckIsTextEditor() 默认 false——InputMethodManager.showSoftInput 检查输入目标不是文本编辑器就拒绝弹键盘（此前无 View 持焦点时 IMM 无 served view 反而照弹）。契约：IME 占位 View 必须覆写 onCheckIsTextEditor → true（SDL DummyEdit 同款）；Java 侧链路探针（nativeImeLog）上报 IMM 询问/焦点变化 | I | 已修（覆写 + 探针，B 档平台胶水 C 档实拍判卷） | —（tests:na，平台胶水） |
| BAR-010 | termview | 圆角屏吃掉首行首字符（首行第一个字符完全看不到）。契约：顶边距 MARGIN_TOP = MARGIN_Y + CELL_H（再下探一整行），底/左右维持 MARGIN；顶带内纯背景 | V | 已修 | tests/termview_spec.rs `spec_边距_首格不贴边` |
| BAR-011 | android/java/scripts | e44fdaa 零日志闪退（连 android_main 进入都没有）：e44fdaa 与上一版 versionCode 相同（16777473），同版本号覆盖安装可能不重解压 .so——「新 dex 调 nativeImeLog + 旧 so 没符号」→ UnsatisfiedLinkError 在启动期炸死。契约：①versionCode 每次打包必须递增（package-apk.sh 注释红线）；②Java 侧任何 JNI 调用不许裸奔——imeLog/commitText/sendKey 统一防护入口，Throwable 全吞，探针/输入哑火绝不等于闪退 | I | 已修（versionCode 递增 + JNI 防护，B 档平台胶水 C 档实拍判卷） | —（tests:na，平台胶水） |
| BAR-012 | android/java/insets | IME 三杂症（73eefc8 实拍）：①进场没点任何东西键盘自弹——占位 View 是文本编辑器且持焦点，IMM 进场自动弹；②什么内容都输不进——Gboard 对英文也开组词（自动纠错），字母走 setComposingText 攒词不 commit，setComposingText 是 no-op 则终端永远不见字；③关掉键盘再点召唤不出——winit set_ime_allowed 走 SHOW_IMPLICIT，用户显式收键盘后 IMM 按策略拒弹。契约：①MainActivity 设 SOFT_INPUT_STATE_HIDDEN，键盘只能触摸主动召唤；②inputType = TYPE_CLASS_TEXT \| TYPE_TEXT_VARIATION_VISIBLE_PASSWORD（Termux 同款），禁英文组词逐字 commit，中文拼音组词不受影响；③触摸召唤走 JNI showSoftInput SHOW_FORCED（insets::force_show_keyboard） | I | ①已修（自弹止住，实拍确认）；③未愈反加重为「完全不弹」。一轮埋点实锤：触摸派发正常、无卡死、IMM showSoftInput=Some(false) 拒弹 → 无 served view（焦点丢失/IMM 拒认）嫌疑。二轮：onWindowFocusChanged 重请求焦点 + 强弹目标换焦点 View 本身 + 每次触摸报「焦点类名/isActive/强弹结果」三数 | —（tests:na，平台胶水） |
| BAR-013 | scripts/insets/manifest | 476ed14 实拍「完全不弹键盘」+ [java] 探针全体沉默实锤：设备 .so 不随覆盖安装重解压——dex 是新的（STATE_HIDDEN 压住自弹）但 .so 是旧的（触摸处理里没有 force_show_keyboard、探针符号缺失被静默吞）。BAR-011 的 versionCode 递增判决下错了，止不住。契约：①manifest extractNativeLibs=false + 打包 .so STORED 不压缩 + zipalign -p 页对齐——.so 直从 APK mmap，与 dex 原子同版本，错配链连根拔；②构建戳 option_env!(KFM_NA_BUILD) 编进 android_main 首行上报，设备跑哪个构建日志首行一读便知；③force_show_keyboard 首调结果上报（None=JNI 失败 / Some(false)=IMM 拒弹） | I | 已修（B 档平台胶水 C 档实拍判卷） | —（tests:na，平台胶水） |
| BAR-014 | android/java/ime_bridge | BAR-012③ 二轮实拍：键盘能弹了（焦点修复生效），但任何内容输不进——且 [java] 探针（onCreate 后 3s 必发的「IME 占位已叠加」）自 BAR-009 加入以来历轮全灭。实锤：Java→Rust 方向 JNI 从未绑上，UnsatisfiedLinkError 被 BAR-011 的 try/catch 静默吞掉。病灶：libkfm_na.so 由框架 NativeActivity 加载、挂在框架 classloader 名下，应用类 KfmImeView 懒解析时在自己 classloader 的 native 库清单里找不到符号。契约：①KfmImeView 静态块 System.loadLibrary("kfm_na")——对已加载库幂等，副作用是登记进应用 classloader 库清单；②ime_bridge 三 extern fn 入口第一行原子计数（任何可能失败的调用之前），事件循环心跳捎带读数——全 0 = 绑定仍死，>0 而 pushed=0 = 死在字符串转换，方向一锤定音 | I | 待实拍判卷（16777480） | —（tests:na，平台胶水） |
| BAR-015 | termview | ls 输出文件夹名后挂方框（2026-08-14 实拍，目击名单实锤 U+0009）：alacritty put_tab 把 '\t' 本体写进格（为选中/复制还原 tab），渲染层照单全收，设备主字体（DroidSansMono）无 tab 字形 → 方框。host 的 DejaVuSansMono 有 tab 空白字形，像素层面咬不住——契约钉在纯函数 paintable（A 档）：空格与控制符（C0/C1/DEL）不上屏，渲染层必须经它过滤 | V | 已修（A 档考题 spec_渲染_tab控制符不落墨不进目击名单，变异抽检咬人） | tests/termview_spec.rs |
| BAR-016 | termview/android_app | 触摸滚动首轮实拍两杂症：①shell 里拖动不是滚屏而是「从下到上一行行消失」——滚进历史后 alacritty 给的行号是负的（Line(-offset)），render_into 一句 line < 0 跳过 + 像素行用绝对行号，历史行不画、内容不随偏移移动；②tmux/kimicode 里完全滚不动——alt screen 没有本地 scrollback，本就该把滚屏翻成滚轮事件发 PTY。契约：①屏行 = 网格行 + display_offset（B 档：滚 3 行顶行必须出墨，变异抽检咬人）；②对端开鼠标上报（?1000/1002/1003 任一）时滚屏翻成 SGR 1006 滚轮序列（看历史=64，看最新=65，坐标 1-based）发 PTY，每次事件按行数发 tick 封顶 10 | V | 已修（待实拍判卷，16777483） | tests/termview_spec.rs `spec_滚动_历史行必须画上屏` / `spec_滚动_鼠标上报模式识别`；tests/scroll_spec.rs `spec_滚轮序列_sgr编码` |
| BAR-017 | android/java/termview | 快捷键行（Java View 版）装好却看不见（16777484 实拍）：行其实建成且布局了（inset 上报 315px 实锤），但终端 busy-loop 每帧往窗口 surface 重绘，Java View 画在同一面上被原生帧整帧盖掉——View 覆盖层这条路在 busy 原生渲染下是死路（文件树/光球面板同此墙）。判词：覆盖层 UI 统一改 Rust 自绘。契约：①keybar.rs 布局/命中/修饰键粘滞全 A 档考题；②termview.render_keybar 画在帧缓冲底部 HEIGHT_PX 带（修饰键高亮、标签居中、缺字记目击不画方框）；③修饰键状态收归 Rust（keybar 静态位掩码），nativeCommitText 落字前 take 一次性联动，Java 退回纯 IME 占位 | I | 已修（待实拍判卷，16777485） | tests/keybar_spec.rs（4 题）+ tests/keymap_spec.rs（9 题） |
| BAR-018 | android_app/keybar | 键盘弹起时快捷键行全体没响应（16777488 实拍）：抬手探针全灭——Touch Started 的 in_bar 判定只减 HEIGHT_PX 没减键盘 inset，键盘弹起时行带浮在 inset 上方，判定却认被键盘盖住的屏底 240px（tap 全落进 keyboard 区）。契约：起点判定与渲染/hit 同一把尺，统一走 keybar::in_bar(y, win_h, ime_bottom)——行带 = 屏底 - inset - 行高 | V | 已修（实拍判卷过，16777489：键盘弹起点按钮全链路生效） | tests/keybar_spec.rs `spec_bar018_起点判定_跟随键盘上浮`（变异抽检咬人） |
| BAR-019 | scripts/deploy-phone | 送包到手机后安装器没弹（2026-08-16 连接 provider 版部署实拍）：`PHONE_PICKUP=~/w/项目/kfm-na` 的 `~` 在本地 shell 展开成 `/root` 再送进 ssh 命令串，手机上 `mkdir -p /root/...` 只读失败，set -e 把第 3 步调安装器一起带走。契约：跨 ssh 用的手机侧路径必须写绝对路径（`/data/data/com.termux/files/home/...`），不许依赖 `~` 的环境展开 | I | 已修（脚本改绝对路径；部署链路 C 档实拍判卷） | —（scripts，无 A 档面） |
| BAR-020 | termview | 启动越来越慢（2026-08-18 用户实拍体感）：boot 里程碑实测「表面建成→TermView 建成」5.9~6.7s——主字体扫描把 NotoSansCJK.ttc(32MB) 与 DroidSansFallbackBBK(44MB) 全量解析后被 usable/mono 探针扔掉，每次启动白付 76MB 解析。病灶是探针修复（BAR-002/003）的副作用：每个修复多养一个「解析完再扔」的巨物。契约：主字体扫描超 MAX_MAIN_FONT_BYTES(8MB) 连读都不读；等宽 Latin 主字体不可能是几十 MB 巨物（CJK 备用表不受闸） | I | 已修（体积闸 + DroidSansMono 提首——真机实证胜者） | tests/termview_spec.rs `spec_字体_体积闸跳过巨物` |
| BAR-021 | termview/build/fonts | BAR-020 体积闸后启动仍 5~6s（16777496 实拍对账）：真正大头是诊断脚手架——候选体检+目录普查每个冷启动全量解析 BBK 44MB×2 + Monster 44MB + NotoSansCJK 32MB；探测链本身也是「解析完再扔」的遗产。终端的正确姿势（Termux 同款）：字体内嵌、启动零探测。契约：①生产字体编译期内嵌（build.rs 选择：local/ 商业覆盖 > 开源占位），启动不碰 /system/fonts；②诊断脚手架拆除；③商业字体永不进库（gitignore + chain 第 1 步闸）；④窄字符居中（烘焙管线 lsb=0 病灶：freetype 系贴左）墨迹中心 = 步进中心 ±15%；⑤CJK 全角步进 = 半角两倍 | I | 已修（内嵌直载 + 烘焙管线 + 双闸；待实拍判卷） | tests/termview_spec.rs `spec_bar021_*`（4 题） |
| BAR-022 | fonts/build.rs/termview | BAR-021 实拍（16777498）两杂症：①启动仍 4.6s——report 双通道时间戳受冲洗节拍量化，段落归因靠猜；②U+25BD(▽) tofu 目击刷屏——子集化只留 GB2312，把终端命根符号（盲文转动点/方块/几何）裁没了，且商业美术字体天然无这些字形，BAR-007 盲文回潮风险。契约：①子集 = GB2312 + 终端符号补丁表（箭头/框线/方块/几何/盲文/powerline）；②CJK fallback 恒定 = 缝合像素子集（终端符号补丁包），不再默认落 local/main.ttf；③启动里程碑把「距 android_main 毫秒数」烘进消息文本，绕开通道量化。归因实锤（16777500 实拍）：EventLoop::build 耗时 0ms，末段窃贼 = 进门第一格 report_sync 同步直报（冷隧道 connect 顶满 2s 重试堵 3.3s；热隧道 1.5s——首次/后续启动 3s/5s 之差的真身）→ 第一格改异步入队，report_sync 只留死亡报告/心跳 | I | 已修（待实拍判卷） | tests/termview_spec.rs `spec_bar022_内嵌cjk字体_终端符号补丁覆盖` |
| BAR-023 | conn/termview/android_app | 冷启动黑屏至提示符 ~2.4s 归因定案（16777505/506/507 三次冷启动实拍）：冷进程「第一条连接」恒吃 ~2.1s 一次性唤醒成本——预演 ws 2147/2162ms vs 真握手 178/285ms（第二条连接即热）；std 阻塞握手与 tokio 同 2.15s（与运行时无关，字节真·晚到应用 socket）；同刻 Termux 探针 0.16-0.4s 全健康（隧道/nginx/服务器不背锅）；TCP connect 0-1ms、请求 0ms 写出、响应整包 2.15s 后一次到齐。内核层根因未查明（疑屏幕亮起→无线电唤醒，Termux 无 netlink/dumpsys 权限），应用侧杀不掉。契约（观感修复）：①会话 spawn 提前到基座就绪即刻，与建终端/字体加载并行；②接通中渲染居中提示行「正在接通服务器…」，首个输出 feed 自动熄灭；③握手四段耗时（TCP/写请求/首字节/头读完）异步入遥测。**实拍判卷（16777509，2026-08-20）**：提示行字号收敛后大小正常，但用户判观感尴尬，拍板取消——提示行整体切除（渲染分支/trait 方法/考题全撤），保留①连接前移与③遥测；黑屏期问题的结构性答案移交 L1（本地秒开、远程后台接） | I | 已修（实拍判卷：提示行取消，①③保留） | —（考题随提示行同撤，BAR-023 契约仅剩①③，行为面由 L1 接管） |
| BAR-024 | android/MainActivity | exec 探针第二轮（16777514）：targetSdk 28 域降级放行 exec ✅，但系统按「旧应用」把窗口压到状态栏下面（终端不再满屏）——edge-to-edge 强制是 targetSdk 35 的行为门控，降级即失效。契约：运行时调用不受 targetSdk 门控，MainActivity.onCreate 强制 `decorFitsSystemWindows(false)` + cutout SHORT_EDGES，内容铺回状态栏下，几何归 Rust MARGIN_TOP 管 | I | 已验证（16777515 实拍：满屏回归正常） | —（Java 壳纯平台胶水，无 host 可跑面，实拍判卷） |
| BAR-025 | termview | 长按选择高亮后选区里的 CJK 只剩左半（2026-08-21 实拍）：单遍渲染时宽字符格 0 画双宽字形、墨探进格 1，随后 spacer 格的 SELECT_BG 背景填充把右半字形盖掉。契约：render_into 两遍制——先全部背景（含选择高亮）后全部字形，任何背景都不许盖字形墨 | V | 已修（待实拍判卷） | tests/select_spec.rs `spec_bar025_选择_高亮_cjk双宽字两格完整`（变异抽检咬人：spacer 盖墨即红） |
| BAR-026 | termview | ⇄(U+21C4) 墨溢进下一格（2026-08-21 实拍）：模糊宽度字符 unicode-width 判 1 格，主字体（商业像素字体）缺它落 CJK 备用，而 FusionPixel 里是全角字形，单格路径无格宽裁剪。契约：单格渲染路径右缘按 1 格宽 clip，双宽（WIDE_CHAR）路径保持 2 格 | V | 已修（待实拍判卷） | tests/termview_spec.rs `spec_bar026_渲染_单格cjk字形按格宽裁剪`（变异抽检咬人：摘 clip 即红） |
| BAR-027 | scripts/font-bake.py/assets/fonts | zsh+oh-my-zsh（agnoster 主题）落地前哨：agnoster 用到的 (U+E0A0)、✘(2718)、⚡(26A1) 与 robbyrussell 的 ➜(279C)、✓/✗(2713/2717) 等不在 GB2312，补丁表不扩就是 tofu。契约：①补丁表扩 2600-26FF/2700-27BF/powerline 下沿至 E0A0；②FusionPixel 缺的 7 个符号（⚡✓✗✘✚➜➦）从 DejaVuSansMono 借形补位（font-bake.py --borrow：upm 缩放+半角格居中，与 monoify 同律）；③借形必须同时登记 vmtx（竖排 metrics 与 hmtx 同表类，漏登记保存即 KeyError——调试实录） | I | 已修（烘焙判卷：补丁表缺口=无；待装机实拍） | tests/termview_spec.rs `spec_bar022_内嵌cjk字体_终端符号补丁覆盖`（代表字符扩至 13 个） |
| BAR-028 | scripts/font-bake.py/termview | agnoster 上机实拍（截图 191959）：蓝色目录段两端的 powerline 箭头渲染成「方括号」——FusionPixel 的 E0A0-E0D4 是全角设计（步进 1200、墨迹 1100×1100 方块），终端按 unicode-width=1 走单格路径，右半被 BAR-026 的格宽裁剪切掉。另揪出借字形格宽写死 500 的暗错（此字体半格实为 600）。契约：①烘焙把 powerline 区间横压进半格、纵向保持满行高、墨迹左右贴齐格缘（powerline 要和相邻段无缝拼接）；②格宽一律从字体现测量（half_cell），不许写死 | V | 已修（烘焙判卷 36 字形压格；待装机实拍判卷） | tests/termview_spec.rs `spec_bar028_powerline字形_单格步进` |
| BAR-029 | android_app/sshd | na 退后台被 Android 冻结，沙箱 sshd 冬眠：TCP 握手由内核 backlog 完成但 banner 发不出，症状 = `Connection timed out during banner exchange`（8024 闸门通车后实拍）。2026-08-23 实证闭环：`am start` 遥控前后台 + 8024 探针，后台 10s 必冻、拉回前台 3s 复活。治本：KfmKeepAliveService 前台服务（Termux 同款：常驻通知 IMPORTANCE_MIN + partial wake lock），进程重要性升格，冻结器不动 | I | 已验证（bbcb5f5 装机实拍：修复前后台 10s 必冻 → 修复后 600s 六十探零断流，test-bg-survival.sh 自动判卷） | —（tests:na，平台胶水；实拍判卷脚本 test-bg-survival.sh） |
| BAR-030 | termview | 长行不换行：一行内容过长时不自动折行，行首被标志隐藏（横向滚动式），且长行粘贴进 na 会被折行撕碎（2026-08-23 实拍）。怀疑 readline 横向滚动或 DECAWM 未开 | V | 销案（2026-08-24 用户实拍不复现：长命令正常换行。四层检查全正常——DECAWM host 探针 24 字符进 10 格折 3 行、活体 pty winsize 62×61、readline horizontal-scroll-mode off、IME commitText 原样透传；疑为 zsh 半装坏残局期环境产物，再现再开） | — |
| BAR-031 | scripts/kfm-pkg | 安装分段非原子：overlay 安装中途被杀会留残局（zsh 案实拍：libcap.so.2 符号链接未建 → zsh 链接失败、命令全卡死）。契约三件套：①中断标记——铺文件前挂 `var/kfm-pkg/<名>.partial`，任何 kfm-pkg 调用见残留即吼自愈命令；②重装自愈——全流程幂等重放，重跑 install 必愈（不需要单独修复命令）；③装后校验——payload 文件齐 + 符号链接全通才摘标记，缺一件非零退出（静默残局不可能） | I | 已验证（host 考题三案绿；2026-08-24 装机实拍：新版落位、假标记 list 吼出正确；顺手把 installed 历史重复行去重 110→70） | scripts/test-kfm-pkg.sh（chain 第 8 步） |
| BAR-032 | scripts/font-bake.py/assets/fonts | agnoster 箭头变「C」根因定案（截图 201724 + PIL/fontdue 双光栅器复现）：FusionPixel 上游的 E0B0 根本不是箭头，是「色块+C 形镂空」的装饰设计（E0B2 同理带内纹），BAR-028 的压格只治宽度治不了芯。契约：烘焙时 E0B0/E0B2 直接换成合成实心阶梯三角（步长=upm/12=1px，像素气质一致；纵向沿用原墨区满行高、横向满半格贴缘；三角实心、顶/底行收腰）——像素级考题钉死 | V | 已修（烘焙判卷+像素考题绿；待装机实拍判卷） | tests/termview_spec.rs `spec_bar032_powerline箭头_实心阶梯三角` |
| BAR-033 | gate/android_app | 飞行记录仪首轮实证（2026-08-25）：回放末屏比读屏少开局横幅 6 行——HELP_BANNER 只 feed 视图不进 PTY，泵的 rec 见证回调天然看不见它，而它是屏幕事实。契约：任何上屏内容都必须入带，视图直喂处在 feed 旁同步 tap 一条 rec_output（按当时活跃名）——「回放末屏 = 读屏」不许有常驻豁免项 | V | 已修（待装机判卷：回放与读屏逐行一致） | —（tests:na，壳层一次性 tap 无纯函数缝；端到端判卷 = na-replay 对 na-text） |
| BAR-034 | gate | 飞行记录仪跨重启追加：旧记录带（上一世进程）在带顶，新开机横幅接在其后，回放把旧时间线输出堆到新屏头顶,「回放末屏=读屏」判卷永远差一截（BAR-033 修复版装机终验实拍）。契约：记录带 = 当前进程的屏幕真相——冷启动开机轮换,旧带整体改名 flight-rec.prev.bin 保全（坠机现场不丢,只保最近一世）,新带写魔数起新时间线 | V | 已修（待装机终判卷） | tests/rec_spec.rs `spec_rec_开机轮换_旧带保全_新带起线` |
| BAR-035 | bin/na-replay | 终验实拍:回放与读屏恒差 1 行折行——回放器起手几何 120×40 与真机 80×24 不同胚,开局横幅在 resize 记录到达前落笔,折行点由起手宽度定,resize 重排与直接落笔不是一回事。契约:起手几何抽共享常量 BOOT_COLS×BOOT_ROWS(termview),真机 build_vendored 与 na-replay 同源——结构性消灭复制漂移 | V | 已修(装机终判卷:回放=读屏逐行完全一致) | tests/termview_spec.rs `spec_bar035_内嵌终端_起手几何钉` |
| BAR-036 | gate/android_app | 看门狗首装即误报(2026-08-26 装机实拍):退后台 5 分钟 na-ping 报 stall beat_age=355s——Activity 挂起态 about_to_wait 合法停跳(闸门值守线程存在的理由就是这个),判决没考虑这个状态。契约:看门狗判决必须前台门控——resumed/suspended 喂 APP_FOREGROUND,watch_verdict 四态(Background 休假/NoBeat/Alive/Stall),挂起不判、进出 Stall 才写迁移档(Background 迁移记 SUSPEND 销案) | V |  已修(2026-08-26 装机双态判卷过:前台 alive beat_age=0ms / 退后台 background 休假,均正确) | tests/gate_spec.rs `spec_bar036_看门狗_前台门控四态` |
| BAR-037 | android_app | panic.log 首战立功(2026-08-26 实拍):suspend 后事件循环未退出(exiting/run_app 返回均无记录,进程被 ROM 冻结在 exit(0) 之前——BAR-029 保活服务抬高了存活率,放大了这个窗口),am start 重开 → android_main 二次重跑 → 心跳线程重复起(双序列并存实锤)→ EventLoop::new RecreationAttempt panic ×2,新活动即生即死,App 再也无法到前台。契约:android_main 极早期(冲洗/钩子之后、任何线程 spawn 与 EventLoop::build 之前)静态 AtomicBool ANDROID_MAIN_RAN 重跑检测——二次进门即 report_sync 遗言「重跑让位」+ exit(0),系统另起全新进程;与 restart-req 通道互为犄角(冻结进程拉回即重跑,防御让位后 na-restart.sh 补拉一次) | V | 已修(2026-08-26 装机实证:force-stop 拉回 + restart-req 全闭环各一轮,boot 戳 cee7cf5/loader-pick hot/ping 应答全对,panic.log 无新行——旧案两行即全部) | —(tests:na,壳层防御无纯函数缝;考场 = 装机反复重启,实证已过) |
| BAR-038 | android_app | BAR-037 防御首发日实拍连环案(2026-08-26,trace 尾行铁证):冻结进程拉回 → 重跑让位正常触发 → 但主线程直接 exit(0) 跑 TLS 析构 → jni 0.22 `sys_detach_current_thread` 断言 `guard_level==0` 炸(left:2——冻结现场里首圈 android_main 的 JVM 附着从未归还,二次进门再叠一层),panic.log 两行噪声。契约:重跑让位的 exit 必须换无线程史的新线程执行(TLS 干净,断言无从触发),主线程 join 原地等死;restart-req 路径天然免疫(值守线程退,装机实证干净)。判卷顺带确认:BAR-037 防御「重跑让位」真场景首发命中 | V | 已修(2026-08-26 装机判卷过:两轮「HOME 冻结 15s → am start 拉回」,重跑让位两次命中、新进程 0.5s 内接续,panic.log java_vm 行零新增) | —(tests:na,线程/TLS 壳层无纯函数缝;装机实证已过) |
| BAR-040 | android_app/termview | 开局横幅两行被顶出视野（2026-08-27 用户实拍：点进页面横幅首两行在屏外，要上滑才看全，「仿佛页面生成后被下滚两行」）。飞行记录仪铁证：横幅 ts=0ms 在 BOOT 80 列落笔，ts=2ms 首个 resize 到真机 61 列，重排折行 +2，标题行「── kfm-na 就绪 ──」与次行前半被顶进 scrollback。契约：横幅必须在首个真实几何 resize（apply_window_size）之后再印；HELP_BANNER 移居 termview（折行点由 BOOT_COLS 决定，与几何常量同文件同审；且 android_app 是 android feature 门控，host 考题够不着——盲补的一课） | V | 已验证（2026-08-27 装机同尺复验过：热更 e56a2c2 重启后 na-text 读屏首行=「── kfm-na 就绪 ──」，横幅完整贴顶，无需上滑） | tests/termview_spec.rs `spec_bar040_开局横幅_*`（2 题） |
| BAR-039 | termview/android_app/gate | textarea 长文后台注入实拍两张皮（2026-08-31 自验轨实拍定罪：挂起态注入 55 字长文，dump 画出 3 行文本但栏带仍是单行高——第三行溢出文本区下缘）。根因：渲染带高读 `snap.lines`，而它是 UI 线程 poll 量宽后的转写写回，后台挂起无 poll = stale；同一帧里文本按实测折 3 行、带高按 stale 画 1 行。契约：渲染几何不许读经手转写的读数——render_inputbar 自带量宽折行（画文本本就要量，一次量两头吃），带高从实测行数派生；rasterize/dump 的 keybar inset 同步改实测（term.bar_text_lines），前后台同一把尺；snap.lines 只服务触摸命中（前台 poll 写回，眼手同尺） | V | 已修（host 回归钉绿；待装机同尺复验：挂起态注入长文，带高=3 行、文本收在文本区内、keybar 随栏上浮） | tests/termview_spec.rs `spec_bar039_render_inputbar_带高从文本实测` |
| BAR-041 | ui/prompt_bar | 输入栏三场景闪退（2026-09-01 用户实机：快打/滚动/点按定位均闪退）。panic.log 定罪：`prompt_bar.rs:246 slice index starts at 21 but ends at 18`——行归属边界分支把「折行行数 n」错当「字符数」用，多行文本光标位 > n 即被判到末行，切片倒挂 panic。契约：行归属抽纯函数 row_of(starts, idx)=最后一个起点≤idx 的行（idx=文末自动归末行，无需第二判据），光标与组合下划线共用；渲染切片不再有第二判据可错 | V | 已修（host 回归钉 2 题绿：row_of 纯函数钉 + 中段光标渲染不 panic 钉；装机复验待热更后三场景重测） | tests/termview_spec.rs `spec_bar041_row_of_行归属` + `spec_bar041_多行中段光标渲染不panic` |
| BAR-042 | android_app/ui-prompt_bar | 定位柄三缺陷+真指拖动滚动无效（2026-09-01 用户实机）：①柄锚死文本区底而非光标行底 ②柄随光标闪烁（应稳显）③拖动滚动逻辑误置 Ended 臂——Moved 事件永远到不了，真指拖动退化成点按。契约：柄锚光标所在行行底+稳显；拖动滚动入 Moved 臂（超 TAP_SLOP 24px 判拖，余数挂账折行） | V | 已修（host 考题更新：柄行底锚+稳显断言；拖动 Moved 臂为 android 层 C 档装机实拍待用户复测） | tests/termview_spec.rs `spec_bar_caret_闪烁相位与定位柄`（更新） |
| BAR-043 | input_bar | 尾锚→手动视口交接不播种，首笔滚动瞬移（2026-09-02 装机实锤：尾锚态 scrollpx -200 视口直接弹到文本最顶——raw scroll_px 语义=距头顶偏移，尾锚期间 raw 恒 0，首笔 clamp(0-200)=0=头。用户报「第一下失效/移动比例失真」的真根因，BAR-042 死区修复的同族余案：钳制治了累积，没治交接）。契约：follow→手动交接首笔先播种 raw=max_eff（当前显示位）再叠加钳制——尾锚位续算 1:1 | V | 已修（host 考题 2 题绿：交接播种红→绿 + 连笔累积钳头护卫；装机三场景复测进行中） | tests/termview_spec.rs `spec_bar043_follow转手动首滚_从尾锚续算` + `spec_bar043_尾锚续算_连笔累积_到头钳住` |
| BAR-044 | gate | bar-inject 空读竞态，指令静默吞（2026-09-02 实锤：writer `cat >` 先截断后写，值守线程撞进截断窗口读到空串，applied=0 bad=0 无声无息——白拍一张图才发现）。契约：空/纯空白内容不消费不删文件（bar_should_consume 纯函数判卷），留待下轮读全量；教训：注入通道写入侧用「.tmp+mv 原子发布」更稳，读取侧这道闸兜底 | V | 已修（host 考题 1 题绿：桩红→真绿） | tests/termview_spec.rs `spec_bar044_空内容不消费` |
| BAR-045 | ui/prompt_bar | 切片倒挂多起 SIGABRT 崩溃后秒重启，用户侧肉眼无感（2026-09-02 夜检 panic.log 定罪：`src/ui/prompt_bar.rs:246` 多起 `slice index starts at 21 but ends at 18` 等，光标偏移/组合态下划线/draw_items_left/tap 定位四处 `items[a..b]` 起止倒挂）。病灶：眼手不同源——`bar_cursor_at` 量宽用 `snap.text` 而渲染用 `display_text(snap)`，组合态下 cursor/starts/items 三把尺各量各的。契约：①所有 `items[a..b]` 统一 `.max(a)` 防御性切片，a<=b 永不 panic；②眼手同源——cursor 量宽原料与渲染层共用 display_text 单源。纪律产出：**unseen bug 的第一抓手是 panic/log 不是用户肉眼**——用户说「没问题」时先看仪器判据（2026-09-11 对账发现本条漏登记 bugs.md，补录；全文在 插件档案-输入栏.md 2026-09-02 节） | L | 已修（一钉：极端 cursor/组合态/滚动组合下 render+cursor 双路不 panic；手机 chain 第 24 轮绿） | tests/termview_spec.rs `spec_inputbar_防御性切片不崩溃` |
| BAR-046 | ui/prompt_bar/android_app | 选择系统实拍五 bug（2026-09-03 用户实机）：①锚点三角/矩形错位（渲染两图元中心 ax+14 vs ax+4）②锚点拖不动（热区中心取柄左缘 tip 点，与视觉中心错位 14/18px）③剪切无效（IME 路径 d1aa3d7 已治；自绘菜单浮出栏带后 DOWN 落终端路径，菜单格点了没反应——触摸分流结构洞）④菜单四格空白（MVP 空色块）⑤菜单离选区太远（选区垂直中心-20 算法）。契约：锚点柄视觉/几何/热区三把尺同点（几何返回柄视觉中心 ax,tip+18）；菜单位置单源 selection_menu_rect 贴选区上方 12px、可浮出栏带守屏顶 8px；菜单触摸 DOWN 分流优先登记、Ended 执行；菜单四格居中自绘文字 | V | 已验证（2026-09-03 装机自动复验：前台守望+注入拖右锚点 sel 0..5→0..2、点剪切格 text 10→8 字且 selecting=false、终版实拍菜单文字/位置全过；剩余 IME 工具栏剪切路径待用户手动判卷） | tests/termview_spec.rs `spec_bar046_锚点视觉中心等于热区中心` / `spec_bar046_菜单贴选区上方` / `spec_bar046_菜单按钮绘文字` |
| BAR-047 | ui/prompt_bar | 长文全选高亮盖穿栏框（2026-09-03 用户实拍：粘贴知乎长文后全选，蓝色高亮块溢出输入栏圆角框——上溢压过框顶、近框圆角处横出框外）。病灶：文字绘制有 per-pixel 垂直裁剪（Some((field_y0, field_y1))），选区高亮矩形什么裁剪都没有——半滚出栏顶的行拿满行高（63px）矩形直接画出栏框（用户现场 follow=尾锚，首行正好半滚出）。契约：选区高亮矩形裁进文本可视区——垂直钳 [field_y0, field_y1]、水平钳文本区右缘（text_cx+text_cw），空矩形不画；方角填充（原生选择矩形即方角，圆角半径在裁剪边无意义） | V | 已修（host 考题红→绿：未修时溢出像素 6650；BAR-046 三钉+旧选择钉复跑全绿；待装机同景复验：注入长文+全选+实拍） | tests/termview_spec.rs `spec_bar047_选区高亮不溢出文本区` |
| BAR-048 | ui/prompt_bar | 选择菜单追隐藏首行爬屏+复测锯齿抖动（2026-09-03 用户实机两轮：①长文全选后滚动输入栏，菜单跟着被隐藏的选区首行往页面上方爬；②修完复测「不会移动了但上下抖动」——锚第一个可见行时滚动中行 y 连续移 63px、跨行边界跳回 63px 的锯齿效应）。病灶：菜单锚定 `line_y(row_s)`，首行滚出栏顶不可见仍追其定位；第一版修复锚可见行又没钉死，逐像素滚动下放出锯齿。契约：①菜单只锚看得见的选区（visible_sel_anchor_y 钳选区与视口交集）；②锚 y 钳进视口边界——选区起点在视口之上时菜单钉栏顶上方 12px 固定位，滚动全程不动；③选区整段滚出视口则菜单不画、bar_selection_geometry 同步 None（看不见的点不得有触摸热区） | V | 已修（host 考题 2 题红→绿：锚可见区对拍+None 钉；防抖动钉——同段滚动行程三位 menu_y 恒等；BAR-04x 十一钉复跑全绿；待装机同景复验） | tests/termview_spec.rs `spec_bar048_菜单锚可见选区` + `spec_bar048_菜单滚动钉死不抖动` |
| BAR-049 | ui/prompt_bar/input_bar | 文字/高亮贴死 field 上下沿（2026-09-03 用户对照其他输入框实拍指正：「输入的内容其实并不是紧贴着栏的上下沿的，而是有一段距离，这样看起来就算是全选也不是很怪」）。kfmv4 `.ai-input` padding 14px CSS ≈ 40 物理（1260 屏 3x DPI）；na 文本视口=field 全高，多行时首行贴框顶、高亮顶框线——BAR-047/048 的「溢出感」根子之一。契约：文本视口 = field 上下各收 TEXT_PAD_Y(40) 内衬（input_bar::text_view_h 单源），文字裁剪/高亮钳制/光标与锚点可见性/菜单钉位/点按换算/滚动钳制（含 gate 注入 gh_field_h 与触摸 scroll_by_px）全吃这把尺；field 框界判定（触摸命中/边缘自动滚）不吃内衬保持全框 | V | 已修（host 考题红→绿：内衬带零高亮像素+视口内高亮健在；spec_bar048 两钉钉位改文本视口上缘、spec_视口滚动 max_eff 302 同尺；78 题全绿；待装机实拍） | tests/termview_spec.rs `spec_bar049_文本内衬_高亮不贴框沿` |
| BAR-050 | ui/prompt_bar | 拖拽柄「脖子」凹口（2026-09-03 用户放大实拍指认：三角形与正方形对齐了，但正方形四角全圆，上边角与三角底边接触处有明显向内凹的缝隙）。病灶：三角底边平直 28px，fill_round_rect 半径 8 把方形上边角也圆了——角部内收、三角底边两角微凸，一凹一凸接缝豁开。契约：方形上边角改直角（下边角保持圆角）——先圆角矩形再以直角矩形平顶，三角底边与方形顶边平直对接（Android 原生柄剪影）；同族余案：光标定位柄（36 三角+44×44 r12）同构同病（三角底边窄于边长凹槽不显但接缝仍豁），用户拍板「一起修掉」，同一招平顶 r=12 带 | V | 已修（host 考题红→绿：锚点顶带 8 行满宽 ≥26px（未修 22）+定位柄顶带 12 行满宽 ≥42px（变异抽检实测 35 必红）；80 题全绿含 BAR-046 中心钉无惊动；待装机实拍） | tests/termview_spec.rs `spec_bar050_锚点柄接缝无凹口` + `spec_bar050_定位柄接缝无凹口` |
| BAR-051 | ui/prompt_bar/termview | 柄形像素级对位两条（2026-09-03 用户放大实拍：①选择锚点三角比下方块偏右约 1px；②定位柄 BAR-050 平顶后三角底边(37)窄于方块边长(44)，原凹槽位置变凸起观感更差）。病灶：fill_triangle_up 旧语义以 cx 为对称轴画闭区间（底行 2*(w/2)+1 奇数宽），与偶数宽 fill_rect 方块对缝恒偏半像素、多出的一列恒在右侧（①即此）；②是三角/方块宽度本就不匹配，圆角时代被掩饰，平顶后裸露。契约：光栅改左缘 x0+精确宽度语义（半开区间底行恰好 [x0,x0+w)，与 fill_rect 同锚）；锚点三角锚 (ax-half) 起 28px 与方块同跨、定位柄三角 36→44 与方块同边长；光栅全仓仅此两调用点 | V | 已修（host 考题红→绿：锚点同轴钉未修差 1px、定位柄同宽同轴钉未修实测 33<36；变异抽检——光栅改回闭区间两钉双双必红；BAR-050/046 旧钉无惊动，82 题全绿；2026-09-03 装机实拍+闸门复验：选区两端图钉与选区边缘对轴、无偏右，通过） | tests/termview_spec.rs `spec_bar051_锚点三角与方块同轴` + `spec_bar051_定位柄三角与方块同宽同轴` |
| BAR-052 | ui/prompt_bar/termview | 两柄三角/方块交接生硬（2026-09-03 用户三指认：「成熟输入方案这个位置其实也是一个圆角，只不过是钝角的圆角」）。病灶：BAR-050 平顶方案让承载块在接缝行比三角行宽出 2px(锚点)/3px(定位柄)——左缘逐行 |Δx| 硬台阶 + 斜边到立边单像素瞬时转向。契约：fill_pin_handle 一体图钉光栅取代三图元拼接——斜边（锚点 45°/定位柄 m=21/17）→ 肩部精确切圆弧（与斜边和立边同时相切，过渡摊 4~6 行）→ 立边 → 底角标准圆角；右缘按承载块边缘等距镜像（BAR-051 同轴纪律继承）；fill_triangle_up 退役；外沿/轴/热区全不变，锚点 r_sh=7、定位柄 r_sh=10；底角标准圆角收边不在平滑契约内 | V | 已修（host 考题红→绿：两平滑钉未修实测锚点 2px/定位柄 3px 台阶必红；BAR-050 定位柄宽度钉阈值随契约 42→40（弧带渐进）、锚点钉 ≥26 守底，BAR-051/046 钉无惊动；变异抽检 r_sh=0 两钉双红；84 题全绿；2026-09-03 装机实拍+闸门复验：图钉一体成型、三角/方块接缝无凹槽无凸起，用户此前已认「还可以」，通过） | tests/termview_spec.rs `spec_bar052_锚点柄肩部钝角圆角` + `spec_bar052_定位柄肩部钝角圆角` |
| BAR-053 | ui/prompt_bar/input_bar/android_app | 长按召唤不了选区（2026-09-03 用户实机：「拖拽柄只有全选一个入口，长按无法触发 = 功能用不了」）。病灶链三环：①长按进选择用 enter_selection（空选区 start=end 无高亮不可见）；②长按抬手落 Field 点按分路 → set_cursor 顺手清选（刚召唤即销毁）；③长按后滑指走滚动分路不扩选。契约：①enter_selection_word 落点词整段选中（word_span_at 终端侧同字符集：CJK 句读段/ascii/路径串整词、标点断词、非词字符单选、越界按末词），活动锚=右锚，空文本不点火；②Ended 补 long_fired 早退臂保持选择；③Moved 加词枢轴扩选分路（pivot_drag_span：词恒整选+扩向指头一侧、入词内回词本体），边缘自动滚屏抽单源 bar_edge_autoscroll | V | 已修（host 考题三钉红→绿：词跨度表/枢轴扩选表/状态核落选区非空可见；42 题全绿；2026-09-03 装机实拍+闸门复验：注入 down/sleep700/up → 日志「输入栏长按 → 选词 (0,4)」+实拍「你好世界」整词高亮双钉菜单齐；续 down/sleep/move400/up → 选区词粒度扩至「你好世界 hello」右锚随行，双路径通过。复验插曲：应用挂后台时 window=None 致 in_bar 全落空、注入手势漏进终端分支（误诊终端选区），am start 拉前台后全通——后台态注入分流已记观察项） | tests/input_bar_spec.rs `spec_bar053_长按选词_词跨度` + `spec_bar053_词枢轴扩选` + `spec_bar053_长按选词_落选区非空可见` |

---

## 案卷区（逃逸条款配套，2026-08-27 立，全文见 docs/active/调试闸门.md §十一）

上方账本维持一行一案。**逃逸 bug**（满足任一：看不见现场 / 一修不好 /
复现不了）除一行登记外，必须在此区开六栏案卷；开案脚手架
`bash scripts/na-case.sh BAR-xxx "现象"`（自动收尸 + 生成下面骨架 +
复现脚本模板）。**「盲区」栏是灵魂**——它逼结案者回答「下次同类病
怎么一眼看见」，而不是只记改了哪行。

<!-- 模板（na-case.sh 自动生成同款）：
### BAR-xxx 案卷:<一句话现象>

- **现象**:(用户原话 + 实拍/日志证据)
- **复现注入序列**:(scripts/cases/BAR-xxx-repro.sh;手势类注明"用户手测")
- **盲区**:(当时为什么看不见——对照观测矩阵哪格是空的)
- **长出的新观测点**:(计数器/trace 段/通道/脚本,没有就写"考题代替")
- **考题**:(tests/ 路径 + 名;纯平台胶水写 tests:na 理由)
- **判卷法**:(同一把尺复验的具体命令与通过标准)
-->
| BAR-054 | ui/prompt_bar/ime_bridge/java/na-loader | IME 工具栏「剪切」哑火（2026-09-03 用户实机：全选/复制/粘贴能用，唯独剪切无效）。病灶链三环全录：①KfmInputConnection 未覆写 getSelectedText()，IME 按剪切前探选区恒得空不发事件（一修覆写走 JNI 直问状态核）；②一修后装机复验：IME 剪切=复制+deleteSurroundingText 翻 DEL，删除半生效（bar_text_len=0 实锤）复制半仍断；③受控实验三环探针定案：**IME 来问了（getSelectedText 探询日志到）但我们答了 null——新增 JNI 方法忘了加 na-loader 转发表**（BAR-039：Java 焊死 loadLibrary(na_loader)，loader 逐符号显式转发当前核心，转发表没有 nativeSelectedText = UnsatisfiedLinkError 被 try/catch 静默吞成 null）。契约：loader 新增 forward_to_core_ret 带返回值转发宏 + nativeSelectedText 转发（缺符号兜底 null 不反咬 Java）；**新加 JNI 方法 = Java/核心 ime_bridge/loader 转发表三处齐备**，机械钉执法不再靠装机实拍 | V | 已修（A 档钉 spec_bar054_jni方法三方齐备：解析 KfmImeView.java 全部 static native 逐一查 loader/核心双侧函数定义行——防裸子串骗局（DISABLED 变体骗 contains 实录）与 jni 0.22 生命期泛型形态；变异抽检删转发即红；全量考题绿）。装机续验（09-03 下午）：剪切=问选区(✅2字)+自进系统剪贴板(粘贴✅)+**删除指令零到达**——第三刀全覆写路径（deleteSurroundingText 系/replaceText/setSelection/DEL 键/commitText("")）探针全静默；用户定调「其他 app 剪切正常 = 我们覆盖广度不够」。第四刀纯探针：performContextMenuAction 未识别 id 静默 fall-through 埋针（最可疑漏洞）+ begin/endBatchEdit/commitCorrection/performPrivateCommand/setComposingRegion/performEditorAction 全埋针，待装机抓指令真名。第四刀装机复验（09-03 晚）：全针照旧死寂——真凶现形：**commitText("") 从没被观测过**（Java 侧 length>0 守卫静默吞空串，第三轮「已排除」是误判：空串根本到不了 Rust 日志）。Android 契约：commit 文本替换当前选区，空串=删选区——这正是 IME 剪切删除半的真身，与其他 app 剪切正常完全咬合。第五刀修复：Java 空串也转发（+探针）、队列 push_text→push_commit（空串入队为 Inject::CommitEmpty）、排干侧输入栏分支 CommitEmpty→delete_selection（终端分支吞掉保沿革），A 档钉 spec_bar054_空commit入队为删选区指令（原 spec_队列_空串不注入 被本钉取代——「空串不注入」对终端分支仍成立，由排干侧吞掉兑现）。**09-03 23:06 装机终验通过（用户实拍「真的好了」+日志实锤）：getSelectedText→2字 → commitText 空串探针响 → commit-empty: 删选区=true → paste 2 chars 系统剪贴板命中——IME 剪切全链四环闭合，BAR-054 收官** | tests/loader_spec.rs `spec_bar054_jni方法三方齐备` + crates/na-loader/src/lib.rs forward_to_core_ret/nativeSelectedText + android/.../KfmInputConnection.java getSelectedText+deleteSurroundingText 探针 + src/ime_bridge.rs nativeSelectedText + android_app.rs paste 命中探针 |
| BAR-055 | ui/prompt_bar/android_app | 拖锚点柄「断触」（2026-09-03 用户实机：同一行内上下挪动就断，不跟手）。病灶：拖动连续态沿用点按严格命中尺 bar_field_char_at——指头纵坐标一出文本框（抓柄时指心本就在框下沿外）返回 None → 选区冻结，挪回界内复活 = 断触感。契约：拖动连续态（锚点拖动+枢轴扩选 Moved 臂）专用 clamp_to_field 钳制尺——框内原样、框外按最近边换算；点按/命中判定仍用严格尺禁混用 | V | 已修（host 考题红→绿：clamp_to_field 边界表五言；变异抽检不钳制必红；42 题全绿；待装机实拍） | tests/input_bar_spec.rs `spec_bar055_拖动钳制尺` + src/input_bar.rs clamp_to_field + android_app.rs bar_field_char_at_clamped |
| BAR-056 | ui/prompt_bar/input_bar | 拖柄越过对端锚 → 整个选择框消失（2026-09-03 用户实机：上面那行的柄拖到下面那行柄的下方即消失）。病灶：set_selection_start/end 旧钳制语义——越过对端钳死原地，选区压成零宽不可见。契约：换锚（Android/浏览器标准）——拖过界两锚交换，旧对端定身为新端点、指头改持新锚继续拖，setter 回传持有锚供触摸侧更新 bt.anchor；贴合（pos == 对端）零宽停留不换锚；程序侧双端同设走新增 set_selection_span 原子落（拆两发会被换锚截胡，枢轴扩选臂同步改用）；旧钉 selection_anchors_do_not_cross 契约作废由新钉继承 | V | 已修（host 考题红→绿：换锚四态+贴合+双端原子三言；变异抽检改回钳制必红；42 题全绿；待装机实拍） | tests/input_bar_spec.rs `spec_bar056_拖锚交叉换锚` + `spec_bar056_程序侧双端同设不换锚` |
| BAR-057 | tests/gate_spec | 手机 chain 偶发误红 spec_包装层_fed与分桶字节账（fed=false）。病灶：全局 PUMP 是进程级单例，两个包装层考题并行跑时互相截胡——fed 题的 "LL" 被 control 题的 pump_once 抢先泵进待机 replay，fed 断言偶发失败（文件头注释写了「单测串行使用」但 cargo test 默认并行，注释挡不住调度）。契约：PUMP_LOCK 静态锁机械执法，凡碰全局 PUMP 的考题串行进场 | V | 已修（两考题进场先取锁；服务器连跑三轮全绿；纯考题基建，无生产代码变更） | tests/gate_spec.rs PUMP_LOCK |
| BAR-058 | android_app/生命周期 | 进程活着时 `am start -n dev.kfm.na/.MainActivity` → 日志 `android_main 重跑让位`（有时两行）→ 心跳全灭、新旧进程同归于尽、8024 死（2026-09-03 深夜期 0③ 自验时三连踩，复现 100%）。对照实验：旧核 7352f25 同样复发 = 既存病非期 0③ 回归；冷启动（进程死透后 am start）则正常。疑与 BAR-037 家族（重跑让位）同族，待查。临时纪律（已落排障手册）：am start 仅限进程死透后用，判死 = 8022 侧 `nc -z 127.0.0.1 8024` 不通 + 心跳停 | I | 已登记待修（临时纪律先行，修复时带钉） | 待补 |
| BAR-059 | ai_chat | 思考流混入可见回复（2026-09-04 期 0③ 真机首验闸门实拍：Kimi highspeed 的 reasoning_content → ThinkingDelta 被当成正文拼进 assistant 消息，AI 页一整段英文内心戏混在回复里）。病灶：ai_chat.rs apply 把 TextDelta/ThinkingDelta 合并推送同一 streaming 串——线路层 brain.rs 早已 deltaType 分流，落格层又和回去了。契约：思考与正文分账独存（思考不是回复；kfmv4 渲染成「已思考」折叠块另存，期 0 纯文本消息行不画思考，折叠块是期 0④⑤ 的活）；收流归位——正文空且思考非空 → 思考归位为正文（kfmv4 陷阱 10 / R3，与 brain.rs RunAccumulator 同判据） | L | 已修（A 档两钉；变异双咬实测——思考混回正文旧行为复活→分流钉红、归位删除→归位钉红，各咬各的；7 题全绿；**2026-09-04 装机复验过**：5f3e0f7 核热更后闸门注入同题问答，AI 页实拍只剩一句话干净回复「我是由月之暗面（Moonshot AI）开发的人工智能助手……」，思考流零混入，结案） | tests/ai_chat_spec.rs `spec_bar059_思考分流不进可见回复` + `spec_bar059_正文空思考归位为正文` |
| BAR-060 | scripts/na-push-so | 陈核哨兵对默认拉取路径失效（2026-09-04 凌晨实踩：5f3e0f7 哨兵首秀当晚，默认路径从手机仓拉 9-2 旧核推上机，期 0③ 被静默降级回 d1aa3d7——哨兵判的是拉下来副本的 mtime=下载时刻，恒新恒通过，形同虚设；又靠人肉读 boot 构建戳抓回）。契约：默认路径必须在拉取前拿远端 .so mtime 比 HEAD（远端 stat 失败按 0=必拒）；显式路径仍比本地文件 mtime（=构建时刻，有效） | I | 已修（修后同景复现：默认路径拒推「.so (09-02) 比 HEAD (09-04) 还旧」，显式路径推 5f3e0f7 核通过+boot 戳咬合；部署脚本无单测基建，tests:na） | —（tests:na，部署脚本） |
| BAR-061 | scripts/check | 覆盖矩阵行序 locale 依赖（2026-09-04 夜班实踩：chain 第 10 步在服务器（glibc en_US，sort 首趟忽略标点 → brain_ep.rs 排 brain.rs 前）与手机 Termux（C locale，反之）产出两种行序——同一份代码矩阵翻烧饼，手机仓脏树致 updateInstead 拒推，三仓同步卡壳）。契约：find 产物管道 `LC_ALL=C sort` 钉死字节序，双环境逐字节一致 | L | 已修（修后服务器重出矩阵与手机版逐字节 diff 为空；bash 检查脚本无单测基建，tests:na） | —（tests:na，检查脚本） |
| BAR-062 | termview/ui | blit_panel_shifted 方向写反：src 顶部拷进 dst 底部，面板实际从屏底往上升（用户要的是从上往下落）；考题同谋——spec_blit_移位压盖三态把反方向钉成「上截下补」标准答案，A 档全绿 C 档实看才逮住（考题与答案同源的盲区，方向性语义必须找第三方锚点）。契约：y_off=-k 时 dst [0,h-k) 行 = src 底部 [k,h) 行，面板底边从屏顶落下 | L | 已修（考题改写钉正确方向 spec_bar062_blit_移位压盖三态，反向变异实测咬红） | tests/fx_spring_spec.rs |
| BAR-063 | ui/渲染合成 | 过渡帧快捷键行硬切离场（0a7b649 设计误判）→ 面板落下/升起两端各一次 pop-out/pop-in，用户实看闪烁。病灶：把「层级遮盖」误做成「离场硬切」——快捷键行层级低于 AI 面板，被盖是自然结果，根本不需要拿走。契约：过渡帧照画快捷键行（前后台同规则），观测钉 = 缝占固定 -300 假动画 → dump 倒过渡帧 → 行带内必须有非背景像素 | L | 已修（spec_bar063_dump过渡帧快捷键行照画：修前红修后绿，钉即复现器） | tests/gate_spec.rs |
| BAR-064 | ui/ai_page | AI 页滚动手势方向反（2026-09-04 用户实看：手指上滑翻出更早消息）。病灶：手势臂像素累积符号写反（acc -= dy），「上滑=看更早」逆主流手感；换算逻辑埋在 android_app 触摸臂里无考题（BAR-062 同类：方向性语义没钉）。契约：drag_accum_rows 为方向唯一出处——下滑 dy>0 = 拉内容看更早 = 正行增量；像素余数留账不丢 | V | 已修（换算提纯函数进 ui/ai_page.rs；反向变异实测两钉齐红） | tests/ai_page_scroll_spec.rs `spec_bar064_手势方向_下滑看更早` + `spec_bar064_像素累积_余数不丢` |
| BAR-065 | insets | 输入栏/快捷键行跟键盘开合慢半拍（2026-09-04 用户实看：召唤后延迟才抬，收起延迟才落）。病灶：BAR-006 轮询节流 500ms——间隔就是感知延迟本身。契约：轮询间隔 ≤150ms（100ms 现值，钉上限防回弹；轮询只在事件循环醒着时跑，成本可忽略）；常量家在 insets（android_app cfg(android) 考题够不着） | V | 已修（间隔钉题防回弹） | tests/input_ime_spec.rs `spec_bar065_键盘轮询间隔_钉死上限` |
| BAR-066 | ui/orb + GLES 合成 | 光球背后一整块黑（2026-09-05 用户实看：AI 页光球背景大黑块）。病灶：加法 sprite（饱和加，设计语义=往真背景上加光）被画进独立 chrome 透明画布（dst=纯黑），条件 alpha（BAR-黑屏案「纯黑=空白」）又把暗色增量强转成不透明像素——加法变「替换」，雾成黑块。契约：①orb 增加半透写出件 blit_orb_sprite_alpha——(α,E) 对（α=加量最大通道，E=去预乘满亮色相），GPU 标准混合的加亮项 α·E ≡ add·gain 逐像素守恒，与加法的差异只剩雾尾 (1-α)·dst 轻微压暗；②条件 alpha 扩版 mark_chrome_alpha——RGB 非零且高字节为 0 才强转不透明，自带 α 的像素原样直通。softbuffer 单层路径仍走真背景饱和加（不受此病） | L | 已修（三钉：sprite 守恒与透形 / 条件 alpha 直通 / over 层入口实弹；棘轮 orb 维持 0、fx_ease 下调 0） | tests/ai_presence_spec.rs `spec_bar066_光球半透写出_守恒与透形` + `spec_bar066_条件alpha_自带透明度直通` + `spec_bar066_光球over层入口_实弹契约` |
| BAR-067 | ui/prompt_bar + theme | 输入栏多行带高下成「黑墙」（2026-09-05 用户装机实看：BAR-066 修后光球黑块已消，但栏带塞满内容后仍是大块黑背景）。病灶：kfmv4 栏带原设计 rgba(18,18,26,.85) 半透叠模糊，CPU 画布时代无真 alpha 压平成不透明「事后色」暗板——内容一多带高暴涨，板成墙。契约：①theme 新常量 CHROME_BAND_ALPHA=0xD9，栏带底写 (α<<24)\|bg；②blend_px 装饰混合保留目标高字节（发丝线/发光/veil 叠半透底不掉 α）；③条件 alpha 直通已由 BAR-066 就位。softbuffer 高字节被忽略 = 退化不透明（兜底可接受） | L | 已修（两钉：栏带 α+发丝线保 α；变异抽检摘 keep 位实咬红） | tests/termview_spec.rs `spec_bar067_栏带底_半透写出` |
| BAR-068 | ui/orb + GLES 合成 | 光球下缘拖出纯黑半圆盖住输入栏（2026-09-06 用户实拍键盘打开态：球悬在快捷键行上，球下方黑盘精确卡在快捷键行底线，盖住栏带与内芯一角）。病灶：BAR-066 的 (α,E) 半透写出是**整像素替换**——雾尾微小 α（1%雾+99%背后）盖掉栏带 85% 底，而栏带背后是网格空行（GPU 清屏黑）→ 纯黑盘；快捷键行上方正常（键帽不透明，99%键帽透出）= 黑盘精确卡在行底线的铁证。契约：blit_orb_sprite_alpha 改**加量叠加**——读底有效 α（条件 alpha 三态：自带 α/无 α 但 RGB 非零=255/纯零=透明），底已带加量 α_d·E_d + sprite 加量求和再去预乘；不透明底退化为饱和加法（与 blit_orb_sprite 逐像素等价），半透带正确积雾 | L | 已修（两钉：半透带贡献不丢（同款舍入期望）/不透明底退化饱和加法） | tests/ai_presence_spec.rs `spec_bar068_光球叠加_半透带贡献不丢` + `spec_bar068_光球叠加_不透明底退化饱和加法` |
| BAR-069 | termview/带切环 | AI 框上下两条 14px 粗边（上青下紫，2026-09-06 用户装机实看；带切优化 0e6f1b8 引入）。病灶：带切环的发光行扫到矩形外（ly<0/≥fh）时渐变墨的 ly 被钳 0——框外发光带被误判成框缘本身，整条刷上渐变 C1/C2（宽=spread=14），同钳还把框外真辉光的 SDF 算没。契约：发光 SDF 用真实 ly（可为负）；渐变墨只在矩形行内生效。软指令：带切类优化的回归钉 = 「框外不得有满 α 渐变墨」 | L | 已修（spec_冒烟_ai页框外_无渐变厚边；变异抽检钳回实咬红） | tests/ai_presence_spec.rs `spec_冒烟_ai页框外_无渐变厚边` |
| BAR-070 | android_app/图层槽 | 光球+输入栏（+放大镜）集体隐身（2026-09-07 图层槽位合成器 8fd907b 热更后用户实看）。病灶：draw_frame_gles 槽位化只设了键行/面板两槽的 set_slot_visible，上层槽（Over）漏设——ChromeLayer 默认 visible=false 恒不上屏，而烘焙/画布全正常（na-shot 走值守 CPU 独立路径照常有球有栏，GLES 真话只在 GPU 合成里——取证盲区教训：槽位病 na-shot 判不了）。契约：三槽可见性单源 slot_visibility（ui/stage.rs），上层槽恒可见 | L | 已修（spec_bar070_上层槽恒可见） | tests/stage_spec.rs `spec_bar070_上层槽恒可见` |
| BAR-071 | 观测/crash | 尸检仪全字段偏 8 字节：SIGSEGV 的 "PC/LR" 双双落线程栈（非代码页）、"x0" 恒等于 si_addr——按书本值 uc_mcontext@168 解读处处自相矛盾（2026-09-09 面板闪退案追凶时发现，前两案结论因此全部作废）。病灶：本机内核在 sigcontext 前多垫 8 字节，真首址 176；旧 "PC@432" 实为 sp，SIGURG 探针当时给出的是反证（落 [stack] 不可执行页）被误读成背书。实证链：①"x0"≡si_addr=fault_address；②按 176 重读 fp/sp 落栈区、lr 落 libc++.so 代码段（nm 定位 to_wstring 族），区域类型全对；③探针反证。契约：UCTX_SC_OFF=176 为实测值，钉死防"按手册改回" | L | 已修（一钉：首址实测值） | tests/selfwatch_spec.rs `spec_bar071_sigcontext首址_实测值176` |
| BAR-072 | gles_present/vsync | 面板闪退真凶（2026-09-09：点光球必闪退，后台注入 70+ 次不崩的海森堡假象）：vsync_cb 回调签名凭空多画首位 *const AChoreographer 形参——NDK 契约 AChoreographer_frameCallback64 只有 (frameTimeNanos, data) 两参，回调里读到的 "c" 实为帧时间戳，自续挂表拿它当 this 回 postFrameCallback64 → libgui 对 时间戳+0x58 做 mutex::lock → SIGSEGV。铁证：三案故障地址减 0x58 后低 48 位全部 ≈64h 开机时长纳秒数；field-reports 史上零 vsync 样本（旧代码 ts 实收 data=null→0，账本永远空转）互证。海森堡得解：后台不渲染→动画不起→vsync 不武装，病灶没被喂到（方法论钉入排障手册：注入测试必须先验 foreground=true）。契约：①ChoreoCallback 类型钉死两参签名（回退即 android 编核编译红）；②回调回 post 的 this 现场 getInstance 重取，不许缓存/伪造；③记账剥进核心层 vsync_book（host 可判卷），首跳只建基线、ns=0 不建基线、回拨 saturating_sub；④getInstance 判空（无 ALooper 线程返回空） | V | 已修（三钉：首跳建基线/60Hz三跳咬合/取账清零；变异抽检摘守卫两题齐红；顺带修考题基建 reset_for_test 的 min 账底未归位 u64::MAX——fetch_min 语义下 0 恒压真值） | tests/vsync_spec.rs `spec_bar072_记账_首跳建基线` + `spec_bar072_记账_60hz三跳_取账清零`；ABI 钉=src/gles_present.rs ChoreoCallback |
| BAR-073 | scripts/na-push-so | 热更默认拉取路径写死交叉编译目录（2026-09-09 BAR-072 热更时实踩：手机原生编核落 target/release/，脚本 stat/cat 的都是 target/aarch64-linux-android/release/ 里 09-09 12:59 的残留旧核——陈核哨兵正确拒推立功，但若不修，ALLOW_STALE=1 强推或哪天真对齐了就静默推旧）。契约：双候选路径取较新者（原生/交叉），哨兵比对与拉取同源同一份挑选结果；两路径皆无 = 当场拒 | I | 已修（修后同景复跑：正确挑中 14:48 原生核推机成功；部署脚本无单测基建，tests:na） | —（tests:na，部署脚本） |
| BAR-074 | bootstrap/壳 | 幂等闸位置错（PIN-boot 挂卷族真凶）：壳 first_boot_install 每启把 32MB bootstrap 资产全读进内存才调 ensure_prefix——而后者第一行就因「环境已装」跳过，32MB 纯白读压在启动关键路径上；模块文档原契约即「zip 字节都不解析」，壳的闸在读之后才问 = 契约没兑现。**归因修正（同日埋点实锤）**：本修复摘掉白读属实，但它不是 PIN-boot 挂卷族真凶——热更后 boot 段仍 3.2s，五层埋点夹出主线程在插件装完后凭空消失 2171ms（锁/资产/装帧逐项排除）= 系统 freezer 中段冻结（熄屏远程重启的环境产物），真凶归 BAR-075。白读该摘仍摘：启动路径不该为「可能不装」读 32MB。契约：幂等闸谓词 prefix_ready 单源在核心层，壳必须先问它再决定读不读资产；ensure_prefix 内部同闸双保险 | V | 已修（一钉：谓词三态；变异抽检谓词改反两题齐红） | tests/bootstrap_spec.rs `spec_bar074_幂等闸谓词_三态` |
| BAR-075 | scripts/cases/PIN-boot | 熄屏冻结误报机检缺失（2026-09-09 埋点确诊）：灭屏 am start 的 boot 被系统 freezer 中段冻结 2~3.5s（主线程插件装完 +68ms 后凭空消失 2171ms，install_sender 锁/资产读/插件装载逐项排除；亮屏期恒绿灭屏期恒红，field-reports 双簇分布互证）——环境产物被当码病判挂。契约：boot 段内 STALL beat_age≥1000ms = 冻结证物 → 跳过（77）不判，与「环被顶出→跳过」同哲学；元契约②补三样本（冻结触发/小 stall 不触发/旧 boot STALL 不污染新 boot 窗口复位） | L | 已修（元契约三样本钉进 test-na-regress-meta.sh；灭屏态实跑由挂转跳过） | scripts/test-na-regress-meta.sh 元契约②（TF/TF2/TF3 样本） |
| BAR-076 | gles_present/观测 | P3 渲染源采样奇偶轮播成体验税（2026-09-09 用户实看「面板落下卡」：实测采样轮 7帧 均值61ms fps16，纯节奏轮 14帧 均值5.8ms fps172——readPixels 回读停顿压垮动画，每两轮必卡一轮）。病灶：动画开表奇偶武装采样，仪器噪音摊到用户每次操作上。契约：采样改点播制——anim-cap-req 触发文件在动画开表时消费才武装，不投 = 零 readPixels 开销；点播口 scripts/na-anim-cap.sh，回收通道 [anim-strip] 报表+anim-strip-png.py 不变 | L | 已修（一钉：触发消费两态；实机对照：点播轮 采样2帧 61ms，未点播三轮 采样0 全 60Hz 满帧） | tests/gate_spec.rs `spec_bar076_采样点播_触发消费两态` |
| BAR-077 | ui/fx_spring+壳 | 面板落下「拖影/不连续的影子」真凶（2026-09-10 用户实看+四路径合诊）：帧时钟写死距上帧 ≥16ms=60fps 硬钳，本机屏 120Hz（vsync 账本实测 110-120Hz、周期 8ms——自观测仪器首战定罪）——屏幕每刷两次画面才动一次，350ms 落下只画 19-21 帧 vs 系统应用 42 帧，肉眼即「拖影」。证据链：panel-anim 报表 19帧/swap均16ms/vsync 110Hz 同框互咬；渲染均耗 3-6ms 远低于 8.3ms 预算（管线跑得起 120fps，是节拍器不让）；录屏期 vsync 掉 60Hz 时钳制与刷新咬合反证。契约：帧预算=显示真实刷新周期——壳每次 resumed 经 JNI `Display.getMode().getRefreshRate()` 喂核心层（读数 24~480Hz 外=离谱不进系统），核心层默认 16ms 保守基线+钳 4~33ms 防病态；旧契「帧率上限 60fps」废止 | V | 已修（一钉：预算可配+钳制三态；变异抽检预算写回 16 硬编码实咬红——2008 臂必红） | tests/fx_spring_spec.rs `spec_bar077_帧预算_跟随刷新率可配` |
| BAR-078 | 工具链/d8 | 手机打包断档真凶（2026-09-10 BAR-077 装机时实踩）：旧 d8.jar（R8 8.2.2-dev）读 javac 21 产物的 MethodParameters 无名条目必 NPE（"Cannot invoke String.length() because parameter1 is null"，崩溃原点 KfmRecService$1.class）——JDK-8292275 起 javac 21 对内部类 this$0 mandated 参数恒发 MethodParameters 属性且 name=null，旧 d8 吃不动。最小复现：任意匿名类 javac21 编译喂旧 d8 必崩；--release 8 / -g:none / -XD 系全救不回；服务器 javac 17 产物无此属性故服务器打包一直绿（09-08 起「服务器打包」实为绕道而不自知——手机打包自 na-rec 落地即断档）。契约：手机 d8 wrapper 换核 R8 8.5.27（Google 官方 maven r8-8.5.27.jar，实测吃下 javac21 全量 class）；旧 jar 留档 ~/kfm-na-toolchain/d8.jar 备查；排障链防=手机打包断档先查 d8 核版本 | I | 已修（换核后 javac21 全量 class→dex 绿；装机验证随 BAR-077 同包） | —（tests:na，工具链换核） |
| BAR-079 | ai_presence/ui | 被覆盖面板再召唤不播入场动画（2026-09-10 动画监控首日实战实踩）：配置页在顶、AI 被覆盖时再点球——栈字段翻转正确（top=config→ai）但帧差分 +1、anim-cap 触发未消费 = 缝没开表，面板瞬移盖顶。病灶：§五B 契约内部矛盾——坍缩②要「再召唤=重播入场」，但目标值语义「在栈（含被覆盖）=靠泊位」使被覆盖者缝目标恒在终位，再召唤时目标 0→0 不变，缓动采样器无从启动；状态级判卷（栈字段+截图）前一天全绿，帧级仪器才看见。契约：目标值从「位置」升级为「（位置, 入场代）」——状态核对被覆盖再召唤/被挤出 bump 入场代（新鲜召唤/退场/露出不 bump），缝占槽件加 replay 重播踢原语（重定基 from=屏外位、目标不动，插值仍在缝内），壳层 poll_ai_presence 机械中继零契约判定，stats 加 ai_epoch/cfg_epoch 让「重播发生了」机器轨直读 | V | 已修（五钉：覆盖再召唤 bump/幂等退场露出不 bump/tap_orb 路径 bump/重播踢从屏外起播/目标即屏外静默瞬移+未首采忽略；变异四咬：去 was_covered bump/退场也 bump/踢不重置 start_ms/踢不置 settled=false——最后一枚首测漏网，补「踢后采样前活性探针即 true」断言后咬死） | tests/ai_presence_spec.rs `spec_bar079_*`（3 题）+ tests/fx_ease_spec.rs `spec_bar079_*`（3 题） |
| BAR-080 | session/壳 | 自动重孵死亡循环烧 2.5 核（2026-09-11 redroid 云安卓首日实踩）：云安卓 local 会话「Opened 即 Failed」瞬死（aarch64 bootstrap 在 x86_64 链接即败），旧防风暴阀「每剧集只自动重孵一次」（SessHealth.retried）被瞬死击穿——每次 Opened 清牌 = 新剧集新第一次，死亡↔重孵每帧一轮（实测定案：7h 帧数 718118 ≈ 死亡 717320，docker 实烧 251% CPU）。契约：自动重孵改纯时间闸——首次立即放行，其后距上次重孵 ≥MIN_AUTO_RESPAWN_MS（5s）才再放行；手动触发（敲键/切入死会话）不过闸但也记账（闸量真实重孵密度）；时钟回拨压住不透支。retried 字段退役 | L | 已修（四钉：首次放行/间隔内压住/到点放行/回拨压住；变异双咬：>= 改 > 咬边界题、None 改 false 咬首放题） | tests/session_spec.rs `spec_首次自动重孵立即放行` / `spec_间隔内压住` / `spec_到点放行` / `spec_时钟回拨压住不炸` |
| BAR-081 | ui/fx_spring + vsync_book + gles_present | 面板动画残影「同位置几帧→跳变」真凶（2026-09-11 用户录屏逐帧实锤，power2.out 曲线换装后仍在 = 曲线非根因）：帧时钟是 4ms 轮询+预算节流的定时器（fx_frame_due 时间闸），与 8.6ms vsync（实测 116Hz）两个时钟互不锁相——panel-anim 报表定罪：渲染均耗 3ms（管线跑得起 120fps）、vsync 周期 8ms，但 swap 间隔 min/avg/max=1/9/25ms（有的周期塞两帧一帧白画=同位置，有的周期零帧=25ms 冻结）。对照：kfmv4 在浏览器走 rAF=Choreographer 驱动，一跳一帧相位锁定，同款动画无残影。契约：①动画期（vsync 挂表窗口内）fx_frame_due 改吃 vsync_book due 账——Choreographer 每跳 vsync_cb 记一笔 due，一跳产一帧、消费清零、连跳合并产一帧（跳帧合并不是丢失：位置采样吃最新时刻）；②看门狗 32ms：挂表后超阈无跳（从未跳/链半路断）= 链死上闩退预算节流节奏防动画卡死，新跳到自动解闩重锁（参考点=最近一跳与最近一帧的较晚者）；③未挂表=旧预算路照旧（Choreographer 缺席平台向后兼容）；④vsync_arm 的 getInstance 空指针不再落武装（链根本起不来就别挂表）；⑤due 账清账归 reset_run/reset_for_test。产线零新唤醒源——due 消费仍走 4ms 轮询圈，最坏延迟 4ms < 8.6ms vsync 周期 | V | 已修（两钉：锁相一跳一帧（无跳不产/一跳一帧/合并跳一帧/收表回预算路）+看门狗防卡死（两种死法+链复活重锁）；变异三咬：take_due 只读不清咬两题、看门狗摘除咬看门狗题、锁相支路摘除两题齐咬；fx_spring 补 reset_frame_clock_for_test 清钟（跨题钟面污染实踩）） | tests/vsync_spec.rs `spec_bar081_锁相_一跳一帧` + `spec_bar081_锁相_看门狗防卡死` |
| BAR-082 | vsync_book + ui/fx_spring | 栈叠收回「无动画直接消失」（2026-09-11 用户实拍：AI 盖配置再收 AI、配置拖拽推回松手——退出动画肉眼看作瞬移；panel-anim 实锤：2 帧 / swap 均 88ms / vsync 样本 1，对照健康入场 15 帧锁相 60Hz）。真凶 = BAR-081 看门狗零基线漏洞：产线动画开局是脏帧直画（tap→poll→dirty，不经 fx_frame_due）→ LAST_FRAME=0；武装后 Choreographer 首跳丢失/迟到 → 参考点 last_due×LAST_FRAME 皆 0 → `ref==0` 被当信任期永久等跳 = 动画冻结，迟到跳到达时缝已超时贴死 = 瞬移。旧版之所以多数场景幸存：首跳通常 8ms 内到，锁相直接生效；首跳丢失只在链路冷启动（resume 后首轮/快速连播）现身。契约：看门狗参考点升三元组 = max(最近一跳, 最近一帧, **本轮武装戳**)——vsync_book.reset_run(now_ms) 落 ARM_MS（动画开表=arm 同刻），零跳零帧也以开表时刻为基线，33ms 后看门狗照样上闩落预算节流，迟到首跳到达 note_due 解闩重锁；`ref==0` 保留为「从未开表」真冷态 | V | 已修（一钉：零基线首跳丢失看门狗——信任期不产/33ms 放帧/放帧后预算节流续走/迟到首跳重锁；变异抽检：arm_ms 从参考点摘除，5033 臂当场红） | tests/vsync_spec.rs `spec_bar082_锁相_零基线首跳丢失看门狗` |
| BAR-083 | ui/stage + android_app + gate + gles_present | 栈叠收回「无动画直接消失」二周目（2026-09-11 用户 BAR-082 复验实锤：先 AI 后配置叠加撤配置再撤 AI 正常；**反过来先配置后 AI，撤 AI 没动画**——BAR-082 看门狗修的是真洞（零基线首跳丢失）但不是这个场景的真凶，上轮自验只数帧数没看像素 = 判卷维度疏漏）。真凶 = z 序「逐帧跟栈顶」：撤 AI 瞬 AI 出栈 → top=配置 → 不透明配置页当场压在 AI 面板槽之上，AI 退出动画在它背后播完 = 用户见瞬消；反向撤配置可见是因拖拽期配置仍在栈顶跟手。契约：z 序规则升「**动者在上**」单源 stage::panel_z_cfg_on_top——AI 缝活跃→AI 在上；配置缝活跃或拖拽锁定中→配置在上；双活跃栈顶 tie-break；双静止跟栈顶（露出零动画承载不变）。安全论证：动画结束时动者必在端点（靠泊=栈顶本身/屏外=不可见），z 回落栈顶帧零像素跳变。三调用点（GLES/softbuffer/dump）同单源；dump 线程摸不到 panel_drag 句柄，拖拽锁定期 z 滞后一拍缝动画（亚秒级，判卷可容） | V | 已修（一钉：动者在上六言（撤AI压顶/撤配置压顶/入场同向/双活跃tie/双静止跟栈）；变异抽检活跃优先级改反实咬红；**09-11 午后装机自验闭环**：抽帧 PNG 两 run 独立实锤退场紫板压在青配置页之上滑出（run12/13 紫带在顶、青页在下渐露）；干净局（无点播 readPixels 税）退场 12 帧 vsync 59.7Hz 锁相 swap 均 15ms 全绿——此前 2帧/5帧 病况纯系点播仪器税（BAR-076 已知 61ms/帧），非产线病；判卷=用户 C 档） | tests/stage_spec.rs `spec_bar083_z序_动者在上` |
| BAR-084 | ui/stage + android_app | 栈空但配置页视觉满屏停住（2026-09-11 redroid 三公民实测实锤：stats 栈顶 none、双截图 diff=0 静止靠泊——状态与画面两张皮）。病灶：BAR-083「动者在上」把 z 序的活性读数（缝动画/拖拽锁定中）泄漏进 presence→target 回路——退场中的面板被算成 present → 缝 target 翻回靠泊 → 采样器掉头回粘，已出栈的面板永远停在靠泊位；与 BAR-083 修的洞同构但反向（那是动画在背后播完看不见，这是动画播不完死不离场）。契约：target/draw 单源 stage::panel_target_and_draw——**target 只问栈**（在栈=靠泊 0 / 不在栈=屏外带符号），**draw 才看活性**（在栈‖活性，退场动画必须画完）；z 序活性读数只许进 z 序，不许进 target/presence。随三公民面板栈落地同提交修复（feat 内根治，非独立 fix） | V | 已修（一钉：四象限+符号忠实（回归案例 (false,true)→(屏外,画) 咬死回粘回路；draw 删活性→退场瞬消红）；变异抽检 target 改 `in_stack‖active` 实咬红） | tests/stage_spec.rs `spec_bar084_target只问栈_draw才看活性` |
| BAR-085 | android_app/softbuffer 兜底 | softbuffer 兜底路径网格整个不画（2026-09-11 终端卡片壳改造时实踩：8fd907b 槽位化把 paint_under 里 render_into 调用连同 gpu_term 形参一起误删，GLES 路径网格走实例管线照常，兜底路径只剩键行+面板——GLES 恒在，无人看见）。病灶：删除点无考题（兜底路径的网格绘制没有钉），重构顺手带走。契约：paint_under 必须先 render_into 画网格再 render_keybar（顺序钉：键行压网格下沿）；顺带根修同案连带的 paint_page_frame_ring u32 下溢（小缓冲/inset 场景 buf_w-MARGIN 减法 panic，termview.rs:351——i64 算术化，所有调用方共享，paint_term_card_chrome 的防御钳制随之退役）。随终端卡片壳落地同提交修复（feat 内根治，非独立 fix） | V | 已修（兜底路径恢复 render_into；i64 根修后 spec_缩放_set_cell_size重算字几何 的 8x8 小缓冲冒烟由 panic 转绿；壳冒烟钉④小缓冲/巨 inset 不 panic 直钉） | tests/termview_spec.rs `spec_终端卡片壳_涂装冒烟` ④ + `spec_缩放_set_cell_size重算字几何`（8x8 冒烟臂） |
| BAR-086 | glyph_atlas/gles_present | 捏合缩放格变字不变、缩小字堆叠（2026-09-11 晚用户实机实拍，redroid GLES 真帧复现钉死：捏合放大后一格一个大空位、字形还是基准字号缩在中央）。病灶：GlyphKey 的字号维是冻结类代号（GLYPH_SIZE_TERM=0）无 px 维，图集无失效路径——捏合改了 cell size 与 font_px，图集照样拿旧字号位图贴新格；CPU 路径逐帧光栅天然正确，所以 na-shot（CPU 重画）判不了此病，只有 GLES 合成帧看得见（BAR-070 同类取证盲区）。契约：①GlyphAtlas::clear 整册清空（slots 灭/页收编/行架归零/coverage 清零/revision bump）；②GlesPresent::sync_term_glyph_size 每帧绘制前对账——格尺寸变即清册逼 misses 路径按新字号重光栅（绘制链单点挂钩，捏合/缩放读回/未来一切 set_cell_size 路径自动全覆盖）；③捏合钳制上限放宽 45x90→108x216（6 倍档，用户对标本 Termux 夸张档；列数个位数是可用性闸不动） | V | 已修（一钉：spec_atlas_清空_槽位归零行架重排（含 revision/coverage/原点重装五言）；redroid GLES 真帧修前复现修后复验） | tests/glyph_atlas_spec.rs `spec_atlas_清空_槽位归零行架重排` |
| BAR-087 | termview（paint_cursor_arc） | 功能光标角区三条线肉眼错位（2026-09-12 深夜用户截图判卷「三条线明显错位」；像素实测四角外凸：左凸 4px 至 x=57、上凸 1px 至 y=54、下凸过 y1——agent 前两轮判卷只测直线带与角弧外观，没测角区带外零墨 = 判卷维度疏漏，用户截图一测即中）。病灶：圆角弧带以框缘为中心对半跨边（cov 居中 dist=R），直线段却画在框内缘贴边（发丝线行带 [y0,y0+3)、左线列带 [x0,x0+9)）——两套参照系，弧与直线在角上必然错位。契约：弧带外缘贴框缘向内铺（dist=R 满覆盖、dist>R 零墨、内缘 0.5px 羽化），与直线段同源同尺不跨框 | V | 已修（一钉：⑤b 弧带不跨框四言——上角上溢/上角左溢/下角下溢/下角左溢带外 1px 零墨；变异抽检：改回居中弧带实咬红） | tests/termview_spec.rs `spec_cfg标签栏_涂装钉` ⑤b |
| BAR-088 | termview（draw_items_left_inset） | 字段标签行每个文字少末字（2026-09-14 用户实机反馈；redroid 倒帧实证：弹簧→弹/缓动→缓/手势仲裁→手势仲/视口平移→视口平/现役→现/本地终端→本地终/Ctrl+]→Ctrl+，条条精确少一字）。病灶：右缘判停 `pen_x + adv >= clip_right` 把「恰好满宽」当装不下误杀——字段行盒宽由实测文宽算出（内宽 = ceil(步进和)），像素字体（CJK）步进为整数、和 == 内宽，`>=` 必咬末字；host DejaVu 步进非整数永远打不中此边界 = 考题全绿行为全错的漏网口（与 CJK 无字形夹具教训同族，十四修字段行四钉无一命中此病）。契约：恰好满宽 = 装得下（`>` 才截断），真装不下仍截断 | V | 已修（一钉：恰好满宽末字落墨+真超宽截断对照双言，手搓整数步进 items 复现边界；变异抽检：判停回 >= 实咬红） | tests/termview_spec.rs `spec_bar088_恰好满宽_末字落墨钉`（经 `spec_draw_items_left` 考题专用薄壳，同 pub text_width 单源先例） |
| BAR-089 | termview（paint_cfg_pool_content_impl）+ android_app（配置页帧泵） | 下池选中三级框内文字消失 + 下拉收起留一行黑余影（2026-09-14 晚用户实机反馈四条之二；redroid 实录帧复核实锤：光标滑行帧选中「控件」行框内无字）。病灶两枚：①十五修把选中全包框挪到行循环后单独画、文字却留在行循环里——涂装序「行字→选中框」，选中框内芯是不透明渐变暗底，选中行文字被框芯盖没（三级框钉只测框缘/芯像素、没测框内文字墨 = 判卷维度又漏一格，BAR-087 角区同族）；②配置页帧泵闸只吃 curr 活性——活性翻 false 那圈不再脏帧，屏幕定格在收敛前最后一帧（progress≈0.02 的一行黑面板余影），泵停无人擦除（redroid 实录因触摸注入多产帧恰好擦干净 = 考场漏网，真机稳态必现）。契约：①选中框/细框是光标不是蒙版——**先框后字**（下池行/下拉选项同规）；②**收敛补终帧**：泵闸 = prev‖curr（ui-base §四 已修宪） | V | 已修（三钉：下池选中行文字落墨钉/下拉细框滑行相位钉（含芯上文字落墨）/两段时序时值三钉（cfg_page_spec）；变异三咬全中：Ⅰ段不冻结/细框瞬移回潮/选中框盖字回潮） | tests/termview_spec.rs `spec_cfg下池_选中行文字落墨钉` `spec_cfg下拉_选中细框滑行涂装钉` + tests/cfg_page_spec.rs `dropdown_pick_two_phase_sel_slide_then_panel_close` |
| BAR-090 | cfg_page（dropdown_panel_rect） | 下拉面板宽 = 触发器宽，长选项名被裁掉末字（2026-09-14 十六修实录判卷新发现：redroid 注入 3 选项 servers.json 复判 BAR-089，f013 帧实证「阿里云主站」显示成「阿里云主」、「备用二号机」成「备用二号」——面板右侧直接裁切；f014 点选后触发器换 5 字文、面板随触发器加宽，全文恢复 = 实锤宽源是触发器不是选项）。病灶：`dropdown_panel_rect` 返回 `w: t.w`（触发器矩形宽），选项文宽从不参与——触发器文比选项短时选项必裁（与 BAR-088 恰好满宽同族症状、不同病灶：那边是判停符号，这边是几何宽源）。契约（2026-09-14 用户拍板修宪 theme.md §六）：**下拉面板宽 = max(触发器宽, 选项最长文宽+双侧内边距 54px)，右缘与触发器右缘对齐、加宽向左长**——钉考题又实锤一处几何真相：触发器右缘 ≡ 池内容内缘（值框锚行右缘恒等式），初稿「左对齐+右钳」会把宽度锁死成触发器宽，修宪文本同步修正 | V | 已修（五钉：cfg_page_spec bar090 三钉——加宽到最长选项/不窄于触发器/左缘钳池内缘；涂装侧眼手同尺复算同步入旧钉；变异：max 改回 t.w 实咬红） | tests/cfg_page_spec.rs `bar090_dropdown_panel_widens_to_longest_option` `bar090_dropdown_panel_not_narrower_than_trigger` `bar090_dropdown_panel_left_edge_clamped_to_pool_inner` |
| BAR-091 | termview（paint_cfg_pool_content_impl Upper 分支） | 上池级视口平移裁剪带左缘 = 池框 x+4，吞了左粗竖条（9px）的右半——粗条像素随内容双代一起平移，用户实机录屏逐帧判「平移的是上池的边框和内容……视觉上断裂」（2026-09-15）。契约（十八修修宪 theme.md §七）：上池级平移裁剪带 = 上池**内容矩形**（POOL_CONTENT_INSET 内缩），池框/左粗竖条一像素不进带；同修留隙律（双代缘距恒 = G：页面级 2 格/上池级 4 格，平移距 = 视口宽+G——「紧贴是挤，视口平移需要页面外有隐藏布局状态」）与曲线换 ease-in-out cubic（ease-out 起步满速「太块」） | V | 已修（框静止四点采样 = 静物参照逐像素等+双代留隙/双 accent oracle 同帧钉+ease-in-out 1/4 程 0.0625/半程 0.5 钉；变异四咬全中：曲线回 ease-out/Page travel 删 G/Upper 带左缘回 +4/Upper travel 删 G） | tests/termview_spec.rs `spec_上池平移_框静止留隙_涂装钉` `spec_视口平移双代同画_涂装钉`、tests/cfg_page_spec.rs `set_tab_hangs_page_pan_with_dir_and_frozen_epoch` |
| BAR-092 | gles_present（PanComp 合成四连） | 十九修 D8 平移升合成期首版只做目标端 scissor，丢了 blit_shift 的「带内**源**→带内目标」钳制语义——纹理里带外像素（上池框左粗竖条/页环缘）随偏移滑进带内，用户实机判「上池的内容平移带上了上池的边框」（2026-09-15，vc426 热更后即报）。另 Upper 域隙底误用 CARD_PAGE_BG scissored clear（页底色矩形盖掉池内芯渐变=第二重「框感」）。补咬（rd427 f0008 实录）：hold 烘焙滞留——贴死后 sig 无变化不触发重烘，「无上池行」画布滞留到下次交互（上池行消失~600ms）；修=ConfigSig 增 pan_hold 布尔维（起步/贴死各翻转一烘，模式开关非动画进度，不违合成期优先律）。三咬（用户真机终验）：偏移 draw 目标 rect.xy 写 (cfg_off+dx, cfg_dy_extra)——layer 顶点着色器 px=a_rect.xy+c*a_rect.zw，rect.xy=四边形屏幕位置，y 漏加 by0=带内容整体上移 by0 再被 scissor 剪进带（用户判「先上提~5px 再平移再落回，像被提起又放下」）；修=目标 y=by0+cfg_dy_extra（dst=源带+(dx,dy)，与 blit_shift 平移语义全等）。契约：偏移 draw 一律源矩形钳制（draw_slot_layer_src，uv=带子矩形，语义=blit_shift 带对带）；隙底分域——Page=页背景（clear，十八修钉）、Upper=池内芯静物（不填，烘焙 hold 期上池行不进静态画布 paint_cfg_pool_content pan_upper_hold 维） | V | 已修（draw_slot_layer_src 源钳制+PanComp.clear_bg 分域+paint_cfg_pool_content pan_upper_hold 维+ConfigSig.pan_hold 维；钉 spec_bar092_upper_hold_rows_out_of_canvas 三断言：行墨进静物即红/hold 静物=无行内芯） | tests/termview_spec.rs `spec_bar092_upper_hold_rows_out_of_canvas` |
| BAR-093 | android_app（合成期帧泵/PanMove 槽） | 十九修 D8 首版合成域三个连带缺陷（2026-09-15 用户真机终验报「新内容不平移进来，等走得差不多了突然出现」「往右平移时旧内容往左闪现一小距离再右移」「页面平移过冲」；redroid panc 逐帧遥测实锤：Upper 平移全程只渲染 t=0.000→0.945 两帧）：①Upper hold 把上池行从配置槽画布拿掉，而新代滑动层画的正是这张画布——新行不在任何滑动纹理里，贴死重烘才闪现；②平移期池高弹簧逐帧改 pool_upper_h=逐帧全页重烘（12MB 上传×~8 帧），帧饥饿放大一切；③偏移 draw 目标 y 漏加 by0（带内容整体上移再被 scissor 剪回，即「提起又放下」）。修=PanMove 槽（起步帧烘新代稳态，连环平移 PanOld 从 PanMove 接力捕获）+平移期冻结池高喂入（弹簧挪到贴死后续弹）+目标 y=by0+cfg_dy_extra；另附实测：redroid WaitUntil(4ms) ~200ms 一圈、Poll 直画启动期黑屏（redroid 不能判动画连续性，只判逐帧正确性——判卷场分工入档） | V | 已修（panc 逐帧遥测+slot_flags 对账 pm_v/pm_b；redroid 中帧像素核验新旧双代在场、纵向对齐） | tests/stage_spec.rs 九槽可见性钉+panc 遥测对账（C 档实录判卷） |
| BAR-094 | cfg_page（cursor_row）+ dual_pool（layout）+ android_app（pan_freeze） | 池区动画时序三症同族（2026-09-15 用户真机逐帧终验 vc430 报四条，全是十九修池高弹簧体系的连带症状，用户明确定性「过冲是我从来没要求你做的」「同步改异步不是我允许的」）：①切标签 Page 平移新页面带**旧池高**平移进来、贴死后再二次高度动画——病灶=BAR-093 的 pan_freeze 冻结喂入，PanMove 起步帧烘「新代稳态」时 pool_snap 还是冻结旧值；②点下池切上池内容的过冲——病灶=十五修池高弹簧（fx_spring ZETA=0.76 欠阻尼，自述过冲 ≈2.5% 屏高「墩一下」，当年为键盘 inset 手感拍的参数被复用到池区）在 pan_freeze 解冻后从冻结值续弹；③上池高度与下池光标异步（光标先到、高度后弹）——病灶=光标弹簧立即起步（350ms）而池高被 pan_freeze 拖到平移贴死（250ms）后才弹（350ms），「原来同步」=十八修涂装域逐帧全画（掉帧但同步）；④下池选中框「瞬移+过冲」——病灶=欠阻尼弹簧中段速度峰+过冲回摆。契约（用户拍板）：**池高 = 目标值硬切零动画**（「新页面就是新页面，整个平移过去后不要再有任何动画」——pan_freeze 随弹簧废除，喂入即到位：sig 起步帧变一次 = 一烘，平移期 sig 恒定无逐帧重烘，PanMove 新代烘的就是目标池高，BAR-093 治帧饥饿的目标由灭弹簧天然达成）；**下池光标 = 250ms ease-in-out cubic 定时缓动**（PAN_MS 同钟同曲线，select 同刻挂账→光标滑行与上池内容平移严格同步；全程单调不过靶零过冲）；fx_spring 保留给键盘 inset（ui-base D5 拍板的合法用户）与标签栏游标（用户未点名，不动） | V | 已修（三钉：cfg_page_spec `bar094_cursor_no_overshoot_and_in_sync_with_pan`（全程采样无过冲+光标相对进度≡pan t 同步律）+dual_pool_spec `bar094_dual_pool_height_snaps_to_target`/`consecutive_feeds_follow_immediately`/`layout_clock_free`（硬切律）；变异两咬全中：缓动回弹簧→无过冲/同步钉红、硬切回中间值→三钉红） | tests/cfg_page_spec.rs `bar094_cursor_no_overshoot_and_in_sync_with_pan` `select_cursor_eases_slides_and_settles` + tests/dual_pool_spec.rs `bar094_dual_pool_height_snaps_to_target` |
| BAR-095 | dual_pool（池高）+ tab_bar（游标）+ termview（预览）+ comp_registry | 二十修（BAR-094）误读用户意图的三处回修（2026-09-15 用户终验 vc431 报）：①「池高变化动画完全没有了……我让你不要有过冲，不是说让你把动画搞没，用跟光标一样速度的动画，保证光标移动到位池高变化也到位」——BAR-094 把池高一刀硬切=把「无过冲」执行成了「无动画」；②「从下池的上行点下行依然有过冲」+「标签页切换也带过冲」——标签栏游标（tab_bar cursor_x）还在欠阻尼弹簧上（BAR-094 时用户未点名我未动，终验点名=违曲线单一源）；③「组件池动效引擎里的视口平移预览描述 ease-out」——Preview::ViewportPush/Swipe 预览松手硬编码 ease_out_cubic 500ms（十四修自编，与实机面板 rise_release 180ms 不同款）=组件池口径与实现漂移。**深挖附证（panc 遥测+stats）**：平移 t 序列数学单调零过冲，但 draw_avg_ms=47ms——光标/游标/池高的 sig 维逐帧触发配置槽全页重烘（14MB），250ms 动画只画 5-7 帧=「跳变/过冲感」的真凶是帧饥饿不是曲线（BAR-096 挂账根治）。契约：**分域律**——Upper 域（点下池）池高 glide_upper_content_h = 250ms ease-in-out 与光标/平移同钟同曲线（「光标到位池高也到位」）；Page 域（切标签）与非平移变更 = set_upper_content_h 直通（新页池高起步帧就位，「新页面就是新页面」）；**曲线单一源纪律入宪**（theme §五）：位移类一律 ease_in_out_cubic 250ms、减速类 power2_out/rise_release、fx_spring 只许键盘 inset+弹簧展品自展示、新动画入册三同步（ui-base §二+组件池+预览曲线）任一漂移=红 **补咬（同夜真机遥测咬出，vc432）**：glide 插值写 `(*to - *from) as f32` = **u32 相减在池高变小时下溢**成 4e9 级巨值 → upper.h 天文数字（release wrap 静默）→ band 垃圾负数，真机实录 `t=0.678 band=(97,175,1169,-1385067369)`、`t=0.953 band=(97,175,1169,-203621225)`——**钉只测了变大方向（216→480），缩小方向（480→216）是漏网维度**；修 = 有符号中间量 i64 + 夹紧 [0, max(from,to)]。教训：数值维度必须双向钉（升/降、增/减），单方向钉=半个钉 | V | 已修（六钉：dual_pool bar095 缓动无过冲/幂等与 set 打断/**缩小方向无下溢** + cfg_page pan_upper_active 分域探针 + tab_bar 游标缓动 250ms 贴死；变异三咬全中：glide 回弹簧→bar095 红、游标回弹簧→缓动钉红、缩回 u32 相减→下溢钉红） | tests/dual_pool_spec.rs `bar095_dual_pool_glide_eases_no_overshoot` `bar095_dual_pool_glide_idempotent_and_set_overrides` `bar095_dual_pool_glide_shrink_no_underflow` + tests/cfg_page_spec.rs `bar095_pan_upper_scope_probe` + tests/tab_bar_spec.rs `spec_tab_bar_select缓动钉` |
| BAR-096 | gles_present（槽画布尺寸）+ termview（拆层涂装）+ android_app（两新槽 sig/烘焙/合成） | 池区动画帧饥饿根治（件一/件二，2026-09-15 用户拍板「就剩下你说的那个了」→ 立案）。病：光标滑行/标签游标/池高缓动三路的动画值都进配置槽 sig → 每帧全页重光栅+~14MB 上传（stats draw_avg_ms=47 → ~21fps 上限）= 用户逐帧所见「跳变/过冲感」的真凶（曲线本身数学单调，panc 遥测+钉双证）。件一=标签栏独立小画布层（ChromeSlot::TabBar=9，屏宽×TAB_LAYER_H≈0.65MB）；件二=下池光标独立小画布层（ChromeSlot::LowerCursor=10，池内容宽×行高≈0.69MB）；两槽支持自有 dims（ChromeLayer.dims + set_slot_dims + bake 按 dims 上传）；ConfigSig 摘除 tab_sel/tab_scroll/tab_cx/cursor_row_q 四维 → 配置槽从此不再逐帧重烘；光标位置进合成期 placement（LayeredPlace，Page 平移期带内双代：旧代位 = pan.old.cursor_row + old_dx）；标签栏层不进平移带（十八修语义标签静止）。**保真条**：拆层不许改任何像素——①标签栏层与整页版逐像素等价（y_shift 语义：层缓冲坐标 = 页坐标 − 内容原点；钉实锤咬出并修正一处真 bug：底线丢了内容原点，页版底线画到 y=110 而非 165）；②光标层渐变参照吃「框在页上原位」的页坐标+页尺（grad_ref 参数，paint_row_frame_gradref），防层画布尺漂色（1000×162 的 denom ≠ 1260×2400 的 denom）。**遗留（BAR-097）**：池高一路（pool_upper_h 仍在 ConfigSig）未拆——上池框底缘/下池 y 位移/底缘圆角依赖三层几何与平移双代全组合，独立一轮做 | V | 已修（钉二：termview_spec `spec_bar096_标签栏层_与整页逐像素等价`（差 0 像素）+`spec_bar096_光标层_渐变保真钉`（页坐标页尺独立复算锚）；存量钉同步：spec_cfg标签栏_涂装钉 补 line_span 喂入） | tests/termview_spec.rs `spec_bar096_标签栏层_与整页逐像素等价` `spec_bar096_光标层_渐变保真钉` | **补咬（2026-09-16 用户真机报，当日修）**：拆层**回归 BAR-089**——「下池三级选择框把文字盖住」（用户原话）+「两池整体左移约 20px」（云安卓 A/B 实测证伪左移：vc433/vc434 的池框/行框/文字左缘逐像素一致＝61/83/123，左移非拆层引入）。病灶：BAR-089 曾定「选中框芯不透明 → 行文字必须**最后画**（同画布先框后字）」，拆层把框移到独立层（画在配置槽**之上**）而文字留在配置槽 → 框芯盖字；`mark_chrome_alpha` 判据是「非纯黑即不透明」，故 accent 亮时必盖、accent 暗且芯恰为纯黑时被误判透明（**redroid 判卷盲区**：我那次看着文字正常，用户真机 accent 亮就盖住）。修 = 选中行文字画回**层内框之上**（尺与 paint_pool_lower 同源：title_band 90/title 36/meta 30/内缩 27/faux-bold 双画），CursorSig 增 focus+epoch 维（聚焦/行表变即重烘小画布）；补钉 `spec_bar096_光标层_选中行文字落墨钉`（文字墨必须落进框内芯区；变异：层内不画文字→红）**〔2026-09-17 注：此钉行为被 BAR-107 否决取代——「文字烤进光标层」正是平移期双画错位病根，新契约=层内无字+芯半透明〕** | **判卷（2026-09-16 真机）**：Page 平移 **3 帧→31 帧（10 倍）**、t 序列逐帧细密；Upper 平移 3→4 帧（池高一路未拆，BAR-097 待办）；`draw_avg_ms 47→9`（每帧成本降 5 倍）；panel-anim fps=70。细节：Page 起步帧 band 用旧池高（时序缝，随 BAR-097 修）；批量注入多 tap 丢后续条目（判卷须单发+查 gest） |
| BAR-098 | android_app（池区帧泵闸）+ fx_spring（产帧判定） | 平移落停帧被 33ms 节流吃掉（2026-09-16 用户真机截屏两连报：点标签播动画后「选中行左侧冒出残字+池框右缘探出屏外」，实定量 = 光标层整体右移 ~25-40px、配置槽文字在真位被移位框芯压剩一截 = 用户此前「两池左移 20px」体感的真身）。病：收敛补终帧机制（2026-09-14 下拉余影案立，「活性翻 false 那圈必须再脏一帧擦终态」）的强制脏帧**与活性期动画帧同走 33ms 节流**——平移贴死那圈距上帧恒 <33ms（活性期每 ≥33ms 刚画过一帧）→ 补帧被跳过、活性探针同瞬关门 → **末帧永是 t<1 的平移中帧**：带内新代（PanMove@new_dx）与下池光标层冻在残余偏移（120Hz ≈13px/60Hz ≈44px/掉帧更狠）。**redroid 判卷盲区**（同案第二处）：帧饥饿场末帧天然落在贴死之后（自渲染贴死态），云安卓 A/B「逐像素一致」全是贴死帧咬不到；真机 31 帧密帧才踩中。另附观测纪律：shot.rgb（CPU 重画）画的是缓存画布非面板栈真实态——AI 面板在顶时它照出终端页，后台判卷必须以 shot-gl+gest 回执为准。修 = 产帧判定收纯函数 `fx_spring::cfg_fx_frame_due`（翻 false 恒产帧，否则 33ms 节流） | V | 已修（钉 `spec_bar098_池区泵_落停补帧不吃节流钉`：翻 false+距上帧 8ms 必产帧/活性期节流正常；变异：删翻 false 强补→红） | tests/fx_spring_spec.rs `spec_bar098_池区泵_落停补帧不吃节流钉` | 待真机终验（同位置点标签复拍） |
| BAR-099 | cfg_page（pan_snap/活性探针）+ android_app（终点帧消费） | 动画结束闪烁/过冲感机制根修（2026-09-16 用户真机终验 vc436 判「问题仍严重」：点标签切页「把偏移当最终位置停下，然后重新闪一下归位」；用户定性底层机制病，拍板先建云安卓完全观测再碰机制）。病：**pan_snap 贴死出 None**——t=1.0 终点态永远无法表达为平移帧，帧泵闸又是**时间驱动**（now < start+250ms 即关门），死线后 t<1 的偏移末帧冻在屏上，等另一代码路径补落停帧 = 「停在偏移+闪一下」整类病灶的根；BAR-098 的补帧不吃节流只补了「补帧被跳过」一臂，死线停泵的时序窗仍在。修 = **状态驱动帧泵 + 贴死钳制**：①pan_snap raw≥1.0 恒出 t=1.0（pan_offsets 数学：old 全出带/new 归零，与稳态逐像素一致）；②活性探针改「账在 = 活性在」（PAN_SETTLE_SLACK_MS=500 宽限兜底停泵防消费链断裂空烧），终点帧必被渲染；③渲染路径每帧 snap 后 consume_settled_pan（消的账 = 刚画那帧的账，同一 cfg_now）——账随帧消泵即停，活性翻 false 那圈 BAR-098 机制再产一帧回稳态单代（逐像素一致无感）；④pan_upper_active glide 域同步延到消费前（贴死帧不换直通喂入，glide 收敛值=直通同值）。**观测配套（本轮先建）**：redroid-anim-watch.sh（screenrecord 拆帧）+ na-anim-bench 水位线口径 + panc 遥测——判卷口径升级为「panc t 单调到 1.000 且贴死帧必有一行 t=1.000」（旧口径下贴死帧沉默）；redroid 上实锤过旧机制极端版：Page 平移仅 1 帧 t=0.000 后沉默 → 跳变 | V | 已修（钉 `bar099_settle_frame_clamped_t1_and_consumed_on_render`：贴死 t 钳 1.0/几何 (-travel,0)/消费账消泵停/宽限停泵账留迟到帧仍钳制归位；存量四钉同步改写语义——贴死出 None → 消费后出 None；变异两咬全中：贴死改回出 None→红、消费永不消账→红） | tests/cfg_page_spec.rs `bar099_settle_frame_clamped_t1_and_consumed_on_render` + `pan_active_probe_drives_frame_pump` + `bar095_pan_upper_scope_probe`（语义同步） | 待真机终验（点标签切页/点下池行复看结束闪烁） |
| BAR-100 | gles_present（PanOld 捕获/新代合成）+ android_app（平移捕获块） | 切页/点行「方向不对称过冲+终点卡帧」真凶（2026-09-16 用户真机终验 vc437 报：左→右点标签过冲、右→左终点卡一下；下池上→下点过冲、下→上终点卡。用户推测「动画终点位置不对+某机制整页刷新补闪」——真机 panc 四方向对拍**证伪过冲**：pan 偏移/池高/光标/游标全程单调、贴死帧恒 new=+0 精确归位（曲线体系 BAR-094/095 后数学零过冲）；真凶 = **首帧三重烘焙帧饥饿**：平移起步帧同帧做 PanOld 捕获（26MB 拷贝+13MB 重传）+ PanMove 整页重画（13MB 光栅+上传）+ 配置槽重烘（13MB 光栅+上传）→ panel-anim 实锤最大 65ms 单帧，前 60-80% 动画时长只产出 1 帧（顺向 t 洞 0.000→0.807/反向 0.000→0.590），肉眼读作「跳变/过冲/卡帧」；**方向不对称的来源 = 新页内容量**（组件池行多烘焙贵→顺向洞大读作过冲；系统管理行少→反向洞小只余终点补闪感）。修 = ①PanOld 捕获改**纹理互换**（slot_swap_tex：旧代像素本就在屏上纹理里，换柄即封存，26MB 拷贝+13MB 上传全省；捕获源裁决抽纯函数 pan_capture_src——上笔 Upper 接力 PanMove（配置槽是 hold 烘焙无行），首笔/上笔 Page 捕配置槽（Page 新代与配置槽同图）；配对纪律：每笔平移必 epoch+1 → ConfigSig 脏 → 同帧重烘，换出的旧纹理必被 fill(0)+全量重涂覆盖）；②Page 域新代**合成期复用配置槽纹理**（PanComp.scope_page——同代际同 accent 同涂装调用，PanMove 整页重画+上传全省；Upper 域配置槽 hold 无行，PanMove 独立层保留不动）。**遗留**：Upper 域逐帧池高重烘（BAR-097 池高一路）未动——Upper 平移仍 ~5 帧，待用户在场窗口拆 | V | 已修（钉 pan_capture_spec 两言：上笔 Upper 接力 PanMove/首笔或上笔 Page 捕配置槽；变异两咬全中：if 两臂对调→两题齐红） | tests/pan_capture_spec.rs `bar100_capture_src_首笔或上笔page捕配置槽` `bar100_capture_src_上笔upper接力panmove` | **BAR-101 结（同日夜）**：真机 vc440 无点播实测 Page 双方向 41-50 帧密帧全绿——本案切标签部分判卷闭环；Upper 部分归 BAR-097 |
| BAR-097 | gles_present（PoolFx/LowerRowsPan 两新槽）+ termview（层涂装两件）+ android_app（sig 冻结/烘焙/合成位）+ dual_pool（target_upper_h 暴露） | 池高一路拆层（BAR-096 遗留，2026-09-16 夜落地）：vc442 真机判卷 Upper 平移仍 7 帧/ras=28ms——BAR-103 LUT 后残余的结构性病灶 = 池高 glide 逐帧改 pool_upper_h 进 ConfigSig → 配置槽 14MB 全页逐帧重烘（页环/池内容在 glide 期根本没变也陪烘）。**窄切法**（不动 Page 平移与稳态路径，BAR-089~100 血泪区零接触）：仅 Upper 平移 250ms 内拆两层——①PoolFx 池框几何层（双池框+内芯无行字，池区小画布，sig 含 upper.h 瞬时值 → glide 逐帧只烘这层 ~10ms）；②LowerRowsPan 下池行层（起步一烘内容静止，合成期 y 跟 lower.y 实时 glide + 底缘 scissor 防行底压底环；**渐变锚 glide 终点位**（dual_pool.target_upper_h 新暴露）——贴死帧与稳态配置槽逐像素一致交接零跳色）；③ConfigSig.pool_upper_h Upper 平移期冻结 0（hold 烘焙不画池，真值逐帧变不再白触发全页重烘）。稳态/Page 域：池照留在配置槽（Page 平移双代捕获链 BAR-092/093/100 原封）。预研 stubs（pool_layer_geometry 最大高烘+uv 裁剪案）弃用——渐变 denom 随 h 变，裁剪案与稳态有色差跳变，逐帧小层烘才是逐像素恒真。契约：Upper 平移期配置槽画布零池墨（钉三言扩展 hold 语义）；层涂装必须平移/锚定后与在页版逐像素等价 | V | 已修（两钉：池框层/下池行层逐像素等价（行层对拍只数层落墨像素——圆角剪影外层透明由 PoolFx 池芯合成期垫底）；变异两咬全中：池框层漏 y_shift→红、行层 grad_ref 锚 0→红；存量 spec_bar092 hold 钉语义随扩（hold 画布连下池行也无）） | tests/termview_spec.rs `spec_bar097_池框层_逐像素等价` `spec_bar097_下池行层_逐像素等价` | **vc443 真机判卷**：Upper 平移 ras 28→18ms、帧均值 57→19ms——但只产 6 帧，帧列实锤中帧成本已降到 3-6ms 而帧间隔 27-36ms：**33ms 帧泵节流成为新天花板**（帧贵时代的自我保护，帧便宜后反成瓶颈）。**补咬**：cfg_fx_frame_due 节流 33→16ms（≤60fps，与采样缝泵同档），钉同步（活性期 15ms 不产/17ms 必产），变异实咬（回 33→钉红）；**用户肉眼终验通过（2026-09-18）结案**（帧数终验早前已判：vc444 反向点行 Upper 13 帧、间隔钉死 15-17ms 满帧率） |
| BAR-103 | termview（paint_rect_ring/paint_row_frame_gradfill/fill_rect） | 渐变涂装逐像素算术热（2026-09-16 BAR-102 后计时考题定罪：全页重烘 63ms 分解 = 池框 29+页环 18+池内容 15+fill 1，文字缓存后几何/渐变成为唯一瓶颈——每像素 1 次整数除法（ring_gradient_rgb 的 t=(lx+ly)*255/denom）+内芯每像素另付 2 次压暗 lerp（frame_bg_rgb 每次重算 dark(c1)/dark(c2)）+逐像素索引边界检查）。修 = **渐变 LUT 化**：环带色/内芯色都只是 s=lx+ly 的一元函数（同一把 135° 尺同分母），每次涂装建表 denom+1 项（~3.7k 项 ≪ 百万像素），内芯中带改行切片 iter_mut 直写 ≈ memset 速；fill_rect 同改行切片 fill。覆盖四面八方法：paint_rect_ring（页环/池框/值框）环带+中带+角带、paint_row_frame_gradref（下池行/上池值框）环带+内芯、paint_pool_upper 标签背衬。blend_px(·,·,·,255) 在行切片借用期内联（blend α255 ≡ fg 仅保目标 α，逐比特一致）。效果：计时考题全页重烘 63ms→26ms（chrome 18→8、池框 29→11、内容 15→6.3）。契约：渐变采样热路径只许 LUT/查表，新涂装代码逐像素调 ring_gradient_rgb/frame_bg_rgb = 回潮 | V | 已修（一钉三言段扫：池内芯中带整行段/环带左粗条整列段/行框内芯行段全部逐点等于直算——单点钉曾漏 ±1 索引变异（整除量化+t 步进亚字节同字节色），段扫钉 ±1 必咬；变异两咬全中：中带 s+1→红、行框 s+1→红） | tests/termview_spec.rs `spec_bar103_渐变lut_采样钉` | 待真机 vc442 实测：Upper 平移帧数/ras 账对照（BAR-097 池高一路拆层与否据此定夺） |
| BAR-105 | gles_present（池区合成 z 序）+ dual_pool（final_pool_snap）+ android_app（PanMove 终点锚） | 贴死闪变/「新内容不跟随，走完突然出现」**真凶**（2026-09-17 vc447 epoch 对账实验定罪——grab_a/grab_b/panmove_bake 全 epoch=8 对同一笔 Upper 平移，仪器清白）：末帧合成（t=1.000）上池**无内容**、首帧稳态详情齐全 = 新代行全程缺席后突然出现。病灶两臂：①**合成 z 序错**——配置槽→PanOld→PanMove→PoolFx→LowerRowsPan 的序里，PoolFx 的不透明池芯（mark_chrome_alpha 把渐变芯 RGB≠0 强转 α=255）压在 PanMove 新代行之上 = 新代行全程被埋（BAR-097 拆层引入， BAR-097 两钉只钉涂装等价没钉合成序 = 钉盲区）；②**PanMove 起步几何锚**——烘焙吃 pan 起步 pool_snap，芯渐变分母（denom 含池高）与池框底缘随 glide 逐帧过期，贴死帧与稳态重烘必错位。修 = ①z 序前移：PoolFx 挪到带内双代之下（配置槽→PoolFx→带内 PanOld/PanMove→LowerRowsPan）；②PanMove 锚终点几何（final_pool_snap 纯函数，与 LowerRowsPan 终点锚同一源——LowerRowsPan 块同步迁用防双源漂移）。契约：合成期层叠顺序是行为契约——不透明层压内容层 = 埋内容；新代滑动层几何必锚 glide 终点 | V | 已修（钉 `spec_bar105_终点池快照_数学同源`：upper.h=target/lower=区−上池−间距/x·w·scroll 继承；变异：lower.y 丢 POOL_GAP→红；z 序为 GLES 合成侧 B 档，判卷归 vc448 实机差分） | tests/dual_pool_spec.rs `spec_bar105_终点池快照_数学同源` | vc448 复判：修复真实有效（z 序后新代行全程可见）但差分仍 1.47——残余闪变真凶另在（BAR-106 带原点笔误），本案 z 序+终点锚两臂不动 |
| BAR-106 | gles_present（带内双代合成放置）+ cfg_page（pan_band_draw_x 纯函数） | 起步左闪+贴死左冲补闪**真凶**（2026-09-17 vc449 全序列仪器+panc 对账定罪——panend 差分机升级全序列留帧后：a00 平移正常滑行、a04 新代近乎到位、a05~a07 新代整体左切 ~70-97px、b 稳态正确；panc 同帧报 `t=1.000 old=-1144 new=+0` 数学精确归位——**放置与数学脱节**，先前「末帧旧内容满屏」判读实为**新代左切两字**（标签**页块**/下拉**面板**/字段**标签列），仪器与 epoch 一直清白）。病灶：BAR-100 源钳制改造（全幅纹理→带内 uv 采样 draw_slot_layer_src）时 x 漏加带原点 bx0（y 对称位却正确含 by0 = 笔误实锤）——t=0 旧代画在 x=0 而非 bx0(=97) = 起步左跳 bx0（用户「旧内容先往左闪现小距离再右移」）；t=1 新代停在 −bx0 = 贴死左冲，稳态重烘 +bx0 补闪归位（用户「过冲+闪一下归位」）；Page 域同病（带原点 43，切标签双向过冲同源）。修 = 两处 draw_slot_layer_src 的 x 抽纯函数 `pan_band_draw_x(bx0, cfg_off, dx)` 含带原点（放置唯一来源）。契约：带内源钳制绘制 = 带原点+偏移，放置数学只许走 pan_band_draw_x | V | 已修（钉 `spec_bar106_带内绘制_放置含带原点` 四言：t=0 旧代在位/新代屏外右待、贴死旧代出尽/新代归位=bx0；变异实咬：放置退化漏 bx0→钉红→复原绿） | tests/cfg_page_spec.rs `spec_bar106_带内绘制_放置含带原点` | **vc450 判卷达标（2026-09-17）**：Upper 域全序列差分 a00 4.14→a01 5.51→a02 4.74→a03 2.41→a04 0.68→a05 0.29 平滑收敛、无跳切无平台（vc449 修前 a05~a07 卡死 1.44/>12 像素 120777）；末帧残差 19386 px 全是一级卡框描边 1-3px 细线级（差分图内容区纯黑=文字逐像素归位，亚感知 glide 尾）。Page 域差分机不吃（set_panend_mark 只喂 PanScope::Upper），判卷=panc 遥测 t=1.000 new=+0 精确归位（带原点 43）+与 Upper 同一两处调用点+钉——**用户肉眼终验通过（2026-09-18）结案** |
| BAR-104 | gate（PanendCap 状态机/双帧倒盘）+ gles_present（present_frame 抓帧）+ android_app（pan 标记喂入） | 贴死交接帧像素闪变（2026-09-17 用户肉眼否 vc444 后，用户系统录屏 18s 逐帧差分定罪）：Upper/Page 平移**末帧合成与稳态帧逐像素差 4.19**（全卡文字/渐变/边框同闪），±30px 最佳平移搜索残差不变 = **非位置没到位，是内容重烘不一致**——用户「过冲/终点卡一下/闪一下归位」体感的真身是交接闪变。本提交 = 观测仪先行（判卷纪律：先定罪再修）：panend-cap-req 点播 → present_frame 逐帧问 PanendCap 状态机（不投零 readPixels 开销，同 BAR-076 点播制）→ 平移末帧合成 panend-a.rgb + 首帧稳态 panend-b.rgb 双帧落盘；na-panend-cap.sh 拉回 + PIL 转图 + 逐像素差分报告（均值/热区/diff PNG）。契约：贴死交接两侧必须逐像素一致；本仪帧数/耗时同 BAR-101 纪律不作性能证据 | V | 仪器已落（钉三枚；变异两咬全中：收尾不缴械→红、平移帧不记账→红）；**根因已定罪+已修（2026-09-17）**：半透明涂装在透明画布上落墨丢底色贡献——blend_px 在 dst=0 画布存 `RGB=C·a/255、α 保持 0`，slot_bake 的 mark_chrome_alpha 再把 RGB≠0 强转 α=255 → 合成出 `C·a/255` 而非稳态页内版 `C·a+B·(1−a)`（页底 B=CARD_PAGE_BG 非零）。修 = 三条层画布预填页底色（PoolFx 池框层/LowerRowsPan 行层另补下池框环+渐变内芯/PanMove 新代行层），两枚 BAR-097 钉页画布改预填底色（钉盲区同修：行内墨全不透明时行内对拍咬不住透明底变异，对拍范围扩到下池整框），变异（预填改回透明）两钉全红→复原复绿 | tests/gate_spec.rs `spec_bar104_交接差分_状态机四态` `spec_bar104_交接双帧_倒盘协议` `spec_bar104_交接差分_点播触发两态` | vc446 已判：**未达标**（差分均值 1.59/>12 像素 14.6 万≈修前）——但捕获帧内容对不上号（a=空上池+旧选中/b=旧选中详情，非贴死两侧应有新代），嫌疑=真·缝隙帧或仪器抓错帧；仪器升级抓帧带 epoch 对账（grab_a/grab_b/panmove_bake 落 trace），待单平移对照实验复判 → **复判结案（2026-09-18）**：epoch 对账证仪器清白，真凶=BAR-105（合成 z 序埋新代行）+BAR-106（带内绘制漏带原点 bx0），两修判卷达标；用户肉眼终验通过（2026-09-18「可以结案了」）——结案 |
| BAR-102 | termview（字形光栅缓存） | 动画关键帧 CPU 文字重光栅（2026-09-16 na-rec 120fps 实录+vc440 分段账双证定罪）：用户实机「终点卡一下/闪一下归位」= 平移到 97% 冻住 75ms 后终点跳到位（实录逐帧定量）；下池点行 Upper 平移 250ms 只呈 4-5 个位置（分段账 ras=49-62ms/帧）。病灶：槽涂装四条热路径（draw_glyph/draw_label/draw_text_centered/draw_items_left_inset）每字每次 font.rasterize 从轮廓重光栅——全页重烘一次 50-130ms，全压在动画起步帧/终点帧/Upper 逐帧上；浏览器同级流畅的真相 = 合成器线程 GPU 纯搬纹理、零逐帧光栅，我们的平移中段也是 GPU 搬纹理（2-5ms/帧）但关键帧全在 CPU 重画文字。修 = TermView 挂字形位图缓存 rasterize_cached：key=(字, 字号bits, 字体id)（字体 id 必进 key——draw_glyph 走 prefer_cjk、槽路径走 pick_font，两族判据对同字可分歧，串字体=拿错位图）；命中=Arc 克隆零光栅零分配；4096 封顶清表。四调用点全转。契约：光栅热路径只许走 rasterize_cached，新画字代码直调 f.rasterize = 回潮 | V | 已修（一钉两言：二次取同字同号必命中同一份 Arc（ptr_eq）+ 缓存位图与直调逐字节一致；变异实咬：命中也重光栅→ptr_eq 言红） | tests/termview_spec.rs `spec_bar102_字形缓存_命中且逐字节一致` | 真机 vc441 已判（2026-09-16 夜）：Page 平移双方向 51-58 帧/ras 11-13ms 达标（终点冻帧消失）；Upper 平移仍 5 帧/ras=49ms 不达标——瓶颈非文字而是几何/渐变涂装，续 BAR-103 |
| BAR-101 | 观测纪律（anim-cap）+ cfg_page/dual_pool（池高 glide） | **anim-cap 点播武装整轮投毒**（2026-09-16 傍晚自查翻案）：上午据「点播轮整轮只产 3 帧、124-233ms/帧」判「Page 平移帧饥饿=用户体感真凶」——vc440 无点播对照实测证伪：同机同动画顺向 50 帧/反向 41 帧、均值 4-5ms/帧（fps 182-224），病灶数据纯系 glReadPixels(14MB) 停顿的仪器 artifact（BAR-076 点播制只防了「不投也付税」，没防「投了整轮饿死」——采样 1 帧即整轮 2-3 帧）。**判卷纪律新钉：anim-strip 只判像素内容对错，帧数/帧耗时一律不作性能证据；判性能只信无点播的 panel-anim/panc**（scripts/README + state.md 已钉）。**同场分段账定罪 Upper 平移真病灶**（BAR-097 定量实锤）：点下池行 Upper 平移 4-5 帧、50-100ms/帧，vc440 分段账 ras(CPU 光栅)=**49-62ms 每一帧**——池高 glide 逐帧改 pool_upper_h 进 ConfigSig → 配置槽 14MB 全页逐帧重烘（BAR-096 拆了光标/游标两路，池高一路未拆）；Page 平移起步帧残余 ~80ms（epoch 重烘+捕获，BAR-100 已从 233ms 压到此）同账在册。契约：性能判卷前必须先问「这轮有没有点播武装」；分段账仪器（panel-anim 行尾 段=[ras/gen/upload/draw]）随本轮落地常驻 | L | 观测纪律案（无码病，钉=文档三处：state.md BAR-101 条/scripts/README anim-cap 警告/调试闸门.md 分段账格式；分段账仪器本身 chore 观测 ecf0998；**已升清单项**：排障手册观测矩阵「性能」行=「anim-cap/panend 点播数据永不作性能证据」——lesson-audit 只扫本表行内词，补此指引使审计认账，2026-09-18 晨班会） | —（观测纪律，tests:na） |
| BAR-107 | termview（paint_row_frame_gradref 内芯 alpha/paint_lower_cursor_layer 摘文字）+ android_app（光标层烘焙调用点） | 下池选中光标「框动行不动」+重影（2026-09-17 用户真机录屏 f462/f494 + vc449 panend 差分 a00 帧「组件」双现定罪）：点下池切行时「整个框都移动了」——选中行文字跟着框走且与行层文字同帧两处错位。病灶 = BAR-089 历史绕道：当年框芯不透明（mark_chrome_alpha 非纯黑即强转 α255），选中行文字不画进光标层就被芯盖没 → 文字烤进光标层；平移期光标层走 250ms 缓动、行层跟池高 glide 走另一条曲线 → 双画错位（用户拍板 C 档视觉契约：**文字只归行层，光标只画框，芯改半透明**）。修 = ①paint_row_frame_gradref 增 core_alpha 尾参（0=旧 opaque 语义，其余调用点恒 0；>0 写真 alpha 字节，mark 对高字节非 0 像素直通——光球半透已实证此路；GLES 合成侧 BLEND 常开段 1293-1666 覆盖光标层 draw，SRC_ALPHA 真混合）；②paint_lower_cursor_layer 删文字块+删 row 参（trait/转发/调用点同步），芯传 LOWER_CURSOR_CORE_ALPHA=0x55（≈33%，观感可微调）。契约：光标只画框、文字只归行层、芯半透明渐变暗底——层内画字 = 回潮 | V | 已修（新钉 `spec_bar107_光标层_框动行不动` 三言：芯 α=0x55 且 RGB 非零/环带裸 RGB 高字节 0/圆角剪影外全 0；变异实咬：core_alpha 传 0→钉红→复原绿；旧 `spec_bar096_光标层_选中行文字落墨钉` 钉的是旧行为，本行取代） | tests/termview_spec.rs `spec_bar107_光标层_框动行不动` | **vc452 仪器判卷达标（2026-09-17）**：①panend 差分机（Upper 域，装修框→控件）：a00 对 b 均值 0.30、>12 像素 21598 全是一级卡框描边 1-3px 细线级（差分图内容区纯黑 = 文字逐像素归位，vc450 同款签名）；②na-rec 内录双跳（控件→组件→控件）逐帧判读：滑行中帧 f03（光标跨 组件/功能光标 两行之间）与 f24（池高 glide 中）每行文字均只出现一次且原地不动，无光标层带字重影——「框动行不动」目视实锤；原生分辨率放大复核文字清晰单现。~~待用户肉眼终验半透明芯（α0x55）观感~~ → **用户肉眼终验通过（2026-09-18）结案** |
| BAR-108 | android_app（触摸命中臂几何源）+ ui/modal（pick_screen_px 纯函数） | 挂起态触摸几何盲区（2026-09-17 十五修判卷现场实录定罪）：退后台 suspended() 弃窗（window=None，BAR-004 既定）后，注入触摸的**起手路由正常**（跳框模态仲裁不查窗口，trace「起手→跳框模态」照打）但**抬手命中臂静默跳过**——跳框关闭臂 `let (Some(page), Some(win))` 因 window=None 整臂空转，无报告无动作，跳框在后台/熄屏态关不死（真机三连复现：挂起后 tap 框外/关闭钮全哑，cfg_epoch 不涨；同进程回前台前所有 Ended 无 ui 留痕）。同病臂：齿轮命中/键栏命中/输入栏命中（Started）+ 输入栏 hit（Ended）+ 面板拖拽宽（feed/finish_panel_drag 取 0）。病灶 = 触摸几何直查 self.window，挂起态跟着瞎——inject_scroll 早在 BAR-004 钉过同款教训（「几何取 last_grid，不取 window」），本臂漏网。修 = 屏尺寸单一源 `screen_px()`：窗口活着吃实时 inner_size，死了回退 last_win_px（apply_window_size 每次 Resized 记账）；取舍律抽纯函数 `pick_screen_px`（缓存 (0,0) 未量过 = None，宁可无动作不瞎猜）。契约：触摸命中几何只许走 screen_px，直查 self.window.inner_size 当几何源 = 回潮 | I | 已修（钉三言：窗口优先/挂起回退缓存/双无=None；变异实咬：缓存抢窗口→钉红→复原绿） | tests/modal_spec.rs `spec_bar108_屏尺寸取舍_窗口优先` `spec_bar108_屏尺寸取舍_挂起回退缓存` `spec_bar108_屏尺寸取舍_双无则_none` | vc454 挂起态判卷达标（2026-09-18 凌晨，熄屏后台）：注入 tap 框外 trace 见「跳框收起（关闭钮/框外）」+ rgb 截屏无框残留 ✅；后台齿轮开配置页亦通 ✅。判卷中顺带定罪观测边界：MediaProjection 授权窗约十余分钟过期 + 熄屏弹窗即 denied = 后台录屏窗口期有限，转 redroid（adb screenrecord 免授权）补位判卷 |
| BAR-109 | scripts/redroid-report-relay.py（上游 socket 超时残留） | redroid 长连接 4s 静默准时被掐（2026-09-19 解析页 tmux 插件判卷现场定罪）：`create_connection(TARGET, timeout=4)` 把超时**留在 socket 上**——pump 的 recv 静默 4s 抛 socket.timeout（OSError 子类，被 except 吞）→ 连接双关。redroid 全部 8021 TCP（终端 ws + exec ws + 报表）都过本接力：attach 静画面 tmux 会话（闲 kimi/静态 shell）opened 后 **+4.1s 准时 connection reset** → na 误判死亡自动重连回默认会话（本地附着牌写 psh、实际重连到 kfm-na = 状态错位，再点报「已附着，零动作」）；kfm-na 会话（活跃 CLI ~130ms 一帧）永不静默故从未踩到。直接证据三链：trace「+1001398 opened → +1005378 reset」精确 4.1s、服务器新进程是默认命令而非 attach 命令、探针实验 relay 路 +4.00s 准时死/直连路存活。修 = connect 后 `up.settimeout(None)`（超时只管 connect 段，recv 无限阻塞）；顺带 RELAY_* 环境变量参数化端口（考题起独立实例防撞生产）。契约：**connect 超时不许残留在 socket 上**——凡 create_connection 带 timeout 的接力/代理，接通即摘 | I | 已修（行为钉不 grep 源码：dummy 上游静默 8s 回字，经 relay 静默 6s（越旧 4s 死亡线）后一问一答存活即绿；变异实咬：摘 settimeout(None)→钉红（recv EOF）→复原绿） | scripts/test-relay-timeout.py（chain 第 9 步挂 scripts/test-relay-timeout.sh） |
| BAR-110 | gate（maybe_dump CPU 重画 Parser 分支） | na-shot 仪器盲区（2026-09-19 redroid 判卷现场定罪）：shot-req CPU 重画路 Panel::Parser 分支只画页壳 chrome 不画 tmux 插件内容——产品侧 GLES 槽烘焙/前台兜底路径都正常（screencap 真屏内容齐全），na-shot 截图却整页空白 = **仪器与前台两张皮**，判卷第一眼误判「内容没画」。修 = CPU 重画分支补 paint_parser_content（与前台兜底同参：parser_page_handle snap + pt_off 传真值，锁序 term→parser_page 同 cfg 规）。契约：CPU 重画与前台渲染同参同源——新页面/新内容层进前台即进重画，漏一层 = 仪器瞎一块 | L | 已修（源码钉：Parser 分支窗口内 chrome 与 content 调用共现，摘 content 调用即红；变异实咬：摘涂装→钉红→复原绿） | tests/gate_spec.rs `spec_bar110_shot重画_parser分支带插件内容` |
| BAR-111 | ime_queue（split_bar_focus 纯函数）+ android_app（栏聚焦分流臂） | 栏聚焦吞方向键（2026-09-19 redroid 连发判卷现场定罪）：输入栏聚焦（软键盘在场）时「键盘按键全归栏」臂把方向十字也吞进 `_ => {}`——栏内 v1 无方向语义 = 死键，快捷键行方向键**点按/长按连发在键盘在场时全灭**（trace 实录：栏聚焦期 hold ↓ 1200ms 零落键；ESC 失焦后同位置 hold 落键 12/12 发 \x1bOB、抬手零补发）。修 = 分流纯函数 `ime_queue::split_bar_focus`：方向十字（keybar::is_arrow_key 单一源）直通终端路径，文本/Enter/退格/Esc/组合态/空 commit 全归栏照旧，各自保序。契约：栏聚焦只拦栏内有语义的键，栏内无语义的键不许吞 | L | 已修（钉两言：方向十字全直通且保序/栏内项全归栏且保序；变异实咬：分流退化为全归栏→钉红→复原绿）；**redroid 判卷达标（2026-09-19，热更核）**：软键盘在场（inset=702 栏聚焦）hold ↓ 1200ms → 落键 12/12（\x1bOB）+ 抬手零补发，A/B 对照修前同场景零落键 | tests/ime_queue_spec.rs `spec_bar111_栏聚焦分流_方向键直通终端` |
| BAR-112 | insets（on_inset_poll 纯函数）+ android_app（poll_ime_inset 变更臂/resumed 重算） | 键盘弹起态挂起 → 回前台视口永久半屏（2026-09-19 用户真机报「视口只有半屏了，重排都没有用」；两枚 shot-gl 实锤：终端内容挤在卡上部 ~40%、底大片空白 = kb_shift 卡死在键盘弹起态）。病灶**双洞**：①poll_ime_inset 变更臂 `if let Some(w) = &self.window`——挂起态（BAR-004 弃窗）轮询检测到 inset 702→0 时**记账不重算**，回前台后新旧值相等再也检测不到变化（轮询只认变更），kb_shift 卡 18 行永不归位；②resumed() 重建窗口后不跑 apply_window_size（系统同尺寸 resume 常不发 Resized 事件）→ 无任何路径重算。重排救不了的原因：重排只抖 tmux 窗口尺寸，kb_shift 是 na 侧视口字段，两条路。修 = ①变更裁决抽纯函数 `insets::on_inset_poll`：窗口死了不记账（留旧值，回前台同差再判时变更臂必带重算）；②resumed() 建窗后幂等强制 apply_window_size 一次（3049 判等闸不抖 pty）——挂起期漏算的几何一律归位，洞类（不只本路径）封死。契约：任何「检测变更→记账→重算」三段链，重算不能落地时不许记账；resume 后几何必须幂等重算一次。redroid 判卷盲区注：redroid 挂起期轮询不点火（循环即睡），4 轮键盘弹起+HOME 竞速全自愈复现不了——计时竞速类病灶 redroid 抓不到。**redroid 回归判绿（2026-09-19 热更核）**：正常键盘周期三链路全保真（弹起 702px+平移 18 行/收起 0px+平移 0 行/弹起+HOME+回前台自愈照旧），终帧全屏截图实锤 | I | 已修（钉四言：死窗不收记账（双向）/同值不抖/活窗变更放行；变异实咬：裁决退化不看窗口→钉红→复原绿） | tests/insets_spec.rs `spec_bar112_轮询裁决_窗口死了不记账` | **补记（2026-09-19 午后，值守事故）**：BAR-112 真机热更值守循环的成功判定写错——grep 脚本输出末两行找「已就位」，而「✅ 热更核心已就位」实际在输出中段（末两行是冒烟回归报表）→ 手机唤醒后推送已成功却判定失败，每 60s 重推一次，na 被 force-stop 重启约十轮，用户实机报「na 为什么反复重启」定罪杀循环。教训：值守/重试循环的成功判定锚必须对脚本实际输出**全文**验证过，且重试前必须先问「上次到底成没成」——推送类动作本身有副作用（重启），盲重试 = 周期性事故 |
| BAR-113 | scripts/font-bake.py（borrow 守卫/清单）+ assets/fonts（fallback 烘焙产物） | kimi 输出符号 tofu 批（2026-09-19 用户真机报「· — ✅ ✨ 这些字符也没有」；主字体（商业像素）与旧 fallback 双缺——实测：Latin-1 仅 19/96、通用标点 11/112、杂项符号 189/256、装饰符号 40/192）。病灶 = 补丁表逐字符手工维护，永远追不上使用域；修 = 借字从「清单制」升「整段制」：borrow 增 f_cmap 跳过守卫（源字体已有码点不借，整段借字的安全前提——原代码无条件覆盖会把像素字形顶成 DejaVu 风）+ cells="auto"（EAW 逐字定格宽）；三段整借（Latin-1/通用标点/货币符号 + ⚠，DejaVuSansMono→DejaVuSans 双级补漏 122+52 个）+ emoji 29 个高频（Noto Emoji 黑白矢量捐体，OFL）。缺口边界：零宽/格式控制符与极冷门货币不补（本就不该有墨）。「完全补上」的诚实口径 = 覆盖使用域全符号；真彩色 emoji 与单色像素管线不兼容不做。烘焙坑：€（U+20AC）不在通用标点段在货币符号段（0x20A0-20BF），首轮漏段补判卷抓出。产物 1.51→1.53MB（BAR-020 体积闸 8MB 余量足）。挂账：tofu 反哺机制（trace tofu 目击持续收编进借字段）只做账本一句，不做实现 | V | 已修（一钉：spec_字体_符号全域补丁——目击四字符+三段代表+emoji 29 全查字形在、汉字不损、光栅有墨双路；变异抽检实咬：指向未烘焙旧字体（git HEAD 版）→红→换回新字体→绿） | tests/termview_spec.rs `spec_字体_符号全域补丁` |
| BAR-114 | android_app（LowerCursor 层合成位闸） | 组件池跳框被下池三级框光标行压盖（2026-09-19 用户实机目击：「上池点击组件跳出内容，下池的三级框行会叠加在跳的内容上」）。病灶 = BAR-096 拆层的盲区：下池光标层合成序在配置槽之上，而跳框模态只画进配置槽画布（槽内最后画=槽内最上）——槽外还有光标层，模态被压。修 = 模态在时光标层不画（合成位闸 cs.modal.is_none()，光标位置块整段跳过含 cursor_old）。契约：跳框模态 = 配置页最上层，任何拆层件的合成位都必须问模态在不在 | V | 已修（判卷 = redroid 截屏：组件池跳框开时画面无光标框残留）；**形态升级（2026-09-21 晨班会审计：段落级 → 代码守卫）**——check-modal-cursor.sh 源码守卫：每处 lp.cursor/cursor_old 合成位赋值上方 25 行必须有 modal.is_none 闸，调用点全灭不许静默；变异两咬全中：闸整段摘除→红、is_none 反转 is_some→红；挂 chain 第 9 步 | scripts/check/check-modal-cursor.sh + scripts/test-bar114-modal-cursor.sh |
| BAR-115 | android_app（about_to_wait 解析页靠泊闸/parser_attach 静默臂） | 解析页会话表永久陈账（2026-09-19 redroid 实拍定罪：服务器侧 `tmux new-session` 加了 4 个 aa-t* 会话，关页重开三次列表恒显 4 条）。病灶 = 开页自动刷闸只吃 `Status::Idle`（一次性首查）——Ready 后重开页不再列，「开页/操作后自动刷」设计口径（刷新钮退役的凭据）实际只剩「操作后」；服务器侧 tmux 增删永远看不见。修 = 靠泊**上升沿**触发 refresh（parser_docked_prev 记上一圈，边沿 + Idle 两路并闸，exec 在途闸在 refresh 内不动）。连带补两个静默死点（排障纪律：点行零响应必须有声）：attach 的 cfg None 臂（原裸 return 零留痕）与活跃非远程臂补 report。契约：面板的「开页自动 X」闸，触发条件必须覆盖**每一次**开页，一次性闸不配叫自动刷 | L | 已修待判（**挂账理由（2026-09-21 晨班会审计拍板不升形态）**：契约「回 X 语义必须落到连接状态」横跨重孵/清网格/附着牌/收页四个调用点，grep 级源码守卫只能断章取义（单点关键词在，编排语义仍可能错）；有效判卷已在行为层——redroid 全链实录（脱离→干净 shell→DETACH_OK_42 无残字→聚焦框灭→再 attach 回 tmux），语义级钉留在判卷案层，源码守卫挂了也是摆设）→redroid 判绿（2026-09-19 实证：8 会话场重开页 logcat 实时「会话表 8 条」+截屏 6 框 3×2+分隔线；垂直拖拽滚出 nz/psh、aa-t1/2 裁断正确，list_clip/scroll 链路同验。弯路实录：前三次判「仍 4 条」全是假证据——screencap 画框冻结（三张跨重启字节级相同）+ 页已开状态再滑开手势无边沿，教训已编码进排障手册速查表） | tests:na（壳状态边沿胶水，钉豁免） |
| BAR-116 | android_app（parser_attach 已附着臂）+ parser_page（DIVIDER_ZONE） | 「点已附着会话 = 回服务器命令行」初版只收页不脱离（2026-09-19 用户真机实报：点聚焦框后「并没有回到服务器的终端」+「聚焦框该灭」）——附着牌没动，终端里还是 tmux 会话，语义只做了一半（nz 同款的正身是**脱离**）。连带第二病灶：脱离换裸 shell 后旧 tmux 残帧混入新输出（redroid 实拍：`DETACH_OK_42` 拼旧残字「e lines…」）——裸 shell 不像 tmux attach 会全屏重画，换内容必须先清网格。修 = ①裸 shell 重孵远程（command None = kfmv4 terminal-pty 交互 shell，协议面实证）+附着牌 None（聚焦框灭）+收页；②横幅前先喂 `\x1b[2J\x1b[H` 清网格；③attach/脱离重孵工序抽公共段 respawn_remote_with（同源防分叉）。分隔线顺手修：DIVIDER_ZONE 1 格→2 格，线体上下各留 1 格净空（用户同日拍板）。契约：「回 X」类语义必须落到**连接状态**上，只收 UI 不算回 | L | 已修待判→redroid 判绿（2026-09-19 全链实录：点聚焦框→logcat「脱离回服务器 shell」+清屏后干净 shell 提示符+注入 `echo DETACH_OK_$((40+2))` 回 `DETACH_OK_42` 无残字；重开页四框全暗无聚焦；再点框 attach 重孵回 tmux 正常） | tests:na（壳重孵编排胶水，B 档钉豁免）；**形态挂账（2026-09-24 晨班会）**：语义级契约（「回 X」必须落到连接状态），无机械判据可写——判卷靠 redroid 全链实录已做，形态止步段落 |
| BAR-117 | tunnel（usable/usable_edge_kick 纯函数）+ android_app（poll_tunnel_kick 两腿 + on_slot_dead/Opened 置脏） | 隧道恢复不回头踢壳层（2026-09-20 接管终判现场定罪：退役 na-tunnel.sh 后看门狗接管全序列绿——spawn→Starting→Up 35s 内完成，但壳层 remote_dead=true 卡死字节冻结）。病灶 = 重孵链**纯死亡事件驱动**：隧道断→ws reset 死亡→重孵→撞 TCP refused（隧道还没拉起）再死→距上次 0.3s 被 5s 时间闸压住→之后**再无死亡事件 = 链断**，隧道 Up 无人通知壳层。修 = 两腿续链：①隧道「不可用→可用」上升沿踢活跃死会话重孵（usable_edge_kick 纯函数：Up/ExternalUp 都算可用；稳定在线不踢防风暴；活会话不踢）；②够钟续链腿——活跃死会话且距上次重孵 ≥5s（同一把 auto_respawn_due 闸）即再踢（redroid 判卷追加定罪：TCP refused 百 ms 返回永远撞闸，持续断网期链必断，死亡驱动救不了「最后一次失败」）。连带：on_slot_dead/Opened 翻 session_over 必须置脏（不置脏帧不来，断线卡永远不画/不灭）。契约：死亡驱动链会断在「最后一次失败」上，恢复沿+够钟两条腿才是完整续链；状态→视觉依赖必须显式置脏 | L | 已修待用户终判（钉一言七断言+变异实咬：prev_usable 取反→红→复原绿；**redroid 判绿 2026-09-20**：掐报表接力造真断线→续链腿每 5s「死会话续链: remote 够钟重孵」链不断；接力恢复→下一拍敲门→会话 opened 自动接回，全程零敲键） | tests/tunnel_spec.rs `spec_bar117_隧道up沿_踢活跃死会话重孵` |
| BAR-118 | na-server（state/wsterm） | health `idle_s` 名不副实（2026-09-20 服务卡上机显形：用户问「s15/s17 闲 24s 是什么」——一直在打字的会话也显示「闲 5m」）。病灶 = build_health 借 `opened_epoch_s` 充数，`idle_s` 实为**会话年龄**冒充空闲。修约「真空闲」：SessionRec 增 `last_active_epoch_s`，wsterm 在 Input（敲键）/Out::Data（产出）接线 `registry.touch`；idle_s = now − last_active。契约：health 面的字段语义必须与字段名一致——显形即修约，名不副实的字段不许活过它的第一个消费者 | L | 已修待判（钉五题全绿+变异两枚实咬：idle_s 改回年龄→红；touch 空操作→红） | crates/na-server/tests/health_spec.rs `spec_health_idle_是真空闲` / `spec_registry_touch_刷新活动` |
| BAR-119 | parser_page（layout 保底/卡高）+ android_app/termview（解析页 inset 链） | 解析页纵向压力全由 tmux 卡独吞（2026-09-20 用户真机报「tmux 会话窗口被压缩了，最小也得有两行四个」+「软键盘召唤出来再召唤解析页，tmux 卡显示不自然，整个行为错位」；redroid 截屏定罪：键盘在场 tmux 卡塌成薄片、卡头文字裁断、下方卡顺延挤压）。病灶两臂：①layout 卡高 `content_h.min(area.h)`——面积一缩可见行塌到 1 行、卡成薄片（tmux 卡是全页唯一纵向泄压阀，下方 conn/svc/sys 三卡的固定/动态高全从它身上扣）；②bottom_inset 吃 `chrome_inset()`（键盘 inset 采样值）——键盘一弹面积直接塌，与终端网格「键盘只盖不重排」红线两张皮。修 = ①MIN_VISIBLE_LINES=2 保底两行四框 + 卡高 = 内容账全价不钳池区（超池出屏归键盘遮盖/下方卡顺延）；②解析页 inset 链全摘键盘（5 处命中/几何臂 chrome_inset 摘除 + paint_parser_content 形参改 bar_inset 只收 bar_h——ime_bottom_px 未弹恒 0，无键盘零行为差）。契约：tmux 卡可见行保底两行四框，任何纵向压力不许吞；解析页永不吃键盘 inset（与终端网格同红线：键盘只盖不重排） | V | 已修（钉 `spec_bar119_纵向挤压保底两行` 四言：挤压极端相保两行/卡高全价账/一行不强撑/三行保两行+第三行可滚达；变异两咬全中：保底退化→红、卡高钳回池区→红——首次变异静默空转（fmt 折行后 python 串替换找不到目标 = 假变异），替换前必须 assert old in s；**redroid 判绿 2026-09-20**：键盘在场解析页 tmux 卡完整 2×2 四框+按钮带无塌无错位、键盘只盖页底，无键盘常态四卡纵排照旧） | tests/parser_page_spec.rs `spec_bar119_纵向挤压保底两行` |
| BAR-120 | android_app（GLES 5708/softbuffer 4613 解析页壳 inset） | 解析页外框与内容两把尺（2026-09-20 用户真机报「召出键盘后，外部框架卡回缩了，但里面的内容却没有，内容超出了卡片」；redroid 截屏像素扫描 + logcat 几何遥测定罪：页外框环吃 bottom_inset=ime+bar_h——键盘在场环底停 1638，环境卡（1279+414）被环底边腰斩；内容吃 bar_h（BAR-119 红线只盖不重排）——卡内容恒到 1927，框与内容两把尺，键盘在场框底边悬在卡半腰）。病灶 = BAR-119 修内容 inset 链时漏了壳这一笔——内容摘了键盘，壳还吃着。修 = 解析页壳（GLES 烘焙/softbuffer 兜底两产品路）与内容同吃 bar_h：框底与内容同缘（恒 2340），键盘遮盖对两者同线一致。契约：同一页的壳与内容必须吃同一份 inset——「只盖不重排」是页级红线，不是内容级 | V | 已修（钉 `spec_bar120_解析页壳内容inset同源` 源码守卫：两产品路 paint_parser_page_chrome 实参必须 bar_h、不许 bottom_inset；变异两咬全中：GLES 点回潮→红、softbuffer 点回潮→红。考题坑：中文注释多字节，按字节切窗切穿 char 边界 panic——逐行扫到闭合；redroid 判卷见提交随行） | tests/parser_chrome_spec.rs `spec_bar120_解析页壳内容inset同源` |
| BAR-121 | parser_page（visible_bottom）+ termview（pclip 内容裁剪带底） | 页环底缘线被卡链内容盖掉（2026-09-20 视口化热更后用户真机报「层级不对，卡片的下沿应该压过内容，现在内容把卡片下沿盖住了」；定罪：visible_bottom 返回页环底缘**线本身**（fy1=屏高−inset−MARGIN=1622），内容裁剪带底 pclip.1 同吃 1622——内容可画到 y=1621，正好盖掉页环底缘 3px 边线（1619..1622）；redroid 判卷盲区实录：只扫了角弧区（x<500，环境卡左缘 x=61 够不到故角弧可见），中段框线被环境卡内芯盖掉的区没扫到，用户真机一眼看到）。修 = visible_bottom 多减 AI_PAGE_FRAME_W（=3）——单一源：滚动窗 scroll_max 与内容裁剪带底都从它出，改一处两路齐修；修后 vbottom=1619，内容止于框底内缘、页环底边 3px 全程压内容（满滚环境卡底贴环底正上方，scroll_max 305→302）；无键盘零行为差不变（链底 1927 < 新 vbottom 2301 时 scroll_max 恒 0）。契约：**框压内容是层级律**——内容可视底必须让出页环底缘厚度，裁剪带底压在环缘线上 = 层级反；redroid 框线判读必扫中段不可只扫角弧 | V | 已修（改钉 spec_视口化_可视底公式 增 W 项；变异实咬：回潮不减 W→钉红→复原绿）；redroid 判绿：满滚/回滚两态环底线 y=1619..1621 中段 1100/1100 像素连续、顶缘完好、横滑无回归——**用户肉眼终验通过（2026-09-20「可以，结案吧」）结案** | tests/parser_page_spec.rs `spec_视口化_可视底公式` |
| BAR-122 | scripts/cases/PIN-boot-accept.sh（boot 段解析内核）+ scripts/test-na-regress-meta.sh（元契约②复刻内核） | PIN-boot 误报「启动族回潮 4033ms」（2026-09-20 BAR-121 热推冒烟现场定罪）：热更重启后 na 被挂起（用户切走/灭屏），boot 段内 +3023「death] suspended」→ +4008 回前台，段末行「GLES present 后端上线 +4033ms」的毫秒戳从进程启动累计，**混入了挂起期墙钟**——表面重建实际只花 4008→4033=25ms，无码病。与 BAR-075 熄屏冻结（STALL beat_age 证物）同族：启动段被系统中断的环境产物。修 = 解析内核增挂起证物：段内见「death] suspended」→ SUSP → 跳过(77)不判（「判不了就不判，不把环境账记到码上」同哲学）。契约：boot 段毫秒戳只在**连续前台**的段里可作启动劣化证据；段内含冻结/挂起中断证物一律跳过 | L | 已修（钉=元契约②新增两样本：挂起段出 SUSP/旧 boot 挂起不污染新 boot；变异实咬：摘 susp 行→挂起样本误判 4033→复原绿） | scripts/test-na-regress-meta.sh 元契约②（boot_core 复刻内核挂起两样本） |
| BAR-123 | android_app（快捷键行抬手 hit 调用点） | 键盘弹起态快捷键行抬手全落空（2026-09-20 深夜用户真机报「ctrl 键没用了，无法触发那个快捷键」——Ctrl-] 会话切换按不出）。遥测定罪链：field-reports 实录 inset=890（键盘弹起）时用户连点 (266~292, 1624~1651) 8+ 次全报「快捷键行抬手」+「命中落空」成对出现，inset=0 时全部命中（09-11 以来无一反例）；数值闭环：渲染 CTRL 中心 = 2800−890−220−240+180 = **1630** = 用户实际瞄准点——眼没看错，是抬手尺错了。病灶 = BAR-119（7018c1a）摘解析页 inset 链「5 处 chrome_inset 摘除」时误伤快捷键行抬手 hit 调用点——Started in_bar 与渲染仍吃 chrome_inset+栏高，抬手 hit 被摘成只吃栏高 220：按下认、抬手丢，键盘弹起态行带在 2340 而用户点 1630 必然落空（git log -L 铁证该行 diff）。修 = 抬手 hit 恢复 chrome_inset()+cur_bar_h() 与 Started/渲染同源，落空诊断行补印栏高（inset=890+220 两把尺一眼对账）。契约：**快捷键行按下/抬手/渲染三处必须同一把尺**（chrome_inset+栏高）——「眼手同尺」升级为「三处同尺」；摘 inset 链是页级手术，键行是浮在键盘上的 chrome，永不在摘除名单 | L | **用户肉眼终验通过（2026-09-21「我看了，效果是好的」——键盘弹起点 Ctrl-] 切换正常）结案**（钉=源码守卫 check-keybar-ruler.sh：android_app.rs 每处 keybar::hit/in_bar 实参必须含 chrome_inset，调用点全灭不许静默；变异两咬全中：抬手 hit 摘除→红、Started in_bar 摘除→红；accept 案 BAR-123-accept.sh 遥测自校正判卷：唤键盘→探针拿真 inset→算 CTRL 渲染中心点按→粘滞位 001→灭灯 000） | scripts/check/check-keybar-ruler.sh + scripts/test-bar123-keybar-ruler.sh + scripts/cases/BAR-123-accept.sh |
| BAR-124 | android_app（switch_session resize 补发）+ session_router（jog_resize 纯函数） | Ctrl-] 切换后页面是对端残影（2026-09-21 用户真机报「按那个快捷键应该刷新一下页面的，不然无法正常用了」；遥测实锤切换动作本身成功——BAR-123 修复已生效，残的是屏）。病灶 = 双会话共用一个终端网格、无每会话屏幕快照：切回时只靠待机 replay 补屏（帽 256KB），**静默待机 replay 为空 → 整屏还是上一个会话的画面**；对端全屏重画的唯一通用扳机是 SIGWINCH，而同尺寸 Resize 净变化为零、内核不发信号（补发同尺寸 = 白发）。修 = resize 抖动：切换时先发 rows-1 抖动尺寸、武装归位账，主循环 120ms 后归位原尺寸——两次净变化 tmux 必收 SIGWINCH 全屏重画（间隔必须：conn.rs Resize 只留最新值，背靠背发被合并成原值；归位那发兼原「补发当前网格尺寸」之职，待机期尺寸变过也纠回）。真 resize 到达即作废抖动账（归位值是按旧网格武装的，且真 resize 自带 SIGWINCH）。已知边界：raw shell 无重画协议只能重印提示行——每会话网格快照是根治欠账（挂账，不在本案）。契约：会话切换必须给对端一次尺寸净变化；「补发同尺寸 Resize」当刷新扳机 = 回潮 | L | **用户肉眼终验通过（2026-09-21 同上——切回页面立即刷新正常）结案**（钉 `spec_bar124_抖动尺寸_减一行保底` 四言：减一行/列不动/1 行不抖成 0/0 行兜回 1；变异实咬：减 2 行保底 2→钉红→复原绿；**redroid 判绿 2026-09-21**：logcat 设备时戳三轮往返逐对干净——抖动 64x56→64x55 后 165~209ms 归位 64x56（120ms 账+泵粒度）；服务器侧实捕 redroid 的 tmux client pts/7 尺寸 64x55 闪变后归 64x56（SIGWINCH 链端到端通）；切回后 CPU 截屏 = 当前 kimi 会话内容无残影。判卷盲区挂账：field-reports.log 时间戳是接力到达时刻非设备时刻（同事件重复行/乱序皆是接力 artifact，计时判读只信 logcat）；redroid local 无 tmux 二进制造不出「静默待机 tmux」真残影场，终验归真机两会话 tmux attach） | tests/session_router_spec.rs `spec_bar124_抖动尺寸_减一行保底` |
| BAR-125 | sess_mode（新纯模块）+ android_app（switch_session 换-mode/清屏）+ termview（mode_bits trait 面） | 切回本地后屏幕残留服务器 tmux 内容 + 上下滑跳「手势操作符乱码」（2026-09-21 用户真机报：Ctrl-] 或设置卡切换都一样）。定罪（代码链+复现限制实录）：①乱码 = SGR 鼠标滚轮序列 `\x1b[<64;列;行M`——服务器 tmux（mouse on）开的鼠标上报残留在**共享仿真器 mode** 里（切换只换路由/补屏，模式不换），本地裸 shell 上滑手势走 BAR-016② 路径被翻成滚轮事件发本地 pty，shell 没开鼠标模式原样回显；②屏幕残留 = 单网格无每会话快照 + 裸 shell 无重画协议（BAR-124 已记的边界同根）。修 = **每会话模式快照**（用户拍板「切换后直接自动清屏」）：切出存 mode bits，切入先复位受管模式（?1/1000/1002/1003/1006/2004/1049，?1049l 打头先回主屏）+滚动区/光标复显兜底+`\x1b[2J\x1b[H` 清屏，再恢复切入会话自己的快照（无快照=全干净，裸 shell 天然如此）——tmux 快照带鼠标上报，切回远程滚轮翻译不丢（不依赖 tmux 重发模式）。契约：**终端壳模式（鼠标/alt/应用光标/粘贴/滚动区/光标显隐）是按会话分的账，切换必须换账**；LINE_WRAP 等双端一致的不进快照面。判卷限制实录：redroid local shell 本来就坏（CANNOT LINK arm64 翻译链），乱码回显场造不出——机制链由钉+遥测锚（模式快照 bits 对账）覆盖，乱码消失/清屏/可直输的终验归真机 | L | **用户肉眼终验通过（2026-09-21 同上——切回本地清屏干净、滑动乱码消失、可直接输入、切回 tmux 滚轮正常）结案**（钉六言：alt 打头/干净态零 DECRST/恢复只设快照位/往返清了必能设回/LINE_WRAP 不进面/快照源 mode_bits 随喂入真实变化；变异两咬全中：恢复 h→l→红、摘 alt 打头→红——首跑钉自咬出钉错：往返题 cur 漏 MOUSE_MOTION 位，钉修不是答案修；**redroid 判绿 2026-09-21**：遥测锚「模式快照」实捕 remote=0x2b0b6（含 MOUSE_DRAG/SGR_MOUSE/ALT_SCREEN/APP_CURSOR——泄漏现场 bits 实锤）存账 → local=0x0000 首切全干净恢复，往返后 remote 快照原样 0x2b0b6 回账；切入 local 截屏 = 干净屏（唯横幅+本地自身输出，服务器 tmux 残影全消）；乱码回显场 redroid 造不出（local shell CANNOT LINK arm64 翻译链），乱码消失/可直输归真机终验） | tests/sess_mode_spec.rs 六枚 |
| BAR-126 | 服务器 sshd（配置缺 ClientAlive）× tunnel.rs（stderr 灭口）× 重启链（多例让位） | na 反复断连重试死循环（2026-09-21 用户真机报「na 现在反复断连重试」；遥测定罪：`[term] 会话 failed: TCP 连接失败(127.0.0.1:9021): Connection refused` 与「会话重连: remote 重孵」成对刷屏，tunnel 看门狗 22 次退避重拉全 exit 255）。病灶三段链：①13:48 热更重启多例让位不收敛（loader-pick 3 秒 3 次 android_main），旧实例暴毙时 TCP RST 被运营商 NAT 吞掉，服务器侧 sshd 会话成**僵尸**（ESTAB 12 分钟+recv-q 374KB 无人读）——sshd 默认 ClientAliveInterval=0 永不收割；②僵尸霸占 -R 9022 监听口，新自持 ssh 每次 `remote port forwarding failed for listen port 9022` → ExitOnForwardFailure → exit 255（-L 9021 陪葬）→ ws 断连重试；③tunnel.rs spawn 时 stderr 指向 Stdio::null——ssh 死因被灭口，排查被迫手动沙箱复跑才见真凶（挂账：stderr 应落文件）。修 = 服务器 kill 僵尸 sshd 会话 + sshd_config 落 ClientAliveInterval 30/CountMax 3（90s 无响应自动收割，reload 不掉线，.bak-kfm-126 留档）。契约：**反连监听口的生命周期必须有应用层心跳兜底**——TCP ESTAB 在移动网络下不等于活着；僵尸占口 + ExitOnForwardFailure = 永久死循环。排查弯路实录：先误判「多实例抢 -L 9021」（field-reports 多心跳序列交错），实则为 redroid 冷启动 android_main 一秒多进（×3 重复行 = 同秒起的多个计数器锁步；另立案 BAR-127）+ 手机多例残留——**field-reports.log 是多设备混流的**，判读先分设备再分进程；多例在 Termux uid 无 force-stop 权限时只能请用户系统设置强停 | I | **用户终验通过（2026-09-21「可以结案了」）结案**（僵尸已杀、9022 释放、自持隧道 Up 稳定、ws 无新增失败行、9022 探针通；sshd 加固已 reload——运维修复无码钉，tests:na） | —（tests:na，服务器运维修复）；**形态挂账（2026-09-24 晨班会）**：可机械化部分已分别升级（心跳兜底 = sshd ClientAlive 运维配置落盘；混流判读 = BAR-134 升数据自带；撞口自愈 = BAR-129 码钉）——剩余为服务器 sshd 配置，不入 chain |
| BAR-127 | na-loader / android_main 入口（多实例）× 新 `src/singleton.rs` | `android_main` 多实例（2026-09-21 夜 BAR-126 收尾立案：redroid 冷启动一秒 4 条「android_main 进入」；**同夜手机侧抓到同一病理的另一面**——封掉探针在途相发布（BAR-130）后日志里 `nasup 状态 ExternalUp → Checking` 仍以 1~3 秒间隔出现，而新代码根本发不出这种行 = **旧 .so 实例还活着**在各自跑看门狗）。后果 = 子系统按进数复制：多套隧道看门狗**互抢反连口**（BAR-128 抓到的 `remote port forwarding failed for listen port 9022` 真因之一 = 用户「反复连接反复断开」的最后一块）、多套 nasup 看门狗（状态词同秒多次迁移）、多份心跳锁步污染遥测。BAR-037 的 `ANDROID_MAIN_RAN` 只管**同进程重跑**，管不到多进程。修 = **flock 独占单实例闸**（新 `src/singleton.rs`）：锁文件在应用私有目录、进程死内核自动释放（从构造上不存在陈旧锁卡住下一次启动），第二实例写遗言（指名持有者 pid）后立刻 `exit`——闸卡在任何子系统起跑之前（看门狗/心跳/EventLoop 全在其后）。契约：**进程级唯一性必须由内核托管**（flock 随进程生死，不靠 pid 文件/时间戳自证） | I | 已修（钉两言：`spec_单实例_第二把必败_松手必胜`（第一把必得/第二把必败/锁文件写持有者 pid）+ `spec_单实例_子进程死后锁自解`（**真进程死**后后人必能抢）；诚实边界：同进程 drop→再抢 在 workspace 并行负载下不稳，不设为判据——生产语义是进程死，由子进程题判卷。**真机验证待手机隧道恢复**（多实例残留清空 → 日志应回到一分钟一次状态迁移） | tests/singleton_spec.rs 两枚 |
| BAR-128 | tunnel.rs（死因仪器 + 抖动退避）+ svc_health.rs（失败上报限流） | na 隧道反复重拉（2026-09-21 夜用户真机报「为什么会出现反复连接反复断开？」，与 BAR-126 同症状的另一半账）。现场实录（field-reports.log）：`[tunnel] ssh 进程退出（ExitStatus(65280)）第 1~20 次退避重拉` 成串（12:42-12:51 九分钟爬 20 次、13:20-13:22 三分钟 8 次），每次 spawn 后 1s 内即死；伴 `[svchealth] 连接失败: Connection refused (os error 111)` 刷屏与 `[alert] 会话 5 分钟内死亡 3 次`。服务器侧取证：sshd 加固已生效（ClientAliveInterval 30/CountMax 3）、9022 由手机自持 sshd 持有（非僵尸）、但**两条到手机的 ESTAB 连接 Send-Q 268KB/157KB 长期积压**（服务端发不出去 = 手机侧 ssh 被 Android 冻结/网络切换，客户端不读）——BAR-126 的僵尸病换了方向：不是服务端不收割，是**手机侧传输在系统冻结下断流**，重拉又撞上「上一轮会话尚未被收割（90s 窗）」或本地口未释放 → 秒级 255 → 每 2s 一次的热循环。两处病灶：①**stderr 灭口**（BAR-126 挂账「stderr 应落文件」未做）——`Stdio::null` 让「为什么断」永远是猜；②**退避归零语义错**——原先「一见 Up 就 attempts=0」，抖动网络下退化成一失败就 2s 重拉（退避表形同虚设）。修 = ①stderr 上管道 + 读线程末 4 行环，退出行落真因（认证/撞口/拒连/keepalive 超时）；②`next_attempts(prev, lived_secs)`：娃活过 STABLE_SECS=30 才算真连接（回 1），短命娃（含一 spawn 即死）**计数续涨** → 退避爬 2/5/10/30s；③svchealth 失败上报限流 60s/面（隧道断时每 2s 撞一次，不限流则报表信道自身变负载源、真事件被淹）。契约：**退避计数只认「活了多久」，不认「连上过」**——抖动期的每一次短命成功都不许把热循环重置；**传输进程的死因必须可读**（灭口 = 排查被迫重演现场） | I | 已修（钉 `spec_抖动_短命退避不归零` 含退避表封顶；仪器面：stderr 环 + 退出行真因；**真因待下一轮实录填表**——本修把「猜」换成「读」） | tests/tunnel_spec.rs `spec_抖动_短命退避不归零` |
| BAR-129 | tunnel.rs（反连口自愈释放） | 「回到 na 就反复掉」（2026-09-21 夜用户二轮陈述：开机/切后台回到 na 就反复断联，手动点重连也照样掉）。机制（BAR-128 仪器面 + 服务器侧实测闭环）：手机退后台/灭屏/进程被杀时，服务端那条 -R 反连会话**在 ClientAlive 收割窗（90s）内仍占着本设备反连口**（实测 9022 的监听者 pid 每几分钟换一个：2985040→2963398→…，正是每轮重拉的痕迹），na 重拉 ssh 撞口 → `ExitOnForwardFailure` → 秒级 255 → 再退避再撞，直到服务端自己收割——用户看到的就是这一段长达 1~9 分钟的「反复连接」。修 = **不等收割，主动释放**：ssh 死因里出现「remote port forwarding failed + 本设备口号」时才（且仅当）请服务器把占口的 **sshd** 收掉（`should_release_forward` A 档严判——认证失败/拒连/keepalive 都不许触发，否则会杀活会话；脚本只杀 comm=sshd，非 sshd 一律不动），释放完成前首轮退避至少 4s。契约：**每设备 10 口段的「口」即设备身份**——占本设备口者必为自己的遗留会话，可以安全释放；判据必须来自 ssh 自己的 stderr（死因即授权），不许按「255 就杀」 | I | 已修（钉 `spec_反连口_释放判决与脚本` 五言：撞本口判真/他设备口判假/认证失败判假/拒连与超时判假/脚本按口过滤且只杀 sshd；**真机自愈实录待下一轮抖动**——仪器+自愈同批上机） | tests/tunnel_spec.rs `spec_反连口_释放判决与脚本` |
| BAR-130 | na_server_sup.rs（探针在途相发布） | 服务卡状态词每 15s 翻一次「自持在线 ↔ 确认中」（2026-09-21 夜用户三陈述「我就算在 na 客户端里，也会看到它反复地重连，哪怕手动点了重连它也掉」）。现场：field-reports 里 `[nasup] 状态 ExternalUp → Checking / Checking → ExternalUp` 在同一秒里迁移数次（多条实例报告交错），而服务器 auth.log 显示每 15s 一次新 ssh 登录（39.144.203.45 连续源口 12142..12149）= ensure 探针 cadence——**探针本身没病，是它的「在途相」被发布成了客户端状态**。病灶 = 看门狗每轮 `set(SupState::Checking)` 进快照 → 服务卡状态词如实显示「确认中」→ 15s 一跳，用户读成「反复重连」（他的会话其实一次没断，9022 自持隧道由另一条 ssh 持有）。修 = `publish_state`（A 档纯函数）：在途相**不发布**，只发布结果相（Up/ExternalUp/Down/TunnelDown）；初始相改 Checking（首探前「我们还不知道」是真话）。契约：**探测过程不是状态**——客户端的连接状态词只许随真实结果翻，探针节拍不许变成卡面节拍 | V | 已修（钉 `spec_状态发布_在途相不跳出卡面` 六言：在途不发布/结果照发/首探前保持/失败相不被盖/隧道相照发/状态词不变；手机已热更） | tests/na_server_sup_spec.rs `spec_状态发布_在途相不跳出卡面` |
| BAR-131 | src/singleton.rs（残留实例自清） | 「打开 na 得反复重启才勉强能用」（2026-09-22 用户三报；取证：手机侧 `单实例让位` 计数 0 = **还没上新核**，而 `nasup 状态` 仍 1~3s 一跳 = 多实例仍在）。病机：单实例闸只挡**新**实例，旧核留下的实例继续抢反连口、刷状态；**而每次重启又养出一个新实例**——越重启越乱，正是用户体感。修 = 拿到锁之后、任何子系统起跑之前，扫 /proc 清**同 uid 的其它 na 进程**（判据三严：同 uid / cmdline 含包名 / 非自己；SIGKILL 收——旧实例可能已被 ROM 冻结，TERM 未必被处理）。**自测实咬一记**：判据在**宿主**上会命中「跑测试的外壳进程」（cmdline 恰好带着包名字符串、uid 又是同一个 root）——当场把自己的 shell 杀了；故清场**安卓专属**（target_os 判据 + 包名非空双闸，非安卓一律空转）——KILL 管辖权只在「同 uid = 同一 app 沙箱」的世界里成立 | I | 已修（钉两言：判据三严（含三类反例）+ 宿主空转（非安卓/空包名零动作、永不含自己）；真机验证待热更后看 `清残留实例 N 个` 行与心跳单序列） | tests/singleton_spec.rs 两枚 |
| BAR-132 | session.rs（重孵闸）× android_app（两处调用点）× tunnel.rs（释放免退避） | 「正常使用时突然掉线、然后反复跳重连」（2026-09-22 用户四报，附「也许你能看到我刚遇到的日志」）。**仪器当场给出两条真因**（BAR-128 的 stderr 面）：①`活 62s/102s/23s → stderr: Connection to 8.145.46.182 closed by remote host.` = **服务器主动断**（App 在后台/灭屏被 Android 冻结 → ssh 子进程不回 keepalive → 服务端 90s 判死踢掉）；②`活 1s/2s → Error: remote port forwarding failed for listen port 9022` = 重拉撞上一轮残留口（自愈已收，`released=…`）。③用户看到的「反复跳」是第三条：**隧道断着时会话仍在每 5s 空转重孵**（05:47:03→29 六次 `会话 failed: 9021 Connection refused`）——必失败的动作在刷屏。修 = ①`session::auto_respawn_allowed`：远程会话**必须隧道可用**才许重孵（恢复交「隧道可用沿」那条腿，BAR-117 已有）；本地会话不吃这条；②`tunnel::retry_wait`：**释放成功 ≈ 口已腾** → 只等 4s 再试、退避账归 1（实测那次断到恢复 29s，其中 30s 是爬升退避白等；别的死因照退避表，防抖不变） | I | 已修（钉两言：`spec_bar132_远程会话重孵须隧道可用` 五断言 + `spec_bar132_释放成功即免退避` 两断言） | tests/session_router_spec.rs + tests/tunnel_spec.rs |
| BAR-133 | tunnel.rs（预防式释放反连口） | 「突然掉线、等三个提示才接回来（十几秒）」（2026-09-22 用户五报，附「它怎么会自己莫名断线」）。取证两段日志：①手机侧 12:43:22 `ssh 退出（活 5s）stderr: Connection to 8.145.46.182 closed by remote host.` → 12:43:33 `会话 opened: s221` = **11 秒**（与用户数的「三个提示」吻合）；②服务器 auth.log 同刻只有 `Accepted publickey ... 39.144.207.47` 与 `disconnected by user`（后者是我们自己因撞口退出），**没有优雅断开记录** = 那条 TCP 是中间设备 RST 掉的。真因 = **手机移动网换 IP**（公网 IP 实录：09-20 `.41` → 09-21 `.112` → 09-22 `.47`，同日内也见 `.44→.45`）——换小区/换 APN 时运营商丢所有既有 TCP，长连接必死（物理层，防不了）。**这 11 秒里有一跳是纯浪费**：重拉撞上一轮残留反连口（等 90s 收割窗）+ 自愈在第二次失败才触发。修 = `should_release_port`：**死前转发绑过（established）就预防式释放**（服务器那条会话此刻已无主，占着口只会让下一轮白撞）；没绑过且非撞口死因一律不动（不许误杀活会话）。恢复预期 11s → 4~6s | I | 已修（钉 `spec_bar133_死前绑过就预防式释放` 四组断言：绑过即释放/撞口也释放（原路保留）/别的死因不动/他设备口号不管） | tests/tunnel_spec.rs |
| BAR-134 | report.rs（行尾设备/实例标识） | field-reports.log 多设备混流判读两度误判（BAR-126「多实例抢口」实为 redroid 锁步行；BAR-127/128 期手机/redroid 行交错分不清谁是谁）——根因 = 行数据本身不带出身，分道全靠人脑记设备节奏。修 = 三条上报路径（report/report_sync/report_sync_once）经单一源 `line_json` 给 msg 末尾挂 `[arch/pid]`：arch 分设备族，pid 分进程实例；放 msg 末尾 = 落盘行尾，既有按前缀 grep 的脚本（scripts/cases/*、na-*.sh 已清点，无行尾锚）全不破。空消息不领前导空格。契约：**任何多源合流的日志，每行必须自带出身标识**——判读纪律（先分设备）挡不住的，让数据自己说话 | L | 已修（钉两言：标识形态+真 pid/真 arch+空消息不豁免；JSON 转义链存活复钉；变异实咬：摘 stamp→两钉全红→复原绿） | tests/report_spec.rs `spec_bar134_报表行_挂设备实例标识` `spec_bar134_标识_JSON转义后仍存活` |
| BAR-135 | 新 src/offline_keys.rs（暂存队列）× android_app（route_input 出向唯一入口/kick_reconnect 隧道闸/Opened 回冲/蓝横幅退役/断线卡 sig 加状态行两维）× down_card（status_text 纯函数）× termview（render_down_card 带状态行文本） | 「断联影响最小化」设计落地（2026-09-23 用户拍板：断线不影响输入、连好把卡住的输入传过去、无蓝提示无重复触发、提示全在跳出的框上）。三处病灶同修：①隧道断时敲键 → kick_reconnect 盲孵必死 conn（每击键一条 `Connection refused` 刷屏，BAR-132 只闸了自动腿）；②闸住期击键进僵尸通道 = 丢；③重孵蓝色内联横幅污染终端正文。修 = 击键出向唯一入口 `route_input`（should_hold 纯函数判定：远程+死会话+隧道不可用 → 进 OfflineKeys 队列，64KB 封顶丢最旧记账；否则维持 kick+conn pending_input 原路——两条缓存轨不重叠无重复触发）；kick_reconnect 同款隧道闸（闸住告示沿只报一次）；Opened 活跃方即 `flush_offline_keys` 保序回冲；蓝横幅删除，告示全部落在断线状态卡状态行（status_text 三态：重连中 > 已暂存 N 字节 > 裸断开；卡 sig 加 connecting/暂存字节两维，文本变卡必重烘）。契约：**断线期的击键只有两条轨——conn pending_input（孵后 Opening 段）与 OfflineKeys（闸住段），任何击键不许落进僵尸通道；断线告示只许上卡，不许 inline 进终端正文** | I | **用户终验通过（静默判过，2026-09-26）结案**（钉五言：收冲保序/容量闸丢最旧/收编判定真值表/状态行三态优先级/转义链不相关未涉；变异三咬全中：摘容量闸→红、should_hold 放宽→红、状态行优先级倒置→红） | tests/offline_keys_spec.rs 三枚 + tests/down_card_spec.rs `spec_bar135_状态行_三态文本` |
| BAR-136 | scripts/pack-toolchain.sh（两阶段可续传改造） | 工具链快照三跑残桩：首跑 24MB、二跑 31MB、三跑 zstd 报 /root 不存在。定罪分两层：①真根因 = 8022 的 Termux 被 vivo 冻结掐死长 tar 流（传输期被杀，与脚本逻辑无关——同结构单测 9.4GB 正常）；②次生 bug = REMOTE_PACK 用 $HOME 在服务器侧展开成 /root（手机无此目录）。修 = 两阶段：手机侧落盘 tar.zst（冻结最多杀一次，重跑即续）+ rsync --partial --append-verify 断点续拉 + sha256 对账才算成；REMOTE_PACK 绝对写死。副产物定档：files-only 清单 9416 文件 1.125GB；dpkg -L 连目录列，tar 见目录递归（/data 全树 9.4GB 事故，files-only 同炉修）。契约：**跨冻结通道的大文件传输必须可续传+对账，不许赌长连接；远端路径绝对写死，不借 $HOME** | L | 已修（契约钉 test-pack-toolchain.sh 挂 chain 第 9 步：续传/对账/绝对路径/files-only 四契约 + 长流直压反模式；变异实咬：摘 append-verify→红→复原绿） | scripts/test-pack-toolchain.sh |
| BAR-137 | scripts/pack-toolchain.sh（闭包补种 libicu） | spike 3 exec 冒烟实踩：重定位工具链在 na 私有目录首跑 rustc 报 `libicuuc.so.78 not found`（libxml2 需要）——libxml2 的 control 文件 Depends 不申报 libicu，apt 依赖闭包抓不到它。修 = 闭包种子手动补 libicu（补库后 LD_TRACE_LOADED_OBJECTS 全库扫描零缺失定档）。契约：**apt 闭包 ≠ 运行时闭包——重定位产物必须在目标环境做一次 not-found 扫描才算齐** | L | 已修（契约钉加第五言：闭包补种被摘即红；变异实咬：摘 libicu→红→复原绿） | scripts/test-pack-toolchain.sh |
| BAR-138 | tunnel.rs（backoff_secs 加抖动） | 9021/9022 反复断连加固第三击（2026-09-23 用户报「9021 也反复断」）：IP 轮换风暴里看门狗各路重试同频撞口成簇（auth.log 513 次 bind 冲突实录，BAR-133 只治了「死前绑过」一路）。修 = 退避加相位抖动：jitter = 基准一半以内（2s 抖 0/1、30s 抖 0~15），首死（0s）不抖。踩坑：首版熵用 boot_ms——测试里 BOOT_T0 未种恒为 0，钉红「连采只见单值」才抓出；改系统时钟纳秒（secs^subsec_nanos）。契约：**多路重试的退避必须带抖动，熵源不许依赖运行时才有的初始化态** | L | 已修（钉 `spec_bar138_退避抖动_相位散打` 连采 40 必见 {2,3} 两值；`spec_bar132` 退避断言改区间钉 [base, base+base/2]；变异实咬：摘 jitter→红→复原绿） | tests/tunnel_spec.rs |
| BAR-139 | report.rs（SERVER_ADDR/HOST_HEADER 8021→9021） | field-reports 断更 26 分钟（2026-09-23 16:21 起）：应用活（logcat 心跳照打）、9022 隧道活（ssh 实拍应答）、手机侧 -L 9021 转发活（curl 404 实拍）——唯报表路死。定罪：报表 POST 打 127.0.0.1:8021 = 旧拓扑 Termux ssh -L 代维口，Termux 被省电冻结即断供，与 na 自持隧道无关。修 = 报表路切 na 自持隧道 9021（na-server /kfmv4 前缀别名认）。契约：**na 的每条出向通路（报表/终端/插件）都必须走自持隧道，不许留 Termux 代维单点** | L | 已修（钉 `spec_bar139_报表走自持隧道` 四口断言：9021/回环/Host 随端口/路径不漂；变异实咬：端口改回 8021→红→复原绿） | tests/report_spec.rs |
| BAR-140 | tunnel.rs（端到端探活 + 检测三件套提速） | 「孤岛也要秒回」加固（2026-09-23 用户报：Termux 全关，na 是孤岛；断联重连好几秒，要更快）。重连耗时账拆三段：①**检测**——NAT 吞 RST 的静默死，本地口探活全绿（ssh 僵尸举着监听），干等 ssh keepalive 15×3=45s 才知情；②**退避**——久活后首死 2s+jitter；③**握手**——移动网 RTT 约 1s。修三笔：a) `ConnectTimeout=5`——断网期 spawn 不许挂 75s TCP SYN 重试，5s 速败让退避表接管；b) ServerAlive 5×2——静默死检测 45s→10s；c) **端到端探活**（治本）——看门狗 1s 拍穿透本地口打 na-server `/api/na/health`（1.2s 超时认 200），连败×2 即杀娃、退避清零立即重拉、死前绑过顺路 kick 释放（免下一 spawn 白撞 255）。僵尸定罪 ≈2~3s，全程重连 ≈4~5s。契约：**任何「本地口通 = 连接活」的判活都不许单信监听态，必须有端到端探活** | I | **用户终验通过（静默判过，2026-09-26）结案**（钉 `spec_bar140_端到端探活连败_即杀即拉` 五言 + 参数钉收紧 interval=5/countmax=2/connecttimeout=5；变异实咬：首败即杀→红→复原绿） | tests/tunnel_spec.rs |
| BAR-141 | tunnel.rs（ResumeKick 回前台即审）× android_app.rs（resumed 踢脚） | 「4~5s 能不能压进 1s」之感知侧答案（2026-09-23 用户问）：真·恢复 <1s ssh 做不到（重握手就吃满 1s，唯一真路径 = QUIC roaming 挂账项），但**感知 <1s 可以**——重连抢在用户察觉之前。修 = 回前台即审：resumed() 踢 TunnelCmd::ResumeKick，看门狗健康不碰/僵尸一拍定罪（不等连败×2）/退避清零立即重拉。纯热核改动（resumed 是 Rust 事件循环自带生命周期，零 Java 零 APK 重装）。契约：**用户在场的检测判据从宽（一拍定罪），不在场从严（连败×2 防冤杀）** | I | **用户终验通过（静默判过，2026-09-26）结案**（钉 `spec_bar141_回前台即审_健康不碰僵尸即杀` 真值表五言；变异实咬：僵尸不杀→红→复原绿） | tests/tunnel_spec.rs |
| BAR-142 | tunnel.rs（STABLE_SECS 30→8 + 释放同步确认） | 用户实测三次自断恢复 20s/10s/15s（2026-09-23 晚，BAR-140 核在跑），与「4~5s」承诺不符——auth.log 逐跳对表定罪两处：①**稳定窗口 30s 太严苛**——IP 轮换风暴里娃常活 10~30s，全被判「抖动」→ 退避账爬 5/10s；②**释放盲等**——kill 后释放是异步踢+盲等 4s，撞口 round 再来一轮（spawn 撞 255→释放→4s→spawn，一轮 ~6s）；且断网期释放踢失败（Network is unreachable 实录）后仍盲 spawn 白撞。修 = a) STABLE_SECS 30→8（活 8s 以上 = 真扛过流量，回首死 2s 级；spawn 即死的真抖动照旧爬账）；b) release_forward_sync 同步核（死亡路 3s/杀娃路 2s 有界）——确认完成零等待直接 spawn（4s 盲等作废），失败 2s 重试不盲 spawn。契约：**「等一个动作完成」必须用同步确认替代固定盲等；阈值类参数要拿真实分布校准（风暴期娃寿命分布），不许拍脑袋** | I | **用户终验通过（静默判过，2026-09-26）结案**（spec_bar132 重写为 release_wait 真值表+STABLE_SECS=8+爬账/回首死边界；变异双咬：回 30s→红、回盲等 4→红→复原绿） | tests/tunnel_spec.rs |
| BAR-143 | 流程（产物路径/git add -A）× na-server/main.rs | 密钥进公网远端事故（2026-09-23 晚，QUIC 部署轮自抓）：NA_QUIC_CERT 缺省 `/root/kfm-na/certs`（**仓内**）——`git add -A` 把 quic.key.der（TLS 私钥）+ quic.psk（客户端证）扫进提交推了 gitee/github。发现即处置：amend 摘帽 + force-push 双远端 + **双证轮转**（旧证全部作废，手机尚未配置故零成本）+ 缺省路径迁 `/root/kfm-na-certs`（仓外）+ .gitignore 加 `certs/` 双保险。轮转后生产冒烟双判复过。契约：**①任何密钥/证书类产物的缺省落盘路径不许在仓内（落盘前先问「git add -A 会不会吃掉它」）；②泄密处置 = 摘帽≠完事，密钥本体必须轮转——历史在远端缓存里活得比 force-push 久** | I | 已修（路径迁移+轮转+ignore）；**2026-09-24 晨班会升形态 纯段落→代码守卫**：scripts/check/check-no-secrets.sh（密钥/证书类后缀白名单制永不进库，实咬：staged 假 .psk → 红 → 撤出复绿）挂 chain 第 1 步防泄漏闸家族 | scripts/check/check-no-secrets.sh（chain 第 1 步） |
| BAR-144 | android_app.rs（respawn_session 远程臂）× tmux_ctl.rs（respawn_attach_cmd） | 切 nz 过一阵子被拽回 kfm-na（2026-09-23 深夜用户实报）：遥测定罪——`attach nz 重孵已发` 后隧道一抖，`死会话续链: remote 够钟重孵` 拿 default_config（设置里的默认会话 attach 命令）一刀切重孵，附着账 `remote_attached` 全程没人查。修 = 重孵命令按附着账裁决：账在 → `cmd_attach(账上会话)`；账空 → 配置原命令一字不动（裸 shell 重孵语义保住）。契约：**「重连后回到哪」是会话状态的一部分，重孵命令必须由附着账裁决，不许拿配置默认一刀切** | I | **用户终验通过（2026-09-24「应该都过了」）结案**（钉双言 + 变异实咬：一刀切默认 → 钉红 → 复原绿） | tests/tmux_ctl_spec.rs `spec_bar144_重孵_附着账在_附回账上会话` / `spec_bar144_重孵_账空_原命令一字不动` |
| BAR-146 | na-quic/lib.rs（HANDSHAKE_TIMEOUT 握手速败）× tunnel.rs（QUIC_START_GRACE + leg_starting_overdue + 看门狗 Starting 超时审理） | 「打开 na 连接不上」（2026-09-24 晨用户实报）定罪链：app pid 13232 全程活着（logcat 心跳连续），field-reports 05:54 断更——QUIC 腿死于网络切换，死信审理收腿重拉，但**新腿挂在 connect().await**：UDP 黑洞里 quinn 握手要等 idle 上限（**4h**）才超时，腿线程不死 → 死信不发；本地口从没绑 → port_open=false → e2e 探活块（`if port_open`）整段跳过；回前台即审同理（`!port_open` 算 probe_ok）。看门狗在 Starting 里 1s 一拍空转 **1 小时**无人定罪（06:52 遥测：app 活、本机 9021 curl rc=7 拒连）。修 = 双保险：①run_client 握手包 8s timeout 速败（死信恢复送达）；②看门狗 Starting 超时审理——腿对象在但口超 12s 宽限没开即主动定罪收腿重拉，跳闸账照记满 3 降级 ssh 兜底。契约：**「对象在 = 活着」不许单信——凡是「起不来」的形态（端口未开/信号未来）必须有超时定罪路径；宽限 > 自身速败超时，兜底不抢跑** | I | **用户终验通过（2026-09-24「应该都过了」）结案**（钉 `spec_bar146_腿starting超时_定罪重拉` 真值表四言+宽限>握手超时不变量；变异实咬：>= 翻 < → 红→复原绿；同日现场实证：回前台数秒通道自愈） | tests/tunnel_spec.rs |
| BAR-145 | 解析页 tmux 会话框命中映射（疑与网络抖动期布局/重排联动） | tmux 会话框点击命中下移 1-2 行（2026-09-23 深夜用户实报，当时网络极不稳定）。已收集证据：①手机点按遥测——落空点 (946,2369)/(952,2100)，命中带 kfm-na≈y1891-1932/nz≈2052，落空与命中差 60-70px≈1 格，方向与体感一致；②redroid（旧核、屏 1260x2560）常态+键盘弹起态均复现不了，点框正中正确命中；③Parser 烘焙 sig 含 (w,h,ime,bar_h,c1,c2,pt_epoch,tunnel_epoch)，「烘焙不吃栏高」假设已排除；④redroid `wm size` 被容器锁死改不了 2800，模拟路断。**2026-09-24 用户裁决挂起**：次日网络恢复后实测已正常，疑似只在昨夜极端网络不稳定窗口出现（假设方向：抖动期终端重排/尺寸事件与命中映射短暂脱节）。复现钥匙 = 网络极不稳定窗口 + 手机（1260x2800）实机；再发时第一动作 = 遥测点按坐标 + na-shot 截解析页像素对表。**2026-09-24 午再发即消（用户实报「又出现了」→ 准备对表时已自愈）→ 仪器先行落地**：android_app.rs 解析页点按抬手处埋 [touch] 遥测行——「解析页点按 起手(x,y) 抬手(x,y) → Session(i)/Kill(i) 行i「名」框y起-止 / 通道卡 Hit / 落空（屏寸/inset/bar/表滚/裁带）」，指位 vs UI 响应一条账，再发时 grep '\"stage\":\"touch\"' field-reports.log + na-shot 直接对表，不再靠运气窗口 | C | 已修待判（仪器观察期至 2026-10-02——退役闸 spec_bar145_仪器退役闸 转红强制裁决：无再现拆仪器+结案，有再现凭屏代账续修） | —（未修，挂起案）；**形态挂账（2026-09-24 晨班会）**：未修复挂起案，形态待复现后随修复同定——证据与复现钥匙已钉行内；同日仪器升级：[touch] 点按遥测入行（代码级留痕，取代手动记坐标）。**2026-09-24 午二度再现+首次对表**：[touch] 七连落空（y≈1644-1669，命中几何裁带 1733-2219、行0「amp」1733-1841），漂移 ~126px≈一行、方向与首发同（得比看到的低点）；后三按补低命中（1834/1881/2091 擦行缘）。但用户滑走后 na-shot 实测：画面行带与命中几何**完全咬合**（且会话数已从 3 变 4「psh」）——漂移是暂态，单侧几何死无对证。仪器二期（47b8249）：渲染侧 [bake] 日志（epoch/会话数/屏寸/inset/bar/裁带/全行带）+ [touch] 补 epoch/会话数，再三度再现时两条账直接对 epoch 差。当前头号嫌疑：会话数变更（4→3）窗口期渲染帧与命中快照不同代——dock 底锚布局下一行不差。**2026-09-24 晚三度再现对表（用户实报「又出现了」+关键线索「IME 召收一次必愈合」）**：本进程 11 按全自洽（两按落空均擦行上缘 1-2px 间隙，余 9 按行带/epoch/会话数与 [bake] 逐一代际咬合）——几何映射腿当庭排除；代码巡检推翻「排水不置脏」假说（parser_exec_done 尾部无条件 dirty，纹理与快照同代锁步机制面完整）。IME 召收必愈合 = 强制重烘即愈合，嫌疑收窄为：**①名次异代型**（纹理名单旧代——用户点看到的名字进的是别家，体感同行漂移，而 [bake] 无名单/[touch] 无屏代 = 账上完全隐形）；②纹理上传/图层腿漏检。仪器三期（本次随核）：[bake] 行补「名[...]」、[touch] 行补「屏代N」（parser_page::note_baked_epoch/baked_epoch 全局账）——再现时屏代滞后 = 陈旧纹理当庭定罪；屏代齐 = 排除纹理腿收窄意图/命名。**2026-09-24 晚定罪修复（用户拍板两臂「行啊，那先这样做吧」）**：力学根 = **底锚槽+动态卡高 × 名单翻动**——会话数增减 → 卡高变 → 全行位移 126px；9-23 夜遥测实证名单 4↔5↔8 横跳（网络换 IP → 重连风暴 → 刷新连发 + 服务器短命会话起灭，用户网络推测实证成立）。修 = ①**卡高容量化**（tmux_card_h/layout_in 吃 used_lines 第三报 cap_lines——容量只随屏幕空间，行/分隔线/按钮对卡底钉死，增减只动列表区底部空位；容量吃无键盘可视底，BAR-119「保底两行」旧修约废止升级为卡内账与压力全脱钩）②**命中吃屏代快照**（parser_page::note_baked_snap/baked_snap 全局账——命中唯一合法源 = 屏上正显示的那一代，眼手同尺的真义）。钉 spec_bar145_卡高容量化_行位与会话数脱钩/命中吃屏代快照（源码守卫）/仪器退役闸 + spec_bar119 挤压修约重写，变异三咬全中（卡高回潮/分隔线回潮/活体命中回潮）。**2026-09-25 四度再现（用户实报「点的行触发上一行」「点新窗触发重排——错位恰一按钮高」「用一会儿/发消息即自愈」）→ 旧修没修到真根**：当夜 [bake]/[touch] 全程自洽（6 按 epoch==屏代、行带恒 1481-1967、名单恒 4 会话零翻动、裁带恒）——几何账两两相等而肉眼错位仍在，证明断环在两账之外：**纹理滞在更早一代的可能两账都照不到**（[bake] 只在真烘时落行，该重烘没重烘 = 账上无声）。代码巡检补证：Parser 槽烘焙被 sig 七维门控（w/h/ime/bar_h/accent×2/插件代/隧道代/重启武装），命中几何=触摸时刻活体重算（parser_geom 同包）——活体输入变了而 sig 漏维 = 纹理滞留；自愈与终端活动/IME 召收强相关（两者都强制重烘）正合此力学。仪器四期（本次）：parser_page::note_baked_geo/baked_geo 屏代几何账（烘焙时刻 sig 七维+行带），[touch] 行尾附「几何对表 同/异」——再现时：异 = 纹理滞留，差维直接指认漏的 sig 维；同而肉眼仍漂 = 病灶在纹理之下（合成/表面层），仪器方向随之前移。**2026-09-25 凌晨五度再现终局定罪（用户发现稳定复现钥匙：熄屏→解锁→进 na 必现，3/3）**：三方对表——①用户系统截屏（02:30:28，发病窗口内实拍）tmux 卡四行位置与几何账逐行咬合（nz 框 1729-1841 ≈ 账 1733-1841）②自动 na-shot GL 帧（点按后 1s 内自动存证，旋转特征确认为真 GPU 合成帧）同样正确 ③[touch] 账：用户点 nz 视觉正中（y≈1787）na 只收到 y≈1650 → 落 kfm-na 带。**真根 = Android 输入边界坐标偏移**：na 收到的触摸 y 系统性偏小 ~130px（恰为状态栏高度）——窗口管理器唤醒后把输入侧窗口位置错记为状态栏之下（+126），显示侧实际全屏（0），两侧各用各的快照。熄屏重协商触发、IME 召收强制窗口重排故必愈合、内部仪器全在边界内侧故账账自洽而用户点错、「点新窗触发重排（恰错一按钮高）」同理所指。na 触摸路径 handle_touch 零变换（winit 原值直传），偏移在 winit/系统窗口层。修复方向：Java 皮 MotionEvent rawY vs y 对表仪器钉到具体层 → resume/焦点变化重贴 immersive 标志（沉浸式 desync 标准修法，待用户拍板做仪器 APK）。**2026-09-25 仪器 APK（用户拍板「好」）**：Java 皮三件套——①dispatchTouchEvent 记 raw vs xy + 窗顶/inset（raw−y=系统认定的窗顶；NativeActivity input queue 可能旁路本回调，sDispatchCount=0 本身是证据）②gate 通道十四 window-state-req → JNI dumpWindowStateFromGate 落 usr/tmp/window-state + motion-java.log（随时远测发病/健康两态）③onResume/onWindowFocusChanged 生命周期几何行（desync 第一现场时序）。钉 spec_bar145_winstate触发与钩子链。**2026-09-26 早六度证言**：用户实报昨日数度再现但稳定复现钥匙已失效（熄屏钥匙不再必现），新观察「**这是整个屏幕被移动了一行**」——与输入边界偏移定罪同向（显示/输入两侧窗口位置快照差），监控继续（[touch]/[bake] 账常驻 + 仪器 APK 待链路通装机）。**教训升形态**：观测矩阵新增「命中漂移」行（内部账自洽 ≠ 无罪——错位案必须先取显示真相+输入真相两条外部账） | tests/parser_page_spec.rs |
| BAR-147 | tunnel.rs（伴生死亡块全量审理 + companion_hold_until 封锁闸 + companion_established/companion_hold_secs 两纯函数） | 伴生 ssh 赛跑活锁（2026-09-24 晨「连接不上」排查中的共犯定罪）：QUIC 模式下 ssh 是 -R-only 伴生（9022 推送路），CGNAT 每 1-2 分钟换 IP 杀 TCP——伴生死亡块在 QUIC 分支里只做 `child=None` 记一行「段尾重拉」，**通用死亡块（BAR-129/142 的释放+退避）被整个跳过**（child 已被取走，`if let Some(c) = child.take()` 落空），直接落重生段零间隔 spawn → 撞服务器侧旧 sshd 尸体（9022 还在它手里）→ ExitOnForwardFailure 255 → 再撞——logcat「段尾重拉」1s 循环 + auth.log `bind 9022: Address already in use` 连发，双侧实录；自愈只靠旧 sshd 被 ClientAlive 收走。数据面不受影响（QUIC 扛着），但 9022 一倒 agent 管理通道全瞎。修 = 伴生死亡块补全量审理（lived/stderr 尾/释放判决照数据腿同一套）+ 重生段前加 `companion_hold_until` 封锁闸（到点放行，Reconnect/ResumeKick 命令优先）+ 零间隔禁令（任何死因至少睡 1s）。契约：**「收尸路径分叉」是退避逻辑的天坑——每条死亡路径都要过同一个「释放+退避」判决，不许有抄近道到 spawn 的分支** | I | **用户终验通过（2026-09-24「应该都过了」）结案**（钉 `spec_bar147_伴生重拉_撞口先释放_零间隔禁令` 真值表七言；变异双咬：>=2 翻 >2 → 红、封锁底 1 翻 0 → 红→复原绿；同日现场实证：回前台 9022 伴生数秒重绑） | tests/tunnel_spec.rs |
| BAR-148 | 提交链（hooksPath 旁路）× android_app.rs（parser 触点命中臂） | 提交闸门旁路漏网事故（2026-09-24 晨通道段改造轮自抓）：提交 bf6cabb 用 `git -c core.hooksPath=.git/hooks commit`——本仓闸门在 `.githooks`（git config core.hooksPath），覆盖后 pre-commit chain **整体被跳过**；漏网病灶 = android_app.rs parser 触点命中臂 `link_card::layout_in` 仍是旧三参调用（n_svc 残参，通道段改造卡高恒定后应两参）——宿主 check/clippy/test 全绿（cfg 盲区），本来 chain 第 6 步 `cargo check --target aarch64-linux-android` 必抓，闸被拆了才漏到编核才炸。修 = 摘除残参两参化 + 纪律升级：**提交永不许 `-c core.hooksPath` 覆盖——hooksPath 就是闸门本体，覆盖 = 拆闸**（AGENTS.md 纪律清单新增硬编码项）。契约：**cfg 盲区代码的唯一兜底是 chain 的 android check 步，任何「走捷径的提交姿势」都是在拆这唯一的兜底** | I | 已修（残参摘除 android 目标编核复绿；流程纪律级，无 A 档面） | —（tests:na，流程/纪律修复） |
| BAR-149 | android_app（ParserSig 第九维 + restart_armed_last 翻相泵）× tests/self_restart_spec（源码守卫钉） | 自重启钮面死钮面（2026-09-24 通道段 redroid 判卷轮定罪：遥测「自重启武装（3s 窗）」已到、截屏钮面仍「重启」不翻「再点确认」）。病灶双臂：①**烘焙 sig 漏维**——ParserSig 八维（w/h/ime/bar_h/c1/c2/pt_epoch/tunnel_epoch）不含武装态，Arm 时 dirty=true 来了帧但 `sigs.parser.feed` 判同不重构，涂装代码（termview 每烘读 restart_armed 翻文案翻错色）根本没机会跑；②**落回无泵**——armed 是纯时间函数（3s 过窗自动落回），没有任何事件驱动翻相，就算武装时重烘了，落回后钮面也卡死「再点确认」直到下次碰巧重烘。用户风险实锤：见字没变以为没点上 → 再点 = 窗内二击直接重启。修 = ①ParserSig 加第九维 restart_armed_now（真从 restart_armed( 喂）；②about_to_wait 加翻相泵（restart_armed_last 照准，翻相即置脏）。契约：**凡「时间函数驱动的视觉态」必须同时有——进 sig 的维 + 事件环的翻相泵，缺一则涂装与状态静默脱节** | I | **用户终验通过（2026-09-24「应该都过了」）结案**（源码守卫钉 `spec_bar149_武装态进烘焙sig_翻相泵在环`；变异双咬；redroid 全舞步判绿：武装→红字「再点确认」→3s 落回→「重启」） | tests/self_restart_spec.rs |
| BAR-150 | scripts/na-push-so.sh（推送段三道原子化）× scripts/test-na-push-so-atomic.sh（契约钉挂 chain 第 9 步） | 半截核落位（2026-09-24 实踩：推 9.7MB 热更核中途断流，2MB 半截核直接落位 hot/libkfm_na.so，靠 .last 回滚）。病灶 = 旧推送段 `na_ssh "cat > .new && { cp .last; mv .new .so; }"` 的 **&& 链假原子**：ssh 中途断流（vivo 冻结/切网）远端 cat 收到 EOF 照样退出 0，`&&` 放行，mv 把半截核当新核落位。修 = 三道：①.new 先落地（旧制保留）；②**md5 对账过了才许 mv**——对账失败 exit 65，.new 留远端待续传/诊断，现行核不动；③推前留档 .last（旧制保留）。契约：**「传输完成」的判据必须是对账（md5/尺寸），不许是 cat 的退出码——管道那头的 EOF 不分辨「传完了」和「断了」** | I | 已修（契约钉 test-na-push-so-atomic.sh 挂 chain 第 9 步：对账先于 mv/对账失败 exit 非零/mv 不挂 cat && 链三言；变异三咬全中：mv 挂回 cat 链→红、摘 exit→红、对账挪 mv 后→红→复原绿） | scripts/test-na-push-so-atomic.sh（chain 第 9 步） |
| BAR-159 | na-quic/lib.rs（client_config_rev/server_config_rev + REV_IDLE_TIMEOUT）× crates/na-server/main.rs（反连腿吃 rev 版配置） | QUIC 反连腿僵尸（2026-09-24 M4-5 兜底演练实踩；**原编号 BAR-151 与滚轮案重号，2026-09-26 改挂 BAR-159**）：systemd 重启 na-server 后，手机反连腿挂在 accept_bi——QUIC 死寂判死靠 idle 上限（沿用数据腿 4h），死信不来 → 看门狗不知腿死 → ssh 伴生不拉 → 9022 无人绑（推送路全断且看似「在线」）。病灶 = 数据腿僵尸有 e2e 探活收尸（本地口还在可打，BAR-140），**反连腿无本地可观测物**，协议层 idle 是它唯一的死亡检测——4h 等于没有。修 = 反连双腿专用 REV_IDLE_TIMEOUT=60s（client_config_rev/server_config_rev，keepalive 10s 的 6 倍：健康连接对端 ACK 续命稳活，死寂 1 分钟定罪；doze 冻结期计时器同冻，醒来一次性重拉，成本与 BAR-141 同级）。契约：**凡是「对端静死」无本地观测手段的连接，idle 上限就是它的死亡检测——取值必须按「兜底接管能忍受多久」定，不许抄长寿路的常量** | I | **用户终验通过（静默判过，2026-09-26）结案**（钉 spec_m4_反连死寂判死_常量契约：60s 钉死 + ≥3×keepalive + 远小于 4h + 装配面真实吃常量；变异实咬：改回 4h → 红→复原绿。**兜底演练复核全绿 2026-09-24**：新核 fb2437e 上机后重启 na-server——60s 级「反连腿死: timed out」定罪 → 伴生 -R ssh spawn 兜底 → 反连腿即重拉 → 角色 ReverseOnly→None 收编 → na-server 重认领持 9022 → na_ssh PROBE2-OK 走 QUIC 反连通，时序与设计逐行对上） | crates/na-quic/tests/rev_bridge_spec.rs |
| BAR-152 | ui/fx_spring.rs（spring_pos 欠阻尼→临界阻尼）× tests/fx_spring_spec.rs | 键盘 inset 弹簧过冲取消（2026-09-24 用户拍板「弹簧过冲效果取消吧，现在看起来多了终端内容后，视觉会很疲劳」）：像素级视口平移（1ac2ab7）落地后终端内容随 inset 弹簧走，欠阻尼 ζ=0.76 的 ≈2.5% 屏高过冲带整屏文字来回「墩一下」= 视觉疲劳；field-reports 实录定罪——弹起趋 754px 目标序列 720→776→769→758→754，776/769/758 越过目标回摆 = 过冲铁证。修 = spring_pos 欠阻尼（ζ=0.76）→ 临界阻尼 pos(t)=target+d(1+ωt)e^(−ωt)（ω=24，零过冲单调趋近，2800px 贴死 ≈440ms 比旧版 ≈500ms 略利落）；重定基/收敛贴死/600ms 兜底语义不动。契约：**跟随类动画（目标 100ms 轮询步进、内容随动）只许零过冲单调趋近——过冲的物理感在「内容随动」场景 = 整屏晃，任何跟随缝不许再引入欠阻尼** | C | **用户终验通过（2026-09-24「可以，效果不错」）结案**（钉 `spec_bar152_弹簧零过冲单调趋近` 双向三程零过冲+单调；变异双咬：过冲振铃项 → 钉红、ω 减半收敛超时 → 收敛钉红→复原绿；仪器复验：弹起 342→…→754 / 收起 748→322→87→0 全程单调零过冲） | tests/fx_spring_spec.rs |
| BAR-151 | src/scroll.rs TouchScroll（滚轮 tick 换算）× src/android_app.rs 像素通道分流 | 像素级滚动开关开时三报（2026-09-24 用户实报）：①tmux 会话里上下滚动无响应；②服务器终端滚动出乱码符号；③疑似失败后回退行级。①已仪器定罪：滚轮路 [scroll] 遥测实录 `d=16/20/20 ticks=0 mode=0x2b0b6`——旧实现把 moved_px 的逐事件像素位移直接 `(d / cell_h).trunc()`，慢拖每笔 <42px 恒 0 tick，余数全吞（旧行级路 moved 有 pending_px 挂账，像素通道接线时把这层弄丢了）。修 = scroll.rs 新增 wheel_ticks/wheel_pending（与 moved_px 同 slop 门同 last_y，位移先挂账再取整，往返借还对称）+ android_app 滚轮路改吃 wheel_ticks。钉 spec_bar151_滚轮挂账_慢拖累计成tick/往返净tick为零/slop段不计入（A 档考题先行红→绿），变异二咬全中（摘挂账→红、trunc 改 round→红→复原绿）。②乱码案 2026-09-24 晚定罪修复：用户复现路径实证（解析页点聚焦 tmux 标签 → 回终端滑动 → 乱码「三个数字一个 M」= \x1b[<64;列;行M 被 bash 回显）+ 日志链（「attach kfm-na 重孵已发 → 会话 opened」）+ 代码定罪——respawn_session 泵换心脏但网格/mode 原样保留，旧 tmux 化身的鼠标上报位带过境；新 PTY 在 attach 就绪前是裸 bash，窗内滚动即发滚轮序列回显乱码（网络越差窗越大，与「极端网络期高发」吻合）。修 = 重孵点模式清场：活跃臂喂 sess_mode::reset_seq（复位不清屏，BAR-135 正文零污染；attach 完成 tmux 自会 DECSET 重开），待机臂 sess_modes 快照清零。契约钉 spec_bar151_重孵清场_鼠标面全覆盖（复位序列必须含 ?1000/1002/1003/1006l + 鼠标面全在 managed_mask 内）。**2026-09-24 深夜实机复现补刀**：杀活跃 tmux 客户端实证乱码一屏（^[[<64;34;40M 连发）——首修只挂 respawn_session，而 attach/脱离走的是 respawn_named_with（点聚焦标签回 shell 的案发路），清场不到。补 = 清场抽共用 reset_modes_on_respawn 两条重孵路同挂；补钉 spec_bar151_清场序列_真网格鼠标位真清（termview_spec：DECSET 真置位 → reset_seq 喂真网格真清 → DECSET 可重开）。**③灵敏度挂账根治（2026-09-25 用户拍板「跟终端同款的像素级滚动」）**：「一次滑闪 5 行」真凶 = tmux 默认绑定 `WheelUpPane → send -X -N 5 scroll-up`（服务器 list-keys 实证）——滚轮 tick 路 1 tick 对端跳 5 行，挂账换算再细也救不了对端 5 倍率。根治 = 外置视口浏览态（契约 docs/active/设置页.md §3.5）：tmux 附体拖动起手抓 capture-pane 全史快照喂独立 Term，渲染/滚动/命中切源直滚快照（像素级 1:1）；live 照常喂，击键/切会话/attach/resize/鼠标位消失即退回最新。钉 termview_spec 浏览态三钉 + tmux_ctl_spec cmd_capture，变异三咬全中（不喂快照/exit 不清零头/——均即红） | C | **用户终验通过（静默判过，2026-09-26）结案**（①②仪器/实证定罪+钉死；③灵敏度由外置视口链 BAR-153~156 根治，整案销） | tests/scroll_spec.rs |
| BAR-153 | src/tmux_ctl.rs（cmd_capture 目标语法 + capture_strip_marker 验收）× src/android_app.rs（外置视口排水臂） | 外置视口首版上机即翻车（2026-09-25 午用户实报实录「会跳一行→can't find pane: =kfm-na→整页消失，回车还能操作 kimicode」）：①`capture-pane -t '=name'` 的 `=` 精确匹配前缀只对 session 目标成立，pane 目标不成立——tmux 报「can't find pane: =kfm-na」（服务器 tmux 3.4 当场复现定罪）；②致命在验收缺位：tmux 的 exit 码经 `sh -c` 传不回来，报错文本与快照同走 stdout——exec 通道 Ok(报错文本) 被当快照喂进 browse Term，整页被一行错误文本盖住（回车退浏览回 live 故还能操作）。修 = 双闸：①目标语法 `'=name:'`（精确会话 + 活动窗格，服务器实证可抓）②尾带 `&& echo KFM_CAP_OK` 成功标记 + capture_strip_marker 验收（无标记 = 抓取失败原地待命，垃圾永许不进浏览态）。契约：**凡是「输出与报错共用一条 stdout、exit 码传不回」的抓取通道，成功标记是唯一的验收闸——内容形似度判不得**（报错文本也是合法文本） | C | **用户终验结案（2026-09-26 早「行为看起来是被修复了，没有复制，正常滚动」——随 BAR-155/156 同一条体验链终验一并结案）**（钉 spec_cmd_capture_精确匹配带色全史 + spec_bar153_快照验收_标记判卷：真快照剥标记还净/报错文本 None/空与半截 None；钉名 2026-09-26 自 bar152 改挂 bar153，原系编号笔误） | tests/tmux_ctl_spec.rs |
| BAR-154 | src/termview.rs（swap_browse_term 锚补偿 + build_browse_term 后台解析入口）× src/android_app.rs（browse_suppress 抑制闸 + 抓取线程预建 Term 排水三分流） | 外置视口 v2 上机三报（2026-09-25 午后用户实报「往下拖动页面卡一下/滚动出界一行一行瞬间消失出现/kimicode 有输出时稍微上滚页面被静止」），field-reports [scroll] 遥测定罪三病灶：①**锚方向反了**——swap_browse 保持 display_offset 原值，而 display_offset 从底部量、新输出追加在底部，offset 不变 = 视口追新内容下移（遥测实录：刷新 offset=37→37→27 而里程 2586→2588 在涨）——每次刷新阅读位跳 N 行（「一行一行瞬间消失」），刷新间隙页面静止（1.5s 节流 + exec 往返）；②**同次触摸内触底后重返浏览态**——遥测实录序列「触底自动回 live →（手指未抬）在途抓取落地 → 切入(首抓) 补滚 -143px」，用户被扔回历史快照（「页面被静止」第二病灶）；③**快照解析占 UI 线程**——266KB ANSI（里程 4229 行）在触摸事件臂/about_to_wait 同步 Processor::advance = 「拖动卡一下」。修 = 三刀：①swap_browse_term 锚定**内容**守恒——offset' = offset + (新总量 - 旧总量)（追加多少抬多少，阅读位钉死，史顶溢出钳顶）；②browse_suppress 抑制闸——触底/击键退场置位，同次触摸内不再挂账/切入/发抓，抬手 Ended/切会话/attach 解锁；③快照 Term 挪抓取后台线程构建（exec 收文本 → 同线程剥 BAR-153 尾标 → build_browse_term → 转发预建 Term，UI 排水只换壳零解析）。契约：**「从底部量的偏移」在底部追加的流上不是稳定锚——锚定内容必须补偿追加量；触底/击键这类「 intent 反转」事件的效力范围 = 整次触摸，不许下一个事件帧就翻案** | C | **用户终验结案（2026-09-26 早「行为看起来是被修复了，没有复制，正常滚动」——随 BAR-155/156 同一条体验链终验一并结案）**（钉 spec_浏览态_原地刷新锚守恒 改补偿语义：追加 2 行 offset 2→4、可见内容同批 A03..A06；变异实咬：摘补偿 → 红→复原绿） | tests/termview_spec.rs |
| BAR-155 | src/ctrl_feed.rs（新模块：播种/续喂相位机）× src/android_app.rs（ctrl_ 族改薄壳） | v4 推流画布上机即死、推流从未生效（2026-09-25 晚用户实报「内容还是掉帧，只是把糊弄机制逼近」——报表实录「播种发出→头块无 KFMHDR 头行」5s 空转循环，跑的一直是 v3 轮询保底）。pty 实录原始流定罪双病灶：①`tmux -C attach-session` 命令行命令**自己的空回应块**（%begin/%end 零正文）最先到达，被当播种头块判负；真播种块随后到达时相位已回稳态全丢弃 → 永远播种不上；②（①修好才会暴露的连环病灶）头行认领后**头块的 %end** 被当 capture 块的 %end → 空 capture 提前 Build、真正文在 Building 相位被丢；③（①②修好上机后仍 100% 判负的根因，ff57cc7 存证版报表拿铁证：「首行=KFMHDR 2898 10000 5 50 %3」完全合法却判负）**pty 流每行带 \r 尾，parse_ctrl_line 只剥 \r 用于分类、ctrl_feed 拿到的原文还带 \r** → parse_seed_header 的 pane 段 "3\r" 数字解析失败 → 判负「头块有正文但无 KFMHDR 头行」。根因 = 状态机写在 android_app（cfg(android)，host 不可测），凭纸面设计没跑真流；行装配/剥 \r 留在壳层同理不可测。修 = 相位机抽纯逻辑模块 ctrl_feed.rs（六相位与 tmux 块序列一一对上：Steady/AwaitHeader/HdrEnd/AwaitBody/InCapture/Building；空块跳过靠「本块见过正文」判据；**feed_bytes 字节流唯一入口收编行装配+\r 剥除**——parse_seed_header 契约保持严格「输入必须已剥 \r」，剥除只许在本层；capture 行账 Vec 缝合——首行空行时 is_empty 缝丢 \r\n 的坑一并钉死），android_app 删 ctrl_buf/ctrl_on_line，ctrl_on_event 改 feed_bytes→ctrl_exec 动作薄壳。契约：**跨进程协议状态机不许落在 host 不可测的 cfg 壳里——先拿对端原始流 fixture 钉块序（fixture 必须真流形态 \r\n，理想化 \n 单行 fixture 测不到 \r 尾病灶），相位粒度与块序列一一对应，少一档就是一颗雷；字节→行→事件的每一道加工（装配/剥 \r/分类）同属状态机，在哪层做就在哪层钉** | I | **用户终验通过（2026-09-26 早「行为看起来是被修复了，没有复制，正常滚动」）结案**（钉 ctrl_feed_spec 八钉：实证流全序/病灶一二三回归/行跨包截半装配/播种窗口字节三路/判负三路/Notify 逼对账与 %exit；变异三咬全中：摘空块跳过 → 钉一红、HdrEnd 产 Build → 钉二红、摘 feed_bytes \r 剥除 → 八钉全红——均复原绿） | tests/ctrl_feed_spec.rs |
| BAR-156 | src/termview.rs（Canvas::build 增收游标 CUP 归位）× src/ctrl_feed.rs（Build 携头行游标）× src/android_app.rs（ctrl_exec 接线） | 浏览态动态行「静态复制」（2026-09-25 晚用户实报「视口稍微上滚，动态行在动，它上面还有一条它的静态内容复制在上面；追底正常」——追底走 live term 故正常，上滚切推流画布才见 = 画布播种病灶）。服务器 tmux 3.4 实录定罪（spinner 不在屏底的会话）：`capture-pane -p -e -S -` **恒发全屏含尾空行**（24 行屏：10 行文本+动态行+13 尾空行），KFMHDR 头行 cursor=(12,10) 而播种文本尾落屏底 23 行——旧制 Canvas::build 只喂 capture 文本，游标停在文本尾；续喂的 `\r\x1b[K` 原位更新帧按错位游标落屏底，真实游标行的快照旧帧永留 = 静态复制。头行的 cursor_x/cursor_y 本来就在协议里（cmd_ctrl_seed 第一件），ctrl_feed 只认领 pane 把游标丢了。修 = Build 动作携头行游标（x,y 屏相对 0 基），Canvas::build 喂完 capture 补 CUP `\x1b[y+1;x+1H` 归位再藏光标（超界 alacritty 自钳）；v3 快照臂 build_browse_term 无续喂、游标无发言权，占位 (0,0) 语义不变。契约：**凡「快照文本 + 续喂流」拼接的协议，快照的游标位是续喂锚——文本尾 ≠ 游标位（尾空行/尾剥白都会拖离），头件里带游标就必须消费它，丢了 = 续喂落点全靠运气** | I | **用户终验通过（2026-09-26 早「没有复制，正常滚动」）结案**（钉 termview_spec `spec_bar156_播种游标归位_动态行不复制`：实录 fixture 全屏含尾空行 + 续喂 \r\x1b[K 帧——新帧原位更新、旧帧零残留；ctrl_feed_spec 实证流全序钉补 Build 携游标断言；变异实咬：摘 CUP 归位 → BAR-156 钉红（旧帧残留断言命中 = 病灶复现）→复原绿） | tests/termview_spec.rs |
| BAR-157 | crates/na-quic/src/lib.rs（run_rev_server 认领循环重构） | 62694 反连腿上机后间歇全灭（手机侧 handshake 超时循环 150+ 次，9022 长期回落 ssh 兜底）。pcap 逐包定罪：手机 Initial 到网卡正常，服务器 20/23 次零回包；仅存回包 = 每 60s 一具陈尸（一小时前的死端口）的 PTO 退避序列（1,1,3,5,11,21s 整组 60s = REV_IDLE_TIMEOUT），23 次新鲜尝试全程零回包。病灶双件：①quinn 的 Incoming **在应用 accept 之前不回第一个包**（等应用裁决 accept/refuse/retry），而旧认领循环**串行 await 每个 Incoming 到终局**——客户端 8s 弃连（HANDSHAKE_TIMEOUT）后服务器要等满 60s idle 才收尸，一具陈尸把 accept 停摆 60s；弃尸来得比收尸快（10.5s/具 vs 60s/具）= 队列永远排不完，腿永久死；②服务期 accept 同样停摆——换网络的手机要等旧连接 60s idle 收尸才能握手。修 = accept 驱动独立任务 + 握手/验签每条 Incoming 一个任务（超时硬顶 HANDSHAKE_TIMEOUT×2，陈尸自己烂自己不拖累驱动）+ 认领走 mpsc channel（积压只留最新，服务期新认领 close 旧连接当场挤换——「新注册挤掉旧的」从等死变主动）。契约：**quinn server 的 accept 轮询是握手应答的唯一泵——任何「await 单条 Incoming 到终局再 accept 下一条」的串行认领都是陈尸放大器；弃尸是公网口的常态不是异常** | I | 已修实证通过（2026-09-26 午：修复版 na-server systemctl 重启后反连即认领（39.144.207.40），na-server 自持 127.0.0.1:9022，na_ssh 经 QUIC 反连桥全链 echo 实测通；钉 rev_bridge_spec `spec_m4_bar157_弃尸风暴_诚实客户端不被憋死`：弃尸 = 冻结线程保 socket——本机 drop 回 ICMP 秒收尸演不出 CGNAT 无声陈尸，idle 考场加速 2s；旧码红 3.9s 超时 = 病灶复现 → 修复绿 2.4s 全绿含回归四钉） | crates/na-quic/tests/rev_bridge_spec.rs |
| BAR-158 | src/scroll.rs（新纯逻辑 browse_pending_gate）× src/android_app.rs（拖动挂账/甩尾挂账/首抓落地三分流同闸） | 追底态下滚页面闪一下（2026-09-26 用户实报「不管哪个 tmux 窗口，追底状态下继续下滚，页面会闪烁一下」）。field-reports [scroll] 遥测实录定罪：「外置视口切入(推流画布): 补滚 -167.1px → 外置视口触底自动回 live」成对连发（同段还有 -64.8/-963.7/-11322.6px 组组如此）——追底态（live，offset=0）朝 live 方向（手指上推 d<0，符号约定 scroll.rs「手指向下=正=看历史」）的拖动本是无效方向，旧制挂账照单全收 → 切入浏览补滚负值被 scroll_px 贴底钳回 offset=0 → 下一笔移动立刻触底退场交还画布，切入→闪退 = 一闪；甩尾路同病灶（朝 live 甩尾挂负账，落地诱捕切入）。病灶本体 = 切入无方向闸（外置视口 v2 设计只想过「朝历史切入」，反向没人判卷）。修 = 方向闸抽纯逻辑 browse_pending_gate（挂账钳非负，只许朝历史净积压），三条切入路同闸：拖动挂账后 ≤0 即返；甩尾朝 live 在追底态不挂账且当场燃尽（不空泵脏帧）；首抓落地 dragging 臂加 pending>0 判据（0 = 全程朝 live，快照落温热账等下一手势）。契约：**「无效方向的位移」必须在挂账入口就拦死——放进状态机再靠边界钳回来，钳回瞬间的态跃迁（切入/退场换源）就是用户看得见的闪烁；方向闸与边界钳是两层，缺一就是这条** | C | **用户终验通过（2026-09-26「好了。感觉修复了」）结案**（钉 scroll_spec `spec_bar158_追底朝live位移不挂账`/`spec_bar158_朝历史净积压转正才切入`：实录同款 -11322.6px 大负位移恒 0/反向扣穿钳 0/混合手势负段清零正段重起；变异实咬：摘 .max(0.0) 钳 → 双钉全红→复原绿；上机遥测复验：vc1790395273 后负值补滚切入 0 次、触底闪退 0 次，甩尾臂方向闸实录拦杀 2 次） | tests/scroll_spec.rs |

/target
field-reports.log
/build
# GPU 期 0 尖刺（spikes/gpu）构建产物
spikes/gpu/target
spikes/gpu/build
# 本地字体覆盖（BAR-021）：商业字体只存在于本机，永不进库。
# chain.sh 第 1 步有防泄漏闸机械执法
/assets/fonts/local/

# 收尸包拉回现场(运行时证据,不入库)
autopsy/
mutants.out/
mutants.out.old/
overnight-power-*.log
